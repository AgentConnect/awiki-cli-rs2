#!/usr/bin/env node
'use strict';

const { readServerConfig } = require('./config.js');

readServerConfig(process.argv[2]);
process.stdout.write('# Generated by scripts/release/cli/render-nginx-download-zones.js.\n');
process.stdout.write('# HTTP-scope state only; limits are applied by awiki-cli-release.conf.\n');
process.stdout.write('limit_conn_zone $binary_remote_addr zone=cli_download_per_ip:10m;\n');
process.stdout.write('limit_conn_zone $server_name zone=cli_download_total:10m;\n');
