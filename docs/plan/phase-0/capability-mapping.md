# Phase 0 Capability Mapping Archive

本文保留为历史入口，因为 `awiki-cli docs phase-0` 仍引用该路径。原始 v1 script 到 v2 CLI 的能力映射已完成，不再作为当前实现计划维护。

当前能力归属：

- CLI 命令面：`docs/architecture/awiki-command-v2.md`
- SDK 能力边界和 CLI/SDK 分工：`docs/architecture/im-core-sdk-architecture.md`
- 功能说明入口：`docs/README.md`

当前代码已经以 `im-core` public services 为业务能力层，旧脚本级映射仅用于历史追溯。需要查看原始映射表时请使用 Git 历史。
