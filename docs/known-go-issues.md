# Known Go Issues And Deferred Fixes

This file records Go behavior or design debt found during the Rust parity port.
Parity work should reproduce observable Go behavior unless a deviation is
explicitly approved and documented here.

Translation rule: implement the Rust port one-to-one with the Go implementation
first. Do not mix optimization/refactoring goals into parity translation work.
When an optimization, cleanup, or Rust-native redesign looks useful, record it
below as a deferred optimization and keep the current translation aligned with
Go behavior. Deferred optimizations require a later, separate goal after parity
is proven.

Parallelism rule: use Native Agents for independent module/file slices when it
improves throughput. Code-writing Native Agents must use GPT-5.5 with xhigh
reasoning and a bounded, non-overlapping write scope; read-only parity/research
agents may use lighter settings.

| Area | Go reference | Issue / debt or optimization opportunity | Rust parity decision | Status |
| --- | --- | --- | --- | --- |
| Translation process | all Go files/modules | Potential Rust-native optimizations may be discovered while translating. | Record only; do not implement during parity translation unless needed to reproduce Go behavior or meet hard Rust safety/build constraints. | standing_rule |
| ANP registry gaps | `internal/anpsdk/registry.go` | Go's registry includes convenience symbols that the local Rust SDK does not expose as equivalent public APIs: `KeyType`, `GenerateKeyPairPEM`, free PEM decode functions, `GeneratedKeyPairPEM`, file-backed direct-E2EE stores, and high-level `MessageServiceE2EEClient`. | Rust now re-exports only existing local SDK authentication/proof/direct-E2EE symbols. Missing convenience APIs remain deferred until a consuming module proves they are required; at that point they should be added as an explicit SDK/facade lane with tests, not hidden in unrelated translation. | deferred_translation |
| Mail RPC profile/trace boundary | `internal/mail/client.go`, `internal/mail/service.go`, `internal/transportcfg/config.go`, `internal/traceutil/trace.go` | Go mail live RPC wraps calls with per-profile context timeouts and trace phases (`RPCPhase`, `EnsureJWTPhase`). The Rust shared HTTP client currently resolves timeout config when the client is constructed and the trace layer is not threaded through service calls. | Rust now wires live mail RPC through the shared authsdk Rustls HTTP client and preserves endpoint, auth, JWT, error, summary, and attachment behavior. Per-call profile-timeout wrappers and trace phase emission are recorded for a later transport/trace integration slice rather than adding a second ad hoc mail transport. | deferred_translation |
| Message service dependency boundary | `internal/cli/msg.go`, `internal/message/service.go`, `internal/message/secure_commands.go`, `internal/authsdk/*` | Non-dry-run direct/group messaging, attachment transfer, WebSocket proxy transport, and secure direct E2EE require auth session/JWT refresh, message-service RPC/WS clients, cache mutation, and E2EE provider decisions. Implementing them inside the dry-run CLI slice would force network/TLS and crypto choices too early. | Current Rust slice preserves Go command catalog, dry-run plan shapes, Cobra-like required flag failures, local text-file reads, and handler validation only. Real message-service execution is deferred to shared authsdk/session plus Rustls HTTP/WS and E2EE slices. | deferred_translation |
| Message signed service execution boundary | `internal/message/auth.go`, `internal/message/service.go`, `internal/authsdk/*` | Go direct send and attachment send combine RFC9421 origin proof with auth session/JWT refresh and service transport. Pulling service execution into the local proof slice would mix private-key signing with HTTP/WebSocket/authsdk dependency decisions. | Rust now translates local RFC9421 origin-proof generation with the local ANP Rust SDK. Auth session refresh, bearer behavior, transport execution, and cache mutation remain deferred to shared authsdk/message-service slices. | deferred_translation |
| AuthSDK service-client wiring boundary | `internal/authsdk/session.go`, service clients under `internal/{mail,content,site,identity,message}` | Go `Session` now has a translated generic HTTP execution layer, and each endpoint-specific service still needs profile timeouts, trace phases, identity-store JWT persistence, and CLI result/error mapping to be wired without duplicating auth transport policy. | Rust now translates `Session::do_json_rpc`, optional JSON-RPC, `Session::do_json`, optional JSON, `Session::ensure_jwt`, one-shot 401 retry via challenge headers or force-new signing, `HttpError`/`RpcError` mapping, and successful response token capture on top of the shared Rustls/std client. Live mail RPC, content page RPC, tenant site RPC, identity phone registration, and `id refresh-token` now reuse that layer or the same shared transport. Remaining endpoint-specific identity/message clients plus per-call profile/trace depth remain deferred to later service slices. | deferred_translation |
| Identity registration email/wait boundary | `internal/cli/id.go`, `internal/identity/service.go` | Go `Service.Register` supports email activation, optional wait/polling, and email status/send REST calls in addition to phone OTP registration. | Rust now implements the live phone `send_otp` and phone+OTP `register` paths needed by page system tests, plus Go-shaped dry-run plans for email/wait. Live email activation and polling remain a later identity-service slice. | deferred_translation |
| Identity profile/resolve/bind live boundary | `internal/cli/id.go`, `internal/identity/service.go`, `internal/authsdk/*` | Go live identity commands beyond phone register and refresh-token include bind, resolve, profile get/update, and related authenticated profile-service calls. | Rust now implements local identity commands, phone registration, and live `id refresh-token` on the shared Rustls/authsdk path. Live profile/resolve/bind execution remains deferred to later identity-service slices so command-specific result/error mapping can stay separated from the refresh-token parity slice. | deferred_translation |
| Workspace v2->v3 identity-service side effects | `internal/upgrade/migration_v0_to_v1.go`, `internal/identity/service.go`, `internal/transportcfg/config.go` | Go `workspaceV2ToV3Migration.Apply` constructs `identity.NewService` when the current identity list is non-empty, even if every identity is already non-k1. That can surface service-construction warnings, such as invalid CA bundle parsing, before any replacement loop runs. For actual k1 identities it calls `Service.ReplaceDID`, backs up old identity material, calls remote `did-auth.replace_did`, writes the new identity, and rebinds local SQLite owner state. | Rust now translates the local completion boundary only: missing/empty/non-k1 identity stores complete v2->v3; current k1 identity stores remain deferred at `workspace_2_to_3_replace_existing_k1_handle_dids`. Service construction warnings for non-k1 identity stores and real k1 replacement remain deferred to the shared authsdk/Rustls identity-service lane so HTTP/TLS dependency decisions are not mixed into this local migration slice. | deferred_translation |
| Config writer durability | `internal/config/write.go` | Go writes config through a temp file, fsync, chmod `0600`, rename, and directory sync. Windows intentionally skips parent-directory sync because Windows directory handles commonly reject it. | Rust now mirrors this durable-write boundary with std-only same-directory temp files, file sync, Unix file/dir modes, atomic rename, temp cleanup, Unix parent-directory sync, and Windows parent-directory sync no-op. Windows replacement semantics still need Windows CI coverage before claiming cross-platform runtime parity. | parity_behavior |
| Config YAML parser/serializer depth | `internal/config/config.go`, `internal/config/write.go` | Go uses `yaml.v3`, while the current Rust config path uses a hand-written scalar parser/renderer for translated fields. Complex YAML scalars, comment preservation, anchors, and exact marshal formatting are not fully equivalent. | Keep the hand-written path for bounded helper parity; full YAML parser/serializer replacement is a separate dependency/format decision so it is not mixed into helper translation. | deferred_translation |
| Hermes CLI/bridge boundary | `internal/cli/runtime.go`, `internal/runtime/hermesbridge/*`, `internal/config/write.go` | Go Hermes host-notify commands do more than mutate config: they configure Hermes route/secret, refresh/restart listener state, and manage bridge behavior. | Rust now translates Hermes config writer helpers, legacy webhook double-write, and the pure Hermes bridge helper boundary. Full `EnsureRoute`/`InspectRoute`, YAML read/write, bridge behavior, and listener refresh orchestration remain separate runtime parity slices. | deferred_translation |
| OpenClaw route confirmation boundary | `internal/cli/runtime_host_notify_routes.go`, `internal/runtime/openclawnotify/webhook.go` | Go non-dry-run `runtime host-notify openclaw route add` persists the route and sends one confirmation webhook message to the new route. That confirmation path requires the OpenClaw webhook HTTP client and TLS/client dependency selection. | Rust now translates local route input normalization, session-key parsing, registry persistence, list/remove, dry-run plans, and `host_notify_config_view.routes`. Confirmation sending is deferred to the future OpenClaw webhook client slice, which must use a Rustls-first HTTP/TLS stack and avoid OpenSSL/native-tls unless a documented Rustls parity failure exists. | deferred_translation |
| Doctor config/upgrade depth boundary | `internal/doctor/doctor.go`, `internal/config/config.go`, `internal/upgrade/*` | Go doctor inherits full `yaml.v3` parse errors and `upgrade.Inspect` meta/journal/detection details. Current Rust config parsing and upgrade detection are still narrower than Go. | The Rust doctor slice preserves the public 10-check report contract and local diagnostics that existing translated modules can support. Full YAML parser behavior and deep workspace-upgrade metadata inspection remain dedicated config/upgrade parity slices. | deferred_translation |
| Transport HTTP client depth boundary | `internal/transportcfg/config.go` | Go `NewHTTPClient` constructs an `http.Transport` with dial/keepalive/TLS handshake/response-header/idle timeout controls, connection pool limits, HTTP/2 intent, optional CA bundle appended to system roots, and TLS 1.2 minimum. Its custom transport does not set `Proxy`, so it differs from Go default clients used elsewhere. | Rust now translates the Rustls-first std-socket HTTP/1.1 boundary: config snapshot, dial/TLS/response-header timeouts, optional PEM CA bundle append to `webpki-roots`, proxy-free default constructor, fixed/chunked response bodies, Go-like CA bundle errors, and Go-compatible tolerance for missing TLS `close_notify` after a complete HTTP-framed response. HTTP/2 `ForceAttemptHTTP2`, exact system-root parity, keepalive/pooling reuse, streaming bodies, and deeper per-call service timeout/trace integration remain deferred. | deferred_translation |
| Update network fetch boundary | `internal/update/update.go` | Go `CheckFresh` fetches npm metadata from npmjs and npmmirror over HTTPS with a 3-second timeout and writes cache metadata with restricted permissions. It uses `&http.Client{Timeout: ...}` with nil transport, so Go default transport proxy env behavior applies. | Rust now translates this narrow update fetch/writeback boundary using the shared Rustls/std client through an explicit proxy-env constructor, preserving `HTTP_PROXY`/`HTTPS_PROXY` and `NO_PROXY` behavior separately from proxy-free `transportcfg.NewHTTPClient`. Broader service HTTP/WebSocket clients still need a separate authsdk/service dependency decision. | parity_behavior |
| Root update preflight guard | `internal/cli/root.go` | Go runs a global update-policy guard before most commands, can block unsupported non-dev builds, and injects newer-version warnings. | Rust now translates the root guard into `app/update_preflight.rs`: command exemptions, soft-fail errors, verbose logging, `version_unsupported` exit code 3, npm primary/mirror hint, and warning prepending are preserved. The visible block path is unit-tested through a test-only current-version hook because default development builds intentionally remain non-blocking in Go and Rust. | parity_behavior |
| Local websocket bridge I/O boundary | `internal/runtime/config.go`, `internal/runtime/bridge_unix.go`, `internal/runtime/bridge_windows.go`, `internal/runtime/listener/server.go` | Go bridge endpoint helpers are local, but full bridge execution needs Unix socket or Windows named-pipe I/O, deadlines, health probes, listener server request handling, and message-service fallback behavior. Pulling all of that into endpoint normalization would mix local path parity with runtime service execution. | Rust now translates the endpoint/default/normalization/preparation/availability and request/response/error shape subset only. Actual local bridge I/O, listener server execution, and WebSocket service behavior remain separate runtime/message service slices. | deferred_translation |
| Replace-DID side effects | `internal/cli/id.go`, `internal/identity/service.go`, `internal/store/rebind.go` | Non-dry-run `id replace-did` generates new e1 DID/key material, calls `did-auth.replace_did`, backs up old sensitive material, rebinds SQLite owner state, and cleans E2EE state. | Rust now has the store-only `rebind.go` helper parity for owner-DID rebind and E2EE cleanup. The public CLI still exposes only the dry-run danger plan; non-dry-run identity replacement, authsdk `did-auth.replace_did`, backup/writeback, and CLI result integration remain deferred. | deferred_translation |
| Group service RPC boundary | `internal/cli/group.go`, `internal/message/group_service.go` | Non-dry-run group commands require registered identity auth, message service RPC, group membership state, and later group E2EE surfaces. | Current Rust slices translate the non-E2EE `group.go` dry-run request-shape contracts only. Remote group lifecycle is deferred to the shared authsdk/message service slice. | deferred_translation |
| Group wire execution boundary | `internal/message/group_wire.go`, `internal/message/group_service.go`, `internal/authsdk/*` | Go group wire builders are pure request constructors, but non-dry-run execution also needs auth session/JWT refresh, transport, status/error mapping, cache mutation, and service lifecycle orchestration. | Rust now translates the base/local and E2EE `group_wire.go` request builders plus origin-proof auth values. Service execution remains deferred to the shared authsdk/message-service slice. | deferred_translation |
| Group E2EE execution boundary | `internal/cli/group_e2ee.go`, `internal/message/group_e2ee_service.go` | Non-dry-run group E2EE commands require MLS provider state, hidden P6 service APIs, focused system-test gating, and stricter storage/security checks. | Current Rust slices translate static `group e2ee ... --dry-run` plans/schema and hidden E2EE request builders only. Real `anp-mls` execution, hidden service RPCs, and storage/security validation remain a separate MLS/provider slice. | deferred_translation |
| Page content RPC trace/profile boundary | `internal/cli/page.go`, `internal/content/service.go`, `internal/transportcfg/config.go`, `internal/traceutil/trace.go` | Go content page live RPC wraps service calls with transport profiles, context timeouts, and trace phases through `identity.RemoteClient.AuthenticatedRPCCallProfile` and `EnsureJWTPhase`. | Rust now wires non-dry-run `page create/list/get/update/rename/delete` through the translated content client, authsdk session bootstrap/JWT persistence, shared Rustls HTTP execution, and Go-style CLI error mapping; focused `awiki-system-test tests_v2/page` passes against `awiki.info`. Per-call profile-timeout wrappers and trace phase emission remain later parity work. | deferred_translation |
| Page dry-run validation boundary | `internal/cli/page.go`, `internal/content/service.go` | Go dry-run validates `page create` slug/title and markdown source conflicts before the service boundary, but does not validate visibility choices or reject empty `page update` plans; those checks happen later in non-dry-run service methods. | Rust preserves the Go dry-run boundary exactly and records richer validation as a possible later service-slice behavior, not a dry-run optimization. | parity_behavior |
| Msg required-flag error boundary | `internal/cli/msg.go` | Cobra required-flag failures for commands like `msg history --with`, `msg attachment download --message-id --output`, and `msg secure init/repair --with` are surfaced before handler execution as `internal_error` exit code 1, not the handler-level `invalid_argument` exit code 2 used for semantic validation. | Rust preserves this CLI boundary for parity instead of normalizing all user mistakes to one error class. | parity_behavior |
| Tenant site RPC trace/profile boundary | `internal/cli/site.go`, `internal/site/service.go`, `internal/authsdk/*`, `internal/transportcfg/config.go`, `internal/traceutil/trace.go` | Go site live RPC wraps service calls with transport profiles, context timeouts, and trace phases through `identity.RemoteClient.AuthenticatedRPCCallProfile` and `EnsureJWTPhase`. | Rust now wires non-dry-run `site root/page` commands through the translated site client, authsdk session bootstrap/JWT persistence, shared Rustls HTTP execution, and Go-style CLI error mapping including `forbidden`. Per-call profile-timeout wrappers and trace phase emission remain later parity work. | deferred_translation |
| Site required-flag error boundary | `internal/cli/site.go` | Cobra required-flag failures for `site` commands are surfaced before handler execution as `internal_error` exit code 1. Handler-level markdown/body-source failures remain `invalid_argument` exit code 2. | Rust preserves this CLI boundary for parity instead of normalizing all user mistakes to one error class. | parity_behavior |
