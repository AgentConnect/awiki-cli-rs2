# Known Go Issues And Deferred Fixes

This file records Go behavior or design debt found during the Rust parity port.
Parity work should reproduce observable Go behavior unless a deviation is
explicitly approved and documented here.

Translation rule: implement the Rust port one-to-one with the Go implementation
first. Do not mix optimization/refactoring goals into parity translation work.
When an optimization, cleanup, or Rust-native redesign looks useful, record it
below as a deferred optimization and keep the current translation aligned with
Go behavior. Deferred optimizations require a later, separate goal after parity
is proven.

Parallelism rule: use Native Agents for independent module/file slices when it
improves throughput. Code-writing Native Agents must use GPT-5.5 with xhigh
reasoning and a bounded, non-overlapping write scope; read-only parity/research
agents may use lighter settings.

| Area | Go reference | Issue / debt or optimization opportunity | Rust parity decision | Status |
| --- | --- | --- | --- | --- |
| Translation process | all Go files/modules | Potential Rust-native optimizations may be discovered while translating. | Record only; do not implement during parity translation unless needed to reproduce Go behavior or meet hard Rust safety/build constraints. | standing_rule |
| ANP registry remaining facade gaps | `internal/anpsdk/registry.go` | Go's registry is a broad convenience facade over ANP key material, authentication, proof, direct-E2EE stores, and the high-level message-service E2EE client. | Rust now exposes the translated local facade symbols needed by current consumers, including Go-shaped key-material helpers (`KeyType`, `GenerateKeyPairPEM`, free standard PEM parse helpers, and `GeneratedKeyPairPEM`) plus file-backed direct-E2EE stores. Runtime PEM parsers still reject legacy `ANP ...` labels; compatibility remains only in the identity migration path. Remaining facade gaps should be added only when a consuming module requires them; the high-level message-service E2EE adapter is tracked under the secure message client row rather than as a simple registry alias. | deferred_translation |
| Identity private-key compatibility migration | `internal/identity/key_compat.go` | Go loads existing identities through a compatibility pass that rewrites legacy ANP private PEM labels and SEC1 EC private keys to standard PKCS#8 before auth/proof code reads the files. | Rust now translates this migration as a load-time compatibility path for key-1, E2EE signing, and E2EE agreement private keys. This is parity behavior, not a broader decision to accept legacy ANP labels in ordinary runtime parsers. | parity_behavior |
| Mail RPC trace boundary | `internal/mail/client.go`, `internal/mail/service.go`, `internal/transportcfg/config.go`, `internal/traceutil/trace.go` | Go mail live RPC wraps calls with trace phases (`RPCPhase`, `EnsureJWTPhase`) in addition to per-profile context timeouts. | Rust wires live mail RPC through the shared authsdk Rustls HTTP client and preserves endpoint, auth, JWT, error, summary, attachment behavior, profile timeout caps, method-level RPC trace phases, and caller-labeled `mail_bootstrap` JWT trace without a nested `business_rpc:get_me`. Remaining mail trace depth is local notification DB/cache paths, not remote RPC/JWT emission. | parity_behavior |
| Message service dependency boundary | `internal/cli/msg.go`, `internal/message/service.go`, `internal/message/contact_sync.go`, `internal/message/secure_commands.go`, `internal/authsdk/*` | Non-dry-run direct/group messaging, attachment transfer, WebSocket proxy transport, and secure direct E2EE require auth session/JWT refresh, message-service RPC/WS clients, cache mutation, and E2EE provider decisions. Implementing them inside the dry-run CLI slice would force network/TLS and crypto choices too early. | Rust now translates ordinary direct text, direct inbox/history contact sync, non-E2EE group text/lifecycle execution, and direct/group attachment send/download on the shared authsdk plus Rustls HTTP path, including direct `msg send/inbox/history/mark-read`, group lifecycle/read/member/message operations, attachment slot/upload/commit/download-ticket/object download flows, local SQLite cache upserts, direct contact handle rebinding/history, handle-history cache expansion, service error mapping, JSON-RPC 1401 refresh/retry, profile timeout caps, and shared RPC/JWT trace phases. Attachment send/download force HTTP and warn under `runtime.mode=websocket`. Direct E2EE production send/init/repair/retry and best-effort prekey publishing before direct inbox/history reads are now translated on the same Rustls/std HTTP client. Remaining secure-direct depth is WebSocket/local bridge secure execution, foreground listener/runtime processing, and broader awiki-system-test acceptance. Group E2EE/MLS, OpenClaw host notify, runtime listener foreground/server contact-sync wiring, and deeper fallback trace wiring remain separate parity slices. | deferred_translation |
| Message signed service execution boundary | `internal/message/auth.go`, `internal/message/service.go`, `internal/authsdk/*` | Go direct/group send and attachment send combine RFC9421 origin proof with auth session/JWT refresh and service transport. Pulling all service execution into the local proof slice would mix private-key signing with HTTP/WebSocket/authsdk dependency decisions. | Rust now translates local RFC9421 origin-proof generation with the local ANP Rust SDK and uses it for ordinary direct text, non-E2EE group lifecycle/text send, and direct/group attachment manifest send over the shared authsdk/Rustls HTTP path. Secure direct E2EE, group E2EE/MLS, WebSocket transport, and remaining cache fallback semantics stay deferred to dedicated message slices. | deferred_translation |
| AuthSDK service-client wiring boundary | `internal/authsdk/session.go`, service clients under `internal/{mail,content,site,identity,message}` | Go `Session` provides a generic HTTP execution layer while endpoint-specific services layer profiles, trace phases, identity-store JWT persistence, and CLI result/error mapping on top. | Rust now translates `Session::do_json_rpc`, optional JSON-RPC, `Session::do_json`, optional JSON, `Session::ensure_jwt`, profile-aware variants, one-shot 401 retry via challenge headers or force-new signing, `HttpError`/`RpcError` mapping, successful response token capture, service profile timeout caps, and shared RPC/JWT trace hooks on top of the shared Rustls/std client. Live mail/content/site/identity/message clients reuse that layer, and direct message contact sync now uses the existing identity DID->handle lookup path. Remaining depth is local DB/fallback trace plumbing and full Go context deadline coverage outside response reads. | deferred_translation |
| Identity profile/resolve/bind live boundary | `internal/cli/id.go`, `internal/identity/service.go`, `internal/authsdk/*` | Go live identity commands beyond phone register and refresh-token include bind, resolve, profile get/update, and related authenticated profile-service calls. | Rust now implements local identity commands, phone registration, and live `id refresh-token` on the shared Rustls/authsdk path. Live profile/resolve/bind execution remains deferred to later identity-service slices so command-specific result/error mapping can stay separated from the refresh-token parity slice. | deferred_translation |
| Workspace k1 replacement service-warning edge | `internal/upgrade/migration_v0_to_v1.go`, `internal/identity/service.go`, `internal/transportcfg/config.go` | Go constructs `identity.NewService` for any non-empty summary list before filtering k1 identities, so an all-non-k1 current store can still surface service-construction warnings such as invalid CA bundle parsing. | Rust preserves this ordering at the shared imported/current k1 replacement helper boundary: non-empty summary lists preflight the same Rustls/std HTTP client CA-bundle construction before filtering non-k1 identities, and failures become the Go-shaped `Automatic k1 to e1 DID replacement was skipped: ...` upgrade warning while the migration still stamps schema version 3. | parity_behavior |
| Config writer durability | `internal/config/write.go` | Go writes config through a temp file, fsync, chmod `0600`, rename, and directory sync. Windows intentionally skips parent-directory sync because Windows directory handles commonly reject it. | Rust now mirrors this durable-write boundary with std-only same-directory temp files, file sync, Unix file/dir modes, atomic rename, temp cleanup, Unix parent-directory sync, and Windows parent-directory sync no-op. Windows replacement semantics still need Windows CI coverage before claiming cross-platform runtime parity. | parity_behavior |
| CLI parser Cobra depth | `internal/cli/root.go` | Go delegates parsing to Cobra/pflag, while Rust uses a hand-written command parser. Cobra rejects unknown command-local flags before handlers run and renders them as `internal_error` exit 1. Other Cobra details such as short flags, help text rendering, and every edge of argument/flag ordering are still broader parser parity work. | Rust now preserves the unknown command-local long-flag boundary for known commands by validating local flags against `cmdmeta` before value parsing. Keep further parser improvements as dedicated parity slices instead of broad rewrites. | deferred_translation |
| Config YAML parser/serializer depth | `internal/config/config.go`, `internal/config/write.go` | Go uses `yaml.v3`, while the current Rust config path uses a hand-written scalar parser/renderer for translated fields. Complex YAML scalars, comment preservation, anchors, and exact marshal formatting are not fully equivalent. | Keep the hand-written path for bounded helper parity; the parser now preserves `#` inside simple single-quoted and double-quoted scalar values for translated fields while still stripping real inline comments. Full YAML parser/serializer replacement remains a separate dependency/format decision so it is not mixed into helper translation. | deferred_translation |
| Hermes CLI/bridge boundary | `internal/cli/runtime.go`, `internal/runtime/hermesbridge/*`, `internal/config/write.go`, `scripts/hermes_notify_adapter.py`, `scripts/host_notify_webhook_server.py` | Go Hermes host-notify commands do more than mutate config: they configure Hermes route/secret, refresh/restart listener state, manage bridge behavior, and launch the Python notify adapter asset. Go also carries a local host-notify webhook fan-out helper for architecture validation and manual/local testing. | Rust now translates Hermes config writer helpers, legacy webhook double-write, the shared `refreshListenerForHostNotifyChange` listener-refresh behavior on the current Rust local-listener boundary, the pure Hermes bridge helper boundary, read-only `InspectRoute`, pure/local `EnsureRoute` route mutation, local-file `runtime host-notify hermes setup` writes, deterministic `service.go` helper behavior for service names/config shape/adapter command shape/status aggregation/lifecycle branch planning/hidden service-run detection/health wait/Apply branch planning, local adapter child process start/wait/stop/kill helpers, hidden `runtime host-notify hermes bridge service-run` dispatch with Go-equivalent bridge config/adapter-plan preflight and local adapter process execution until SIGTERM/SIGINT shutdown, the byte-identical Go Python adapter script plus its Python unit tests as repository assets, and the byte-identical Go host-notify webhook helper as a dev/test asset. Platform service install/start/stop/restart/uninstall, real service status lookup/control, lifecycle plan execution, owned HTTP health probing, bridge `Apply` execution from non-dry-run setup, full non-dry-run setup readiness, Windows service-control shutdown parity, and release archive inclusion for runtime/helper assets remain separate runtime/release parity slices. | deferred_translation |
| OpenClaw `set --hook-url` validation ordering | `internal/cli/runtime.go`, `internal/runtime/openclawnotify/config.go` | Go returns the dry-run `host_notify_openclaw_set` plan before validating whether the supplied hook URL uses a loopback host. This allows remote/non-loopback hook URLs to appear in dry-run plans even though live writes reject the same values. | Rust preserves this observable ordering: dry-run echoes the raw hook URL and does not write config, while non-dry-run still validates loopback before persistence. Do not "fix" this by moving validation before dry-run in the parity phase; stricter dry-run validation can only be considered as a later behavior change after Go parity. | parity_behavior |
| OpenClaw route confirmation boundary | `internal/cli/runtime_host_notify_routes.go`, `internal/runtime/openclawnotify/webhook.go`, `internal/runtime/openclawnotify/config.go` | Go non-dry-run `runtime host-notify openclaw route add` persists the route and sends one confirmation webhook message to the new route. OpenClaw settings can come from AWiki config, `OPENCLAW_GATEWAY_PORT`, `OPENCLAW_HOOK_TOKEN`, and OpenClaw JSON config. | Rust now translates route persistence plus confirmation sending on the existing Rustls/std HTTP client, including explicit AWiki config hook URLs, `OPENCLAW_GATEWAY_PORT`, OpenClaw JSON config port/path/token probing, optional config/env/OpenClaw bearer token precedence, Go-style loopback URL validation, 2xx/`ok`/`runId` acceptance, duplicate-route no-send behavior, and post-persistence warning semantics on confirmation failure. Remaining OpenClaw work is listener/service-manager/Hermes/runtime behavior, not this settings/webhook boundary. | parity_behavior |
| Doctor config/upgrade depth boundary | `internal/doctor/doctor.go`, `internal/config/config.go`, `internal/upgrade/*` | Go doctor inherits full `yaml.v3` parse errors and `upgrade.Inspect` meta/journal/detection details. Current Rust config parsing and upgrade detection are still narrower than Go. | The Rust doctor slice preserves the public 10-check report contract and local diagnostics that existing translated modules can support. Full YAML parser behavior and deep workspace-upgrade metadata inspection remain dedicated config/upgrade parity slices. | deferred_translation |
| Transport HTTP client depth boundary | `internal/transportcfg/config.go` | Go `NewHTTPClient` constructs an `http.Transport` with dial/keepalive/TLS handshake/response-header/idle timeout controls, connection pool limits, HTTP/2 intent, optional CA bundle appended to system roots, and TLS 1.2 minimum. Its custom transport does not set `Proxy`, so it differs from Go default clients used elsewhere. | Rust now translates the Rustls-first std-socket HTTP/1.1 boundary: config snapshot, dial/TLS/response-header timeouts, per-request profile timeout caps that can shorten but not extend the base response timeout, optional PEM CA bundle append to `webpki-roots`, proxy-free default constructor, fixed/chunked response bodies, Go-like CA bundle errors, Go-compatible tolerance for missing TLS `close_notify` after a complete HTTP-framed response, and shared service RPC/JWT trace hooks. HTTP/2 `ForceAttemptHTTP2`, exact system-root parity, keepalive/pooling reuse, streaming bodies, and full Go context deadline coverage for dial/TLS/write phases remain deferred. | deferred_translation |
| Update network fetch boundary | `internal/update/update.go` | Go `CheckFresh` fetches npm metadata from npmjs and npmmirror over HTTPS with a 3-second timeout and writes cache metadata with restricted permissions. It uses `&http.Client{Timeout: ...}` with nil transport, so Go default transport proxy env behavior applies. | Rust now translates this narrow update fetch/writeback boundary using the shared Rustls/std client through an explicit proxy-env constructor, preserving `HTTP_PROXY`/`HTTPS_PROXY` and `NO_PROXY` behavior separately from proxy-free `transportcfg.NewHTTPClient`. Broader service HTTP/WebSocket clients still need a separate authsdk/service dependency decision. | parity_behavior |
| Root update preflight guard | `internal/cli/root.go` | Go runs a global update-policy guard before most commands, can block unsupported non-dev builds, and injects newer-version warnings. | Rust now translates the root guard into `app/update_preflight.rs`: command exemptions, soft-fail errors, verbose logging, `version_unsupported` exit code 3, npm primary/mirror hint, and warning prepending are preserved. The visible block path is unit-tested through a test-only current-version hook because default development builds intentionally remain non-blocking in Go and Rust. | parity_behavior |
| Local websocket bridge I/O boundary | `internal/runtime/config.go`, `internal/runtime/bridge_unix.go`, `internal/runtime/bridge_windows.go`, `internal/runtime/listener/server.go` | Go bridge endpoint helpers are local, but full bridge execution needs Unix socket or Windows named-pipe I/O, deadlines, health probes, listener server request handling, and message-service fallback behavior. Pulling all of that into endpoint normalization would mix local path parity with runtime service execution. | Rust now translates endpoint/default/normalization/preparation, Unix availability and Unix bridge call/listen/probe I/O, Windows named-pipe default/blank-normalization/prefix-validation helpers, and request/response/error shape parity. Actual Windows named-pipe dial/listen/availability/health I/O, listener server execution, and WebSocket service behavior remain separate runtime/message service slices. | deferred_translation |
| Runtime listener foreground notification wiring | `internal/runtime/listener/contact_sync.go`, `internal/runtime/listener/server.go`, `internal/runtime/listener/{host_notify.go,openclaw_host_notify.go}` | Go foreground listener notification handling parses direct/mail/group incoming and group state-change notifications, syncs incoming contacts, enriches host notifications with handles, stores direct/mail/group records, mutates group/member state, formats/delivers host notifications, dispatches host notifications, and persists host-notify `LastError` status changes. Full parity also requires the foreground WebSocket/session processing loop, direct-secure E2EE normalization execution, local queue delivery, and listener server state. | Rust now translates the deterministic `contact_sync.go` helper as `runtime::listener_contact_sync`, the direct/mail/group/group-state parser subset as `runtime::listener_message_records`, the pure host-notification normalizer/enrichment helper as `runtime::host_notify`, the pure OpenClaw host-notify formatting helper as `runtime::openclaw_host_notify`, the route/action planner as `runtime::listener_notification_plan`, a single-notification side-effect executor as `runtime::listener_notification_execute`, the changed-only host-notify `LastError` status mutation helpers, a small `runtime::listener_notification_handler` composition boundary, the `runtime::listener_handle_lookup` remote DID-to-handle helper, the `runtime::listener_identity_watch` identity-discovery control-flow helper, the `runtime::listener_session_bootstrap` decision helper for Go `ensureSession`, the `runtime::listener_session_methods` helper for Go `session` accessors/client-close/initial-signal methods, the `runtime::listener_supervisor_init` initialization/cleanup helper for Go `NewSupervisor`, the `runtime::listener_supervisor_shutdown` shutdown-order helper for Go `Supervisor.Close`, the `runtime::listener_foreground` orchestration helper for Go `Supervisor.Run`/`startSocket`/`acceptLoop`, the `runtime::listener_bridge_dispatch` outer bridge request flow helper for Go `handleBridgeRequest`, the `runtime::listener_bridge_connection` injected execution adapter for Go bridge framing plus RPC send/local mark-read order, the `runtime::listener_local_notification_flush` injected adapter for Go queued local-notification flush-to-handler order, and the `newService` config shape, `serviceStatusFor` result mapping, `RunService` outer action plan, plus `serviceProgram.Start`/`Stop` state/action planner in `runtime::listener_service`. The executor applies Go-ordered local SQLite/contact/host-sink side effects, preserves best-effort host notify failure reporting, exposes a three-way host-notify status update signal, and has an executor-plus-status entry point that sets, clears, or leaves status unchanged with Go changed-only write behavior. The real foreground WebSocket loop, secure backlog replay, and local secure-ack delivery now pass a Go-equivalent remote handle lookup callback instead of `None`, so first-seen direct/group sender DIDs can enrich host-notify `sender_handle` when user-service has a handle binding. The foreground runtime also owns and shares the local secure-ACK notification queue, queues managed-but-not-yet-connected local ACK notifications, and flushes queued local notifications after session connect before secure backlog polling. Remaining deferred depth is real `servicepkg.New`/service-manager execution, real platform service status lookup/control, real `svc.Run` execution, real `serviceProgram` execution, real accept-loop socket iteration and `handleConn` goroutine spawning, full owned-session locking/runtime lifecycle depth, Windows named-pipe I/O, notification ping timeout parity, bridge RPC transport reuse, and full service runtime. | deferred_translation |
| Runtime listener foreground signal/process execution | `internal/runtime/listener/run_foreground_unix.go`, `internal/runtime/listener/run_foreground_windows.go`, `internal/runtime/listener/sysproc_unix.go`, `internal/runtime/listener/sysproc_windows.go` | Go foreground mode installs different signal sets by platform and Go Unix child process setup mutates `exec.Cmd.SysProcAttr` with `Setsid: true`; Windows child setup is a no-op. | Rust now records the deterministic platform plan in `runtime::listener_service` and wires actual listener shutdown signals through `runtime::listener_shutdown_signal`: Unix installs SIGINT/SIGTERM handlers, Windows installs a console Ctrl-C/Ctrl-Break handler, the handler only flips an atomic signal flag, and the foreground supervisor wait promotes that flag into the existing supervisor shutdown flag before normal listener cleanup. Unix subprocess tests prove `runtime listener run` exits cleanly and removes pid/status/socket artifacts on SIGTERM and SIGINT. Real child process `SysProcAttr`/spawn integration remains helper-only and deferred to a later process-management slice. | deferred_translation |
| Group service RPC boundary | `internal/cli/group.go`, `internal/message/group_service.go` | Non-dry-run group commands require registered identity auth, message service RPC, group membership state, and later group E2EE surfaces. | Rust now translates the ordinary non-E2EE group HTTP lifecycle/read/member/message path and forced-HTTP group attachment send/download over the shared authsdk/message-service HTTP stack, including service profile timeout caps and shared RPC/JWT trace phases. Group E2EE/MLS execution, WebSocket/local bridge/runtime listener fallback, cache fallback depth, and deeper local DB/handle/fallback trace wiring remain deferred. | deferred_translation |
| Group wire execution boundary | `internal/message/group_wire.go`, `internal/message/group_service.go`, `internal/authsdk/*` | Go group wire builders are pure request constructors, but non-dry-run execution also needs auth session/JWT refresh, transport, status/error mapping, cache mutation, and service lifecycle orchestration. | Rust now translates the base/local group wire builders and executes ordinary non-E2EE group RPCs plus group attachment manifest send/download over the shared Rustls/authsdk path. E2EE request builders remain wire-only; group E2EE execution and WebSocket/local bridge transport remain deferred to dedicated slices. | deferred_translation |
| Group E2EE execution boundary | `internal/cli/group_e2ee.go`, `internal/message/group_e2ee_service.go` | Non-dry-run group E2EE commands require MLS provider state, hidden P6 service APIs, focused system-test gating, and stricter storage/security checks. | Current Rust slices translate static `group e2ee ... --dry-run` plans/schema and hidden E2EE request builders only. Real `anp-mls` execution, hidden service RPCs, and storage/security validation remain a separate MLS/provider slice. | deferred_translation |
| Page content RPC trace boundary | `internal/cli/page.go`, `internal/content/service.go`, `internal/transportcfg/config.go`, `internal/traceutil/trace.go` | Go content page live RPC wraps service calls with transport profiles, context timeouts, and trace phases through `identity.RemoteClient.AuthenticatedRPCCallProfile` and `EnsureJWTPhase`. | Rust now wires non-dry-run `page create/list/get/update/rename/delete` through the translated content client, authsdk session bootstrap/JWT persistence, shared Rustls HTTP execution, profile timeout caps, Go-style CLI error mapping, shared RPC method trace phases, and `content_bootstrap` JWT phase labeling; focused `awiki-system-test tests_v2/page` passes against `awiki.info`. | parity_behavior |
| Page dry-run validation boundary | `internal/cli/page.go`, `internal/content/service.go` | Go dry-run validates `page create` slug/title and markdown source conflicts before the service boundary, and Cobra enforces required flags for `page get/update/rename/delete` by flag presence rather than non-empty value. It does not validate visibility choices, reject empty `page update` plans, or reject explicitly provided empty required flag values such as `--slug ""`; those checks happen later in non-dry-run service methods. | Rust preserves the Go dry-run boundary exactly and records richer validation as a possible later service-slice behavior, not a dry-run optimization. | parity_behavior |
| Msg required-flag error boundary | `internal/cli/msg.go` | Cobra required-flag failures for commands like `msg history --with`, `msg attachment download --message-id --output`, and `msg secure init/repair --with` are surfaced before handler execution as `internal_error` exit code 1, not the handler-level `invalid_argument` exit code 2 used for semantic validation. | Rust preserves this CLI boundary for parity instead of normalizing all user mistakes to one error class. | parity_behavior |
| Tenant site RPC trace boundary | `internal/cli/site.go`, `internal/site/service.go`, `internal/authsdk/*`, `internal/transportcfg/config.go`, `internal/traceutil/trace.go` | Go site live RPC wraps service calls with transport profiles, context timeouts, and trace phases through `identity.RemoteClient.AuthenticatedRPCCallProfile` and `EnsureJWTPhase`. | Rust now wires non-dry-run `site root/page` commands through the translated site client, authsdk session bootstrap/JWT persistence, shared Rustls HTTP execution, profile timeout caps, Go-style CLI error mapping including `forbidden`, shared RPC method trace phases, and `site_bootstrap` JWT phase labeling. | parity_behavior |
| Site required-flag error boundary | `internal/cli/site.go` | Cobra required-flag failures for `site` commands are surfaced before handler execution as `internal_error` exit code 1. Handler-level markdown/body-source failures remain `invalid_argument` exit code 2. | Rust preserves this CLI boundary for parity instead of normalizing all user mistakes to one error class. | parity_behavior |
