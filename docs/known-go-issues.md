# Known Go Issues And Deferred Fixes

This file records Go behavior or design debt found during the Rust parity port.
Parity work should reproduce observable Go behavior unless a deviation is
explicitly approved and documented here.

Translation rule: implement the Rust port one-to-one with the Go implementation
first. Do not mix optimization/refactoring goals into parity translation work.
When an optimization, cleanup, or Rust-native redesign looks useful, record it
below as a deferred optimization and keep the current translation aligned with
Go behavior. Deferred optimizations require a later, separate goal after parity
is proven.

| Area | Go reference | Issue / debt or optimization opportunity | Rust parity decision | Status |
| --- | --- | --- | --- | --- |
| Translation process | all Go files/modules | Potential Rust-native optimizations may be discovered while translating. | Record only; do not implement during parity translation unless needed to reproduce Go behavior or meet hard Rust safety/build constraints. | standing_rule |
| Mail RPC dependency boundary | `internal/mail/client.go`, `internal/mail/service.go`, `internal/authsdk/*` | Non-dry-run mail RPC requires auth session/JWT refresh plus HTTP/TLS dependency selection. Implementing it inside the first mail command slice would mix translation with dependency architecture and SSL policy decisions. | First Rust mail slice preserves CLI validation, dry-run plans, and local `mail notify`; remote RPC is deferred to a dedicated authsdk/mail client slice using a Rustls-reviewed stack. | deferred_translation |
| Message service dependency boundary | `internal/cli/msg.go`, `internal/message/service.go`, `internal/message/secure_commands.go`, `internal/authsdk/*` | Non-dry-run direct/group messaging, attachment transfer, WebSocket proxy transport, and secure direct E2EE require auth session/JWT refresh, message-service RPC/WS clients, cache mutation, and E2EE provider decisions. Implementing them inside the dry-run CLI slice would force network/TLS and crypto choices too early. | Current Rust slice preserves Go command catalog, dry-run plan shapes, Cobra-like required flag failures, local text-file reads, and handler validation only. Real message-service execution is deferred to shared authsdk/session plus Rustls HTTP/WS and E2EE slices. | deferred_translation |
| Message signed service execution boundary | `internal/message/auth.go`, `internal/message/service.go`, `internal/authsdk/*` | Go direct send and attachment send combine RFC9421 origin proof with auth session/JWT refresh and service transport. Pulling service execution into the local proof slice would mix private-key signing with HTTP/WebSocket/authsdk dependency decisions. | Rust now translates local RFC9421 origin-proof generation with the local ANP Rust SDK. Auth session refresh, bearer behavior, transport execution, and cache mutation remain deferred to shared authsdk/message-service slices. | deferred_translation |
| Config writer durability | `internal/config/write.go` | Go writes config through a temp file, fsync, chmod `0600`, rename, and directory sync. Windows intentionally skips parent-directory sync because Windows directory handles commonly reject it. | Rust now mirrors this durable-write boundary with std-only same-directory temp files, file sync, Unix file/dir modes, atomic rename, temp cleanup, Unix parent-directory sync, and Windows parent-directory sync no-op. Windows replacement semantics still need Windows CI coverage before claiming cross-platform runtime parity. | parity_behavior |
| Config YAML parser/serializer depth | `internal/config/config.go`, `internal/config/write.go` | Go uses `yaml.v3`, while the current Rust config path uses a hand-written scalar parser/renderer for translated fields. Complex YAML scalars, comment preservation, anchors, and exact marshal formatting are not fully equivalent. | Keep the hand-written path for bounded helper parity; full YAML parser/serializer replacement is a separate dependency/format decision so it is not mixed into helper translation. | deferred_translation |
| Hermes CLI/bridge boundary | `internal/cli/runtime.go`, `internal/runtime/hermesbridge/*`, `internal/config/write.go` | Go Hermes host-notify commands do more than mutate config: they configure Hermes route/secret, refresh/restart listener state, and manage bridge behavior. | Rust now translates Hermes config writer helpers and legacy webhook double-write only. Hermes command wiring, bridge behavior, and listener refresh orchestration remain separate runtime parity slices. | deferred_translation |
| Doctor config/upgrade depth boundary | `internal/doctor/doctor.go`, `internal/config/config.go`, `internal/upgrade/*` | Go doctor inherits full `yaml.v3` parse errors and `upgrade.Inspect` meta/journal/detection details. Current Rust config parsing and upgrade detection are still narrower than Go. | The Rust doctor slice preserves the public 10-check report contract and local diagnostics that existing translated modules can support. Full YAML parser behavior and deep workspace-upgrade metadata inspection remain dedicated config/upgrade parity slices. | deferred_translation |
| Update network fetch boundary | `internal/update/update.go` | Go `CheckFresh` fetches npm metadata from npmjs and npmmirror over HTTPS with a 3-second timeout and writes cache metadata with restricted permissions. | Rust now translates this narrow update fetch/writeback boundary using Rustls-first direct HTTPS, local mock registry tests, and cache writeback checks. Broader service HTTP/WebSocket clients still need a separate authsdk/service dependency decision. | parity_behavior |
| Root update preflight guard | `internal/cli/root.go` | Go runs a global update-policy guard before most commands, can block unsupported non-dev builds, and injects newer-version warnings. | Current Rust slice implements explicit `upgrade` only. The preflight guard remains a later CLI-root parity task after update metadata fetch behavior is complete. | deferred_translation |
| Replace-DID side effects | `internal/cli/id.go`, `internal/identity/service.go`, `internal/store/rebind.go` | Non-dry-run `id replace-did` generates new e1 DID/key material, calls `did-auth.replace_did`, backs up old sensitive material, rebinds SQLite owner state, and cleans E2EE state. | Current Rust slice exposes only the public schema and dry-run danger plan. Non-dry-run replacement remains a dedicated authsdk/store-rebind slice. | deferred_translation |
| Group service RPC boundary | `internal/cli/group.go`, `internal/message/group_service.go` | Non-dry-run group commands require registered identity auth, message service RPC, group membership state, and later group E2EE surfaces. | Current Rust slices translate the non-E2EE `group.go` dry-run request-shape contracts only. Remote group lifecycle is deferred to the shared authsdk/message service slice. | deferred_translation |
| Group wire execution boundary | `internal/message/group_wire.go`, `internal/message/group_service.go`, `internal/authsdk/*` | Go group wire builders are pure request constructors, but non-dry-run execution also needs auth session/JWT refresh, transport, status/error mapping, cache mutation, and service lifecycle orchestration. | Rust now translates the base/local and E2EE `group_wire.go` request builders plus origin-proof auth values. Service execution remains deferred to the shared authsdk/message-service slice. | deferred_translation |
| Group E2EE execution boundary | `internal/cli/group_e2ee.go`, `internal/message/group_e2ee_service.go` | Non-dry-run group E2EE commands require MLS provider state, hidden P6 service APIs, focused system-test gating, and stricter storage/security checks. | Current Rust slices translate static `group e2ee ... --dry-run` plans/schema and hidden E2EE request builders only. Real `anp-mls` execution, hidden service RPCs, and storage/security validation remain a separate MLS/provider slice. | deferred_translation |
| Page content RPC dependency boundary | `internal/cli/page.go`, `internal/content/service.go`, `internal/authsdk/*` | Non-dry-run page CRUD requires active identity auth, DID-auth JWT refresh, and `/content/rpc` calls over HTTP. Implementing it inside the dry-run page CLI slice would force the shared HTTP/TLS dependency decision early. | Current Rust slice translates the full `page` command dry-run/local-validation surface only. Real content RPC is deferred to the shared authsdk/session plus Rustls HTTP client slice. | deferred_translation |
| Page dry-run validation boundary | `internal/cli/page.go`, `internal/content/service.go` | Go dry-run validates `page create` slug/title and markdown source conflicts before the service boundary, but does not validate visibility choices or reject empty `page update` plans; those checks happen later in non-dry-run service methods. | Rust preserves the Go dry-run boundary exactly and records richer validation as a possible later service-slice behavior, not a dry-run optimization. | parity_behavior |
| Msg required-flag error boundary | `internal/cli/msg.go` | Cobra required-flag failures for commands like `msg history --with`, `msg attachment download --message-id --output`, and `msg secure init/repair --with` are surfaced before handler execution as `internal_error` exit code 1, not the handler-level `invalid_argument` exit code 2 used for semantic validation. | Rust preserves this CLI boundary for parity instead of normalizing all user mistakes to one error class. | parity_behavior |
| Tenant site RPC dependency boundary | `internal/cli/site.go`, `internal/site/service.go`, `internal/authsdk/*` | Non-dry-run tenant site root/page commands require active identity auth, DID-auth JWT refresh, `/site/rpc` HTTP calls, and service-side domain authorization. Implementing that inside the dry-run CLI slice would force the shared HTTP/TLS dependency decision early. | Current Rust slice translates `site` command dry-run/local-validation surface only. Real tenant site RPC is deferred to the shared authsdk/session plus Rustls HTTP client slice. | deferred_translation |
| Site required-flag error boundary | `internal/cli/site.go` | Cobra required-flag failures for `site` commands are surfaced before handler execution as `internal_error` exit code 1. Handler-level markdown/body-source failures remain `invalid_argument` exit code 2. | Rust preserves this CLI boundary for parity instead of normalizing all user mistakes to one error class. | parity_behavior |
