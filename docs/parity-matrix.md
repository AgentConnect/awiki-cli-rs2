# Parity Matrix

Status values: `not_started`, `in_progress`, `ported`, `unit_verified`, `system_verified`, `deferred_with_reason`.

Dependency constraint: Rust modules should prefer pure Rust, cross-platform
dependencies. SQLite, SSL/TLS, crypto, and platform integration choices must
avoid host system libraries by default. SQLite currently has a user-approved
exception: `rusqlite + bundled` compiles SQLite into the CLI binary for runtime
portability. Any exception must be recorded here and in
`docs/dependency-decisions.md` with the reason and verification evidence.
TLS should use Rustls-first stacks. OpenSSL is not preferred even when bundled;
bundled OpenSSL is still a native dependency exception and needs explicit
parity evidence before adoption.
The recommended TLS default is `rustls`; OpenSSL/`native-tls` and OpenSSL
`vendored`/bundled feature paths must not be introduced before Rustls-backed
options have failed a documented parity requirement.
Future HTTPS/WebSocket lanes should begin with Rustls-backed clients and verify
the dependency tree stays free of OpenSSL/`native-tls`; bundled OpenSSL is not a
preferred fallback.

Parallel execution constraint: Native Agents may be used for independent
module/file slices when that improves throughput. Any code-writing Native Agent
must use GPT-5.5 with xhigh reasoning and a bounded, non-overlapping write
scope; read-only Native Agents may use lighter settings.

Current command catalog metadata slice: Rust static schema metadata now matches
the selected Go catalog fields for `upgrade` text/`side_effect`, identity
short/flag usage text, command flag `choices` for `msg`, `group`, `runtime`,
host-notify, and page visibility surfaces, runtime/listener/host-notify
grouping-node `implemented=false` metadata, OpenClaw token required metadata,
and debug DB phase/text/use/path usage. This is schema metadata parity only;
runtime behavior and business implementation remain separate rows/slices.

Current system-test selector evidence: `awiki-system-test` can build the Rust
binary from this sibling repository with `AWIKI_CLI_UNDER_TEST=rust` and
`AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2`. On
2026-05-17, the Rust binary passed `tests_v2/core/test_basic_commands.py`,
`tests_v2/core/test_output_contracts_cli.py`, `tests_v2/debug/test_debug_cli.py`,
and `tests_v2/update/test_update_policy.py` with no failures or skips under
cache-only update checks. Full `awiki-system-test` acceptance remains pending
for the overall port.

Current group E2EE selector evidence: on 2026-05-17, the Rust binary passed the
focused local group E2EE selectors in `awiki-system-test` with
`AWIKI_GROUP_E2EE_CONTRACT_TEST=1`: `test_awiki_cli_group_e2ee_local.py`,
`test_awiki_cli_group_e2ee_lifecycle_local.py`,
`test_awiki_cli_group_e2ee_recovery_local.py`,
`test_awiki_cli_group_e2ee_update_rejoin_local.py`, and
`test_awiki_cli_group_e2ee_negative_local.py`. This evidence covers the
transition-candidate CLI boundary where unsupported `group e2ee leave-requests`
and `group e2ee leave-request list` must fail like Go/Cobra so callers can fall
through to `group e2ee pending`, plus the non-owner `group add` error envelope
that must preserve Go's owner-role hint. Full `awiki-system-test` acceptance
remains pending for the overall port.

Current group E2EE send/decrypt system evidence: on 2026-05-17, the Rust binary
passed `tests_v2/cli/test_awiki_cli_group_e2ee_local.py` with
`AWIKI_GROUP_E2EE_CONTRACT_TEST=1`: 1 passed, 0 failed, and 0 skipped. This
covers the focused Alice/Bob real-MLS loop for publish-key-package, E2EE group
create/add, welcome repair, encrypted `msg send --group`, decrypted
`group messages`, one-shot `anp-mls` process exit, SQLite restore boundaries,
and absence of business-DB private MLS state. It does not cover every group
E2EE lifecycle/recovery/update edge, WebSocket/local bridge group E2EE
transport, foreground listener group E2EE handling, mail, or full repository-wide
system acceptance.

Current group E2EE remove/leave system evidence: on 2026-05-17, the Rust binary
passed `tests_v2/cli/test_awiki_cli_group_e2ee_lifecycle_local.py` with
`AWIKI_GROUP_E2EE_CONTRACT_TEST=1`: 3 passed, 0 failed, and 0 skipped. This
covers owner `group remove --e2ee` revocation, same-epoch local-terminal
self-leave fail-closed behavior, PR-B1 `group leave --e2ee` leave-request
creation, owner `group e2ee process-leave-request`, remaining-member repair,
removed-member send/read denial, commit opacity, and no plaintext/private MLS
material in tested storage boundaries. It does not cover recover-member,
update-key, rejoin, WebSocket/local bridge transport, foreground listener group
E2EE handling, mail, or full repository-wide system acceptance.

Current broad CLI selector evidence: on 2026-05-17, the Rust binary passed a
broad non-mail, non-message-service `awiki-system-test` subprocess batch under
`AWIKI_CLI_UNDER_TEST=rust`, covering `tests_v2/core`, `debug`, `update`,
`runtime`, `id`, `page`, `multi_tenant`, direct/group CLI, runtime listener,
service-run, and OpenClaw host-notify selectors: 70 passed, 3 skipped, 0
failed. The three skips were message-service tenant-admission environment gates
requiring `E2E_MESSAGE_V2_DID_ONLY_DOMAIN` or
`E2E_MESSAGE_V2_MESSAGE_ONLY_DID`. Mail, message-service-only protocol tests,
and full repository-wide `tests_v2` acceptance remain pending.

Current page CLI system evidence: on 2026-05-17, the Rust binary passed
`tests_v2/page/test_page_cli.py` with 3 passed, 0 failed, and 0 skipped under
`AWIKI_CLI_UNDER_TEST=rust` against the configured `awiki.info` services. This
covers real subprocess `page create/list/get/update/rename/delete`, structured
local validation failures, `unlisted` visibility, dry-run page create planning,
duplicate conflict, and missing-page not-found mapping. It does not cover tenant
site page commands, mail, trace phase assertions, or full repository-wide
system acceptance.

Current runtime listener host-notify delivery evidence: on 2026-05-17, the Rust
binary passed `tests_v2/cli/test_awiki_cli_runtime_listener_local.py` plus
`tests_v2/cli/test_awiki_cli_host_notify_openclaw_local.py` with 3 passed, 0
failed, and 0 skipped. This covers foreground listener direct-notification
SQLite/inbox persistence and OpenClaw host-notify webhook delivery through a
loopback route. It does not cover mail-system acceptance, group/group-state live
notification delivery, Hermes host-notify POST delivery, noop/log/file
foreground sink delivery, Windows named-pipe listener I/O, or platform
service-manager execution.

Current runtime CLI system evidence: on 2026-05-17, the Rust binary passed the
full `tests_v2/runtime/test_runtime_cli.py` file with
`AWIKI_ENABLE_LISTENER_SERVICE_TESTS=1`: 14 passed, 0 failed, and 0 skipped.
This supports scoped system evidence for subprocess CLI behavior: runtime
mode/setup/apply/status, listener config and local lifecycle-state output,
host-notify config and enable/disable, OpenClaw local config/route/webhook
behavior, and the already split Hermes local guide/status/config/setup rows.
The service-management gate and working `systemctl --user` environment ensured
the gated selectors did not skip, but the current Rust listener lifecycle code
still models local listener state and does not prove real platform
service-manager execution. It does not cover mail-system acceptance, Windows
service-manager parity, macOS launchd parity, full cross-platform
service-manager parity, real foreground listener restart after host-notify
changes, real Hermes POST delivery, or real mail notification delivery.

Current runtime host-notify enablement selector evidence: on 2026-05-17, a new
Rust subprocess selector
`tests_v2/runtime/test_runtime_cli.py::test_runtime_host_notify_enable_disable_round_trip`
passed, and the full `tests_v2/runtime/test_runtime_cli.py` file passed with
10 passed, 0 failed, and 0 skipped. This covers `runtime host-notify enable`
and `runtime host-notify disable` persistence without requiring mail-service,
message-service local topology, Hermes, or real user service-manager
permissions.

Current debug handle-history selector evidence: on 2026-05-17, a new Rust
subprocess selector
`tests_v2/debug/test_debug_cli.py::test_debug_db_handle_history_reads_contact_bindings`
passed, and the full `tests_v2/debug/test_debug_cli.py` file passed with 5
passed, 0 failed, and 0 skipped. This covers `debug db handle-history` local
SQLite `contact_handle_bindings` reads, handle normalization, owner
aggregation, and no-row `not_found` mapping without requiring mail-service,
message-service local topology, or registered identities.

Current deterministic debug import selector evidence: on 2026-05-17, a new
Rust subprocess selector
`tests_v2/debug/test_debug_cli.py::test_debug_db_import_v1_imports_seeded_legacy_sqlite_without_message_service`
passed, and the full `tests_v2/debug/test_debug_cli.py` file passed with 6
passed, 0 failed, and 0 skipped. This covers `debug db import-v1` against a
seeded local v6 legacy SQLite `database/awiki.db`, identity-owner inference
from a locally imported legacy flat identity, missing-table skip reporting,
message row import, default direct thread/content-type normalization, empty
`e2ee_outbox` reporting, and contact handle-binding side effects without
requiring mail-service, message-service local topology, registered identities,
or real user service-manager permissions.

Current runtime Hermes selector evidence: on 2026-05-17, two new Rust
subprocess selectors
`tests_v2/runtime/test_runtime_cli.py::test_runtime_host_notify_hermes_guide_status_and_config_commands_work`
and
`tests_v2/runtime/test_runtime_cli.py::test_runtime_host_notify_hermes_setup_writes_local_files`
passed, and the full `tests_v2/runtime/test_runtime_cli.py` file passed with
12 passed, 0 failed, and 0 skipped. This covers local Hermes
`guide`/`status` views, Hermes set/set-secret/clear-secret redaction and config
show integration, plus dry-run and local-file `setup` behavior using an
isolated `HERMES_HOME`. It does not require mail-service, message-service local
topology, real Hermes, or real user service-manager permissions.

Current trace timing selector evidence: on 2026-05-17, a new Rust subprocess
selector
`tests_v2/core/test_output_contracts_cli.py::test_trace_timing_preserves_cli_output_channels`
passed, and the full `tests_v2/core/test_output_contracts_cli.py` file passed
with 3 passed, 0 failed, and 0 skipped. This covers
`AWIKI_CLI_TRACE_TIMING=1` stdout/stderr separation for `status` and raw
`completion bash` output without requiring mail-service, message-service local
topology, or registered identities.

Current config set selector evidence: on 2026-05-17, a new Rust subprocess
selector
`tests_v2/core/test_basic_commands.py::test_config_set_did_domain_persists_normalized_domain_and_rejects_invalid_inputs`
passed, and the full `tests_v2/core/test_basic_commands.py` file passed with
11 passed, 0 failed, and 0 skipped. This covers `config set --did-domain`
dry-run, normalized persistent write, `config show` readback, no runtime socket
side effects, and invalid input errors without requiring mail-service,
message-service local topology, real user service-manager permissions, or
registered identities.

Current workspace-upgrade read-only selector evidence: on 2026-05-17, a new
Rust subprocess selector
`tests_v2/core/test_basic_commands.py::test_doctor_and_config_show_report_workspace_upgrade_meta_and_journal`
passed, and the full `tests_v2/core/test_basic_commands.py` file passed with
12 passed, 0 failed, and 0 skipped. This covers `doctor` and `config show`
reporting of isolated `upgrade/meta.json` and `upgrade/upgrade_journal.json`,
including warning precedence and config snapshot embedding, without running
real migration execution or requiring mail-service, message-service local
topology, service-manager permissions, or registered identities.

Current update root-preflight selector evidence: on 2026-05-17, a new Rust
subprocess selector
`tests_v2/update/test_update_policy.py::test_root_preflight_soft_fails_non_exempt_update_checks_and_exempts_local_commands`
passed, and the full `tests_v2/update/test_update_policy.py` file passed with
4 passed, 0 failed, and 0 skipped. This covers cache-only update-check
soft-fail behavior for non-exempt `status`, verbose stderr logging, quiet
non-verbose behavior, and local/recovery command exemptions for
`version`/`upgrade`/`init --dry-run`/`docs`/`schema`/`config show`/`doctor`/
`completion bash` without requiring npm registry access, mail-service,
message-service local topology, service-manager permissions, or registered
identities.

Current full-suite attempt evidence: on 2026-05-17, `AWIKI_CLI_UNDER_TEST=rust`
with `AWIKI_GROUP_E2EE_CONTRACT_TEST=1` ran `uv run pytest tests_v2 -ra -q`.
The result was 97 passed, 1 failed, and 9 skipped in 127.85s. The only failure
was `tests_v2/mail/test_awiki_cli_mail_notification_local.py::test_awiki_cli_mail_notification_flow_local`
because `awiki-mail-service` was not reachable at `http://127.0.0.1:9899`;
four additional mail cases skipped for the same unavailable service. This is
tracked as an environment blocker rather than Rust CLI acceptance. Full
`tests_v2` acceptance remains pending until mail-service-backed selectors are
run against a reachable mail service.

| Go path | Go symbol / feature | Rust path | Status | Tests | Notes |
| --- | --- | --- | --- | --- | --- |
| `awiki-cli/cmd/awiki-cli/main.go` | process entrypoint | `crates/awiki-cli/src/main.rs`, `crates/awiki-cli/src/lib.rs` | `system_verified` | `cargo build -p awiki-cli --bin awiki-cli --offline`; `tests_v2/core/test_basic_commands.py` | Initial Rust entrypoint delegates to library execute and is exercised as a subprocess by core system tests. |
| `awiki-cli/README.md` | public repository README | `README.md` | `unit_verified` | `cmp -s ../awiki-cli/README.md README.md`; `stat -c '%a %n' README.md`; `wc -l README.md` | Copied byte-for-byte from the Go repository so the Rust port carries the same public project overview, installation/onboarding instructions, quick links, layout notes, config-template pointer, and support guidance. This is a documentation asset parity slice only; linked onboarding/architecture docs and release packaging assets remain separate parity lanes. |
| `awiki-cli/onboarding.md` | first-install onboarding guide | `onboarding.md` | `unit_verified` | `cmp -s ../awiki-cli/onboarding.md onboarding.md`; `stat -c '%a %n' onboarding.md`; `wc -l onboarding.md` | Copied byte-for-byte from the Go repository so the Rust port carries the same first-install instructions for npm installation, Skill installation, workspace init, identity registration/recovery, runtime setup, and status checks. Go README currently references `onboarding.en.md`, which is also absent in the Go repository; this slice preserves the existing Go asset rather than editing links. |
| `awiki-cli/docs/installation.md` | installation and local development guide | `docs/installation.md` | `unit_verified` | `cmp -s ../awiki-cli/docs/installation.md docs/installation.md`; `stat -c '%a %n' docs/installation.md`; `wc -l docs/installation.md` | Copied byte-for-byte from the Go repository so the Rust port carries the same install/build/database/runtime/troubleshooting documentation asset. The source guide is Go-specific, including Go 1.22, pure-Go SQLite, and Go release commands; this slice preserves it as source documentation parity and does not rewrite it for Rust dependencies. |
| `awiki-cli/docs/publish.md` | release and rollback handbook | `docs/publish.md` | `unit_verified` | `cmp -s ../awiki-cli/docs/publish.md docs/publish.md`; `stat -c '%a %n' docs/publish.md`; `wc -l docs/publish.md` | Copied byte-for-byte from the Go repository so the Rust port carries the same release/tag/npm/Gitee/rollback documentation. This is documentation parity only; `.goreleaser.yml`, GitHub workflows, and `scripts/release/*` are not copied or executed in this slice. |
| `awiki-cli/docs/architecture/awiki-command-v2.md` | command-surface architecture document | `docs/architecture/awiki-command-v2.md` | `unit_verified` | `cmp -s ../awiki-cli/docs/architecture/awiki-command-v2.md docs/architecture/awiki-command-v2.md`; `stat -c '%a %n' docs/architecture/awiki-command-v2.md`; `wc -l docs/architecture/awiki-command-v2.md`; `xtask check-structure` | Copied byte-for-byte from the Go repository to satisfy the README-linked command architecture path. The source document explicitly frames the implementation as Go; this Rust port preserves it as source architecture/documentation parity and does not rewrite wording in the translation lane. The file is 1017 lines, below the default 1200-line review-size cap. |
| `awiki-cli/docs/architecture/awiki-v2-architecture.md` | v2 system architecture document | `docs/architecture/awiki-v2-architecture.md` | `unit_verified` | `cmp -s ../awiki-cli/docs/architecture/awiki-v2-architecture.md docs/architecture/awiki-v2-architecture.md`; `stat -c '%a %n' docs/architecture/awiki-v2-architecture.md`; `wc -l docs/architecture/awiki-v2-architecture.md`; `xtask check-structure` | Copied byte-for-byte from the Go repository to satisfy the README-linked v2 architecture path. The source document explicitly records the Go single-binary architecture decision; this Rust port preserves it as source architecture/documentation parity and does not rewrite wording in the translation lane. The file is 1111 lines, below the default 1200-line review-size cap. |
| Go docs referenced by `internal/docs/topics.go` | docs topic reference assets for overview, phase-0, architecture/mail/site/skills, output, review, and selected skill references | `docs/architecture/{awiki-mail-cli,awiki-site-pages,awiki-skill-architecture,output-format}.md`, `docs/plan/awiki-v2-implementation-plan.md`, `docs/plan/phase-0/{implementation-constraints,capability-mapping,audit-findings,adr-index}.md`, `docs/harness/review-spec.md`, `skills/SKILL.md`, `skills/references/{02-identity,03-messaging,11-site-pages}.md` | `unit_verified` | `cmp -s` for every copied asset against `../awiki-cli`; `cmp -s ../awiki-cli/docs/architecture/参考文档/output-format.md docs/architecture/output-format.md`; `stat -c '%a %n'`; `wc -l`; `cargo +1.79.0 test -p awiki-cli --test core_contract docs_list_and_topic_lookup_preserve_go_topic_contracts --locked -- --exact`; `cargo +1.79.0 run --bin xtask --locked -- check-structure` | Copied byte-for-byte from the Go repository so Rust `docs` topic references no longer point at missing same-repo documentation assets for mail, site, skills, output, review, overview, phase-0, identity, and messaging. The output topic preserves Go `internal/docs/topics.go`'s canonical `docs/architecture/output-format.md` reference by copying the Go source asset from `docs/architecture/参考文档/output-format.md` to that referenced path in the Rust repository. The copied files preserve Go/source wording, including Go-specific implementation notes, and this slice intentionally does not rewrite documentation for Rust. Every copied file is below the default 1200-line review-size cap. |
| `awiki-cli/internal/cli/app.go` | application execution wrapper | `crates/awiki-cli/src/app.rs` | `system_verified` | `cargo test -p awiki-cli --offline`; `tests_v2/core/test_basic_commands.py`; `tests_v2/core/test_output_contracts_cli.py` | First core slice implements status/version/config/docs/schema/completion/init/dry-run-visible planning paths. Full command surface remains incremental. |
| `awiki-cli/internal/cli/root.go` | root command flags and dispatch | `crates/awiki-cli/src/cli/mod.rs`, `crates/awiki-cli/src/app.rs` | `system_verified` | `cargo test -p awiki-cli --offline`; `tests_v2/core/test_basic_commands.py`; `tests_v2/core/test_output_contracts_cli.py` | Hand-written parser covers first system-test slice; full Cobra parity is still pending for later command lanes. |
| `awiki-cli/internal/cli/error_hints.go` | Windows directory-sync compatibility hint refinement | `crates/awiki-cli/src/app/error_hints.rs`, `crates/awiki-cli/src/app.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli error_hints --locked`; `cargo +1.79.0 test -p awiki-cli internal_anyhow --locked`; `go test ./internal/cli -run 'TestIsWindowsDirSyncCompatibilityError|TestRuntimeExitRefinesWindowsDirSyncCompatibilityHint|TestConfigCommandExitRefinesWindowsDirSyncCompatibilityHint' -count=1` | Translates Go's narrow refinement for Windows `Access is denied` failures caused by directory sync markers: `sync config dir`, `sync route registry dir`, and `sync dir`. Rust applies the refined hint to generic workspace/config errors without changing normal permission-error hints. No dependency was added. |
| `awiki-cli/internal/output/output.go` | output envelopes / renderers | `crates/awiki-cli/src/output.rs` | `system_verified` | `crates/awiki-cli/tests/core_contract.rs`; `tests_v2/core/test_output_contracts_cli.py` | JSON, pretty JSON, NDJSON arrays, table rendering, first jq subset, and invalid jq error are verified for core coverage. |
| `awiki-cli/internal/cmdmeta/catalog.go` | command catalog | `crates/awiki-cli/src/cmdmeta/mod.rs` | `system_verified` | `crates/awiki-cli/tests/core_contract.rs`; focused `schema_exposes_go_stub_command_families_and_stub_errors`; focused `schema_metadata_matches_go_catalog_for_choices_and_grouping_nodes`; `crates/awiki-cli/tests/update_contract.rs`; `tests_v2/core/test_basic_commands.py`; `tests_v2/core/test_output_contracts_cli.py`; Go `go test ./internal/cmdmeta ./internal/cli -run 'TestCatalog\|TestCommandFromSpec\|TestBuildRoot' -count=1` | Command specs are added module-by-module as implementation slices land. The planned Go stub command families `group.code`, `runtime.heartbeat`, `people`, `people.contacts`, `debug.raw`, `debug.schema-cache`, and `debug.logs` are now present in Rust static schema metadata where Go exposes them, parsed to their canonical command names, and dispatched to Go `runStub`-style `internal_error` envelopes with `planned for PHASE` hints. This is schema/parser/stub-boundary parity only; it does not implement group join-code, heartbeat, people/contact, raw RPC, schema-cache, or log-follow business behavior. The runtime host-notify/Hermes command tree includes the Go hidden `runtime.host-notify.hermes.bridge` grouping node and implemented hidden `runtime.host-notify.hermes.bridge.service-run` leaf in static schema metadata, matching the existing parser/dispatch path. Visible grouping nodes such as `runtime.mode`, `runtime.listener`, `runtime.listener.config`, `runtime.host-notify`, `runtime.host-notify.config`, `runtime.host-notify.openclaw`, `runtime.host-notify.hermes`, and `runtime.host-notify.openclaw.route` keep Go's grouping-style `implemented=false` metadata while their executable children carry handlers. Static metadata drift fixed in this slice includes `upgrade` short text and `side_effect=false`, Go identity/register/bind/profile text and flag usage, `choices` arrays for `msg.send --secure`, `msg.inbox --scope`, `group.create --message-security-profile`, `group.e2ee.publish-key-package --purpose`, `runtime.setup --mode`, `runtime.host-notify.config.set --sink`, and `page.create/page.update --visibility`, runtime apply/mode/listener/host-notify command text, OpenClaw `set-token --value` required metadata, and debug/debug.db phase/text/use/path metadata. |
| `awiki-cli/internal/buildinfo/buildinfo.go` | build metadata snapshot and version/status JSON fields | `crates/awiki-cli/src/buildinfo.rs`, `crates/awiki-cli/tests/core_contract.rs` | `system_verified` | `cargo +1.79.0 test -p awiki-cli buildinfo --locked`; `AWIKI_CLI_VERSION=9.9.9 AWIKI_CLI_COMMIT=abc AWIKI_CLI_BUILD_DATE=2026-05-17T00:00:00Z AWIKI_CLI_CGO_ENABLED=0 cargo +1.79.0 test -p awiki-cli buildinfo --locked`; `cargo +1.79.0 test -p awiki-cli --test core_contract version_reports_current_build_info --locked`; `cargo +1.79.0 test -p awiki-cli --test core_contract status_reports_phase_version_paths_state_and_config --locked`; `cargo +1.79.0 test -p awiki-cli --test doctor_contract --locked`; `go test ./internal/buildinfo`; `tests_v2/core/test_basic_commands.py::test_core_query_commands_return_structured_success`; `tests_v2/core/test_output_contracts_cli.py::test_output_formats_and_jq_filters_follow_cli_contracts` | Preserves Go JSON field names and default metadata values: `version=dev`, `commit=unknown`, `build_date=unknown`, and `cgo_enabled=unknown`. Rust keeps the observable `go_version`, `goos`, `goarch`, and `compiler` keys while reporting Rust runtime/compiler values and mapping Rust target names to Go-style `darwin`/`amd64` names. Public usage through `status`, `version`, and `doctor` is covered by focused Rust and system tests. Go release `ldflags` metadata injection is represented in Rust by compile-time `AWIKI_CLI_VERSION`, `AWIKI_CLI_COMMIT`, `AWIKI_CLI_BUILD_DATE`, and `AWIKI_CLI_CGO_ENABLED`; ordinary builds still keep Go defaults. No dependency or build script was added. |
| `awiki-cli/internal/traceutil/trace.go` | trace timing run, phase formatting, fallback formatting, command emission, shared RPC/JWT phase wiring | `crates/awiki-cli/src/traceutil.rs`, `crates/awiki-cli/src/app.rs`, `crates/awiki-cli/src/authsdk/mod.rs`, service clients | `system_verified` | `crates/awiki-cli/tests/traceutil_contract.rs`; `crates/awiki-cli/tests/core_contract.rs` trace timing tests; `crates/awiki-cli/tests/mail_live_contract.rs` trace timing tests; `go test ./internal/traceutil`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/core/test_output_contracts_cli.py::test_trace_timing_preserves_cli_output_channels -q`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/core/test_output_contracts_cli.py -ra -q` | Translates Go's `AWIKI_CLI_TRACE_TIMING` truthy values, trimmed command capture, start-order phase records, local/RPC/JWT/handle phase names, fallback labels, Chinese pretty output, known humanized labels, and millisecond/second formatting. Rust uses a thread-local current run instead of Go `context.Context`, creates a run after command parse, records `resolve_config`, emits timing to stderr after rendered success/error envelopes, preserves raw completion output without trace stderr, records shared service RPC phases, and records caller-labeled JWT refresh phases such as `mail_bootstrap`, `content_bootstrap`, `site_bootstrap`, `identity_refresh_token`, `identity_bootstrap`, `message_bootstrap`, and `message_service_retry` without adding nested `business_rpc:get_me`. The `awiki-system-test` selector proves the subprocess output-channel contract: `AWIKI_CLI_TRACE_TIMING=1 status` keeps the JSON success envelope on stdout while writing the Chinese trace to stderr, and `AWIKI_CLI_TRACE_TIMING=1 completion bash` keeps raw shell completion stdout with empty stderr. Direct-message trace-depth call sites now cover Go `handle_lookup:target_resolve`, `handle_lookup:contact_sync_by_did`, `local_db:persist_direct_send`, `local_db:persist_inbox_messages`, `local_db:persist_history_messages`, nested `contact_sync`, `local_db:read_inbox_cache`, `local_db:read_unified_direct_inbox_cache`, `local_db:read_mail_notification_cache`, `local_db:read_history_cache`, `local_db:read_inbox_cache_by_peer_dids`, and `local_db:read_history_cache_by_peer_dids`. Group trace-depth call sites now cover Go `local_db:persist_group_send`, `local_db:persist_group_snapshot`, `local_db:persist_group_members`, `local_db:persist_group_messages`, `local_db:touch_group_cache`, `local_db:mark_group_left`, `local_db:read_group_snapshot_cache`, `local_db:read_group_members_cache`, `local_db:read_group_messages_cache`, `local_db:read_group_inbox_cache`, and `local_db:read_all_group_inbox_cache`. Attachment trace-depth now covers direct/group attachment local persistence through `local_db:persist_direct_send`, `local_db:persist_group_send`, nested `local_db:touch_group_cache`, and direct attachment send/download through the shared `handle_lookup:target_resolve` path. Message service-level JWT fallback refresh now preserves Go's `message_fallback_refresh` label for direct send, inbox, history, and mark-read wrappers while keeping transport-level RPC `1401` retry on `message_service_retry`; remaining non-direct-message fallback call sites stay as separate parity work. No dependency was added. |
| `awiki-cli/internal/transportcfg/config.go` | environment-resolved bridge/HTTP/profile timeouts and `NewHTTPClient` boundary | `crates/awiki-cli/src/transportcfg.rs`, `crates/awiki-cli/src/transportcfg/http.rs` | `system_verified` | `crates/awiki-cli/tests/transportcfg_contract.rs`; `crates/awiki-cli/tests/transportcfg_http_contract.rs`; `go test ./internal/transportcfg`; `tests_v2/page` | Translates Go's profile names, default bridge/HTTP/profile durations, environment variable names, positive duration/int fallback behavior, profile default fallback, and a Rustls/std shared HTTP/1.1 client for the `NewHTTPClient` boundary. The client snapshots `Resolve()`, applies dial/TLS-handshake/response-header timeouts, applies per-request profile timeout caps for service calls without extending the base response timeout, appends optional PEM CA bundles to `webpki-roots`, preserves `read ca bundle:` / `invalid ca bundle:` errors, sends default `User-Agent`, decodes fixed/chunked bodies, tolerates Rustls missing `close_notify` only after an HTTP-framed response is complete to match Go's EOF tolerance, and stays proxy-free like Go's custom `transportcfg.NewHTTPClient` transport. HTTP/2, system-root parity, keepalive/pooling reuse, streaming bodies, and full Go `context` deadline coverage for dial/TLS/write phases remain deferred. No dependency was added. |
| `awiki-cli/internal/config/config.go` | config resolution defaults and runtime/host-notify fields | `crates/awiki-cli/src/config/mod.rs` | `system_verified` | `crates/awiki-cli/tests/core_contract.rs`; `crates/awiki-cli/tests/runtime_contract.rs`; `tests_v2/core/test_basic_commands.py`; `tests_v2/core/test_output_contracts_cli.py`; `tests_v2/runtime/test_runtime_cli.py` | Partial parser for translated fields. Runtime listener and host-notify OpenClaw/Hermes/Webhook config keys are now represented, including OpenClaw token persistence with redacted output and Hermes/Webhook helper persistence. DID domain normalization for `config set` follows Go's permissive bare-host behavior. Full Go YAML/config/env parity remains incremental. No native dependency added. |
| `awiki-cli/internal/cli/config.go`, `awiki-cli/internal/config/write.go` | `config set --did-domain` dry-run and persistent write | `crates/awiki-cli/src/app.rs`, `crates/awiki-cli/src/config/{mod,write}.rs`, `crates/awiki-cli/src/cli/mod.rs`, `crates/awiki-cli/src/cmdmeta/mod.rs` | `system_verified` | `crates/awiki-cli/tests/core_contract.rs::config_set_did_domain_matches_go_contract`; `crates/awiki-cli/tests/core_contract.rs::config_set_validates_did_domain_like_go`; `crates/awiki-cli/tests/config_writer_contract.rs`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/core/test_basic_commands.py::test_config_set_did_domain_persists_normalized_domain_and_rejects_invalid_inputs -q`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/core/test_basic_commands.py -ra -q` | Implements Go command catalog entry, required `--did-domain`, dry-run `config_set_did_domain` plan, normalized `services.did_domain` write, `config show` readback, no runtime socket/pid side effects, and Go-style durable config replacement through the shared writer. The `awiki-system-test` selector verifies dry-run no-write behavior, normalized live write from mixed-case/trailing-dot input, persisted config text, readback through `config show`, and missing/URL-like/path-like invalid-argument errors through the real Rust subprocess. Uses std-only config writer code and adds no dependency. Full `yaml.v3` parser/serializer parity is not claimed by this narrow slice. |
| `awiki-cli/internal/durablefs/syncdir_unix.go`, `awiki-cli/internal/durablefs/syncdir_windows.go` | cross-platform directory sync helper | `crates/awiki-cli/src/durablefs.rs`, `crates/awiki-cli/src/config/write.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli durablefs --locked`; `cargo +1.79.0 test -p awiki-cli --test config_writer_contract config_writer_uses_go_style_tempfile_permissions_and_cleanup --locked`; `go test ./internal/durablefs ./internal/config -run 'TestSyncDirectory|Test.*Durable|Test.*Write|Test.*Config' -count=1` | Extracts the Go `durablefs.SyncDirectory` contract into a Rust module: Unix/non-Windows opens and syncs the directory, missing directories fail; Windows is intentionally a no-op. `config/write.rs` now wraps the shared helper with the Go `sync config dir` error prefix. No dependency was added; Windows behavior still needs Windows host evidence for full cross-platform runtime claims. |
| `awiki-cli/internal/config/write.go` | config writer helper mutators and durable write | `crates/awiki-cli/src/config/write.rs`, `crates/awiki-cli/src/config/mod.rs` | `unit_verified` | `crates/awiki-cli/tests/config_writer_contract.rs` | Translates helper-level mutators in a split Rust writer module for schema-version stamping, active identity, runtime/listener fields, DID domain, host-notify enabled/sink, OpenClaw token/hook, Hermes notify/deliver/secret, one-shot Hermes host notify setup, and legacy webhook dual-write behavior. Also ports the Go durable-write sequence with same-directory `.config-*.tmp`, file sync, temp-file chmod 0600 on Unix, atomic rename, temp cleanup, new config-dir chmod 0700 on Unix, Unix parent-directory sync, and Windows directory-sync no-op. No dependency was added. Full `yaml.v3` parser/serializer parity and Hermes CLI/bridge wiring remain separate slices. |
| `awiki-cli/internal/cli/upgrade.go` | `upgrade` command status payload and npm upgrade boundary | `crates/awiki-cli/src/app/update_handlers.rs`, `crates/awiki-cli/src/cli/mod.rs`, `crates/awiki-cli/src/cmdmeta/mod.rs` | `system_verified` | `crates/awiki-cli/tests/update_contract.rs`; `tests_v2/update/test_update_policy.py` | Implements the Go output data fields, summaries, warnings, `upgrade_attempted`, npm primary/mirror command sequence, cache-only decision path used by system tests, and network/cache-stale status from the translated update module. Npm command formatting is shared with the root preflight hint to preserve Go wording. |
| `awiki-cli/internal/cli/root.go` | root update preflight guard, update exemptions, soft-fail logging, `version_unsupported`, newer-version warning injection | `crates/awiki-cli/src/app.rs`, `crates/awiki-cli/src/app/update_preflight.rs`, `crates/awiki-cli/src/cli/mod.rs`, `crates/awiki-cli/tests/update_contract.rs` | `system_verified` | `cargo +1.79.0 test -p awiki-cli app::update_preflight --locked`; `cargo +1.79.0 test -p awiki-cli --test update_contract --locked`; `cargo +1.79.0 test -p awiki-cli update --locked`; Go `go test ./internal/cli ./internal/update -run 'Test.*Update|Test.*Preflight|Test.*Root|TestIsUpdateExemptCommandAllowsListenerServiceRun' -count=1`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/update/test_update_policy.py::test_root_preflight_soft_fails_non_exempt_update_checks_and_exempts_local_commands -q`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/update/test_update_policy.py -ra -q` | Translates Go's root preflight after global format validation: exact local/recovery command exemptions represented by current Rust parser names, including hidden `runtime listener service-run` and Hermes bridge service-run parsing; config errors ignored during preflight; `update::check` soft failures ignored unless `--verbose` prints `[awiki-cli] update check failed: ...`; unsupported non-dev versions produce `version_unsupported` exit code 3 with Go npm primary/mirror hint; and newer-version warnings are prepended to command warnings. The hidden Hermes bridge `service-run` command now dispatches to a dedicated handler, runs Go-equivalent bridge config/adapter-plan preflight, and only then reaches the deferred bridge-execution boundary instead of the generic schema stub. The `awiki-system-test` selector proves the real Rust subprocess behavior for cache-only update-check soft failures: non-exempt `status` succeeds quietly without `--verbose`, logs `[awiki-cli] update check failed:` and the cache-only cause with `--verbose`, and does not log update-check failures for local/recovery commands (`version`, `upgrade`, dry-run `init`, `docs`, `schema`, `config show`, `doctor`, and raw `completion bash`). Tests use `AWIKI_CLI_UPDATE_CACHE_ONLY=1` in subprocess helpers to avoid live npm network during unrelated deterministic CLI tests; production behavior still uses cache/network policy from `update::check`. No dependency was added. |
| `awiki-cli/internal/update/update.go` | version policy decision, strict-disable controls, metadata cache, npm registry fetch/writeback | `crates/awiki-cli/src/update/{mod,cache,version}.rs` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --lib 'update::' --locked`; `cargo +1.79.0 test -p awiki-cli --test update_contract upgrade_cache_only_uses_seeded_metadata_for_dev_builds --locked`; `cargo +1.79.0 test -p awiki-cli --test update_contract upgrade_strict_disable_follows_config_and_env_override --locked`; `go test ./internal/update -count=1`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/update/test_update_policy.py -ra -q`; live dry-run smoke `awiki-cli upgrade --dry-run --format json` | Translates dev-build detection, truthy env parsing, config/env TTL controls, cache path `<cache>/update/metadata.json`, semver-like prerelease ordering, strict-disable block suppression, `AWIKI_CLI_UPDATE_CACHE_ONLY`, npmjs/npmmirror fallback, combined registry errors, required `version`, optional `awikiCli.minSupportedVersion`, cache writeback, Unix update-dir/file permissions, proxy env handling from Go's default update client (`HTTP_PROXY`/`HTTPS_PROXY` with `NO_PROXY` bypass), and Rustls-first HTTPS fetch. Existing `awiki-system-test` coverage verifies the accepted subprocess surface in deterministic cache-only mode: seeded update metadata is consumed by `upgrade`, config/env strict-disable controls are reflected in JSON output, dev builds do not hard-block below the seeded minimum, and root preflight soft-fails guarded `status` while exempting local/recovery commands (`version`, `upgrade`, `init --dry-run`, `docs`, `schema`, `config show`, `doctor`, `completion bash`). Live registry fetch/writeback, proxy fallback, stale-cache fallback, cache permissions, and non-dev hard-block behavior remain covered by focused Rust/Go tests plus live smoke rather than deterministic `awiki-system-test`, because system tests intentionally avoid real npm registry access. Uses existing `rustls` + `webpki-roots`, no OpenSSL/native-tls/reqwest/hyper. |
| `awiki-cli/internal/upgrade/fsutil.go` | upgrade-local path checks, atomic write, JSON write, directory sync, file copy, and tree copy helpers | `crates/awiki-cli/src/upgrade/fsutil.rs`, `crates/awiki-cli/src/upgrade/{meta,journal,detect}.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli upgrade::fsutil --locked`; `cargo +1.79.0 test -p awiki-cli --test workspace_upgrade_contract --locked` | Splits the existing meta/journal atomic writer into a traceable Rust fsutil module and translates Go helper behavior: empty path checks return false, atomic same-directory `.upgrade-*.tmp` writes with temp cleanup and parent directory sync, pretty JSON writes, direct truncate-and-copy file backup helper, recursive tree copy with missing-source no-op, directory `0700` creation, Unix file mode preservation, and durable sync through the shared `durablefs` helper. Backup assembly and SQLite `VACUUM INTO` remain in a later `backup.go` slice. No dependency was added. |
| `awiki-cli/internal/upgrade/backup.go` | workspace backup assembly and SQLite backup helper | `crates/awiki-cli/src/upgrade/backup.rs`, `crates/awiki-cli/tests/workspace_upgrade_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli backup --locked`; `cargo +1.79.0 test -p awiki-cli --test workspace_upgrade_contract --locked` | Translates Go backup behavior using existing `Paths`: fixed/default backup IDs, backup dir creation, Go backup names (`config.yaml.bak`, `config.json.bak`, `identities/`, `awiki-cli.db.bak`, `meta.json.bak`, `upgrade_journal.json.bak`), absent input skipping, final backup-root sync, SQLite destination removal, single-quote escaping, and `VACUUM INTO` through the existing `rusqlite + bundled` store path. Full upgrader `Context` and `UpgradeIfNeeded` wiring remain deferred. No dependency was added. |
| `awiki-cli/internal/upgrade/{types.go,upgrader.go}` and `migration_v0_to_v1.go` k1 replacement paths | upgrade context, migration trait, default upgrader registration, plan validation, `UpgradeIfNeeded` journal phase loop, local v0->v1 apply wiring, imported legacy k1 DID replacement, and v2->v3 current k1 DID replacement | `crates/awiki-cli/src/upgrade/{upgrader,migration_v2_to_v3}.rs`, `crates/awiki-cli/tests/workspace_upgrade_contract.rs`, `crates/awiki-cli/tests/workspace_upgrade_if_needed_contract.rs`, `crates/awiki-cli/tests/workspace_migration_v0_to_v1_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli upgrade::upgrader --locked`; `cargo +1.79.0 test -p awiki-cli upgrade::migration_v2_to_v3 --locked`; `cargo +1.79.0 test -p awiki-cli --test workspace_upgrade_if_needed_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test workspace_upgrade_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test workspace_migration_v0_to_v1_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test identity_replace_did_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test store_rebind_contract --locked` | Adds the Go-shaped Rust execution skeleton and phase loop: `Context` fields mirror Go `Context`, `new_context` resolves upgrade paths, `Migration` exposes `from/to/name/is_done/apply/validate`, `new_default_upgrader` registers Go's 0->1, 1->2, and 2->3 migration names, and `plan` preserves Go's contiguous-version ordering plus error strings. `UpgradeIfNeeded` mirrors Go's inspect/current-meta capture, newer-than-supported error, empty/latest no-op return, stale journal clearing, file-lock acquisition/release, lock-inside second inspect, journal backup reuse, backup creation, `checking`/`applying`/`validating` journal phases, migration apply/validate calls, per-migration meta stamping, `Context.current_meta` updates, and final journal clearing. Local v0->v1 apply is wired for config/schema/legacy local imports, refreshes resolved config after import, and feeds imported summaries into the shared Go-style k1 replacement helper; v1->v2 cleanup can run through this loop; v2->v3 validates as a Go no-op and runs the same replacement helper for current-workspace identities. The helper now mirrors Go's service-construction ordering by preflighting the Rustls/std identity HTTP client CA-bundle path for every non-empty summary list before filtering non-k1 identities; it then skips non-k1/no-longer-k1 records, preflights handle-shaped DIDs before remote replacement, calls the existing Rust `identity::replace_did`, rebinds local SQLite owner state through `store::rebind_local_identity_state`, appends service-construction/replacement/rebind failures as warnings, stamps schema version 3, and clears the journal. Rollback remains deferred. No dependency was added. |
| `awiki-cli/internal/upgrade/{types.go,meta.go,journal.go,detect.go}` | read-only workspace upgrade paths, metadata, journal, and detection for doctor/config surfaces | `crates/awiki-cli/src/upgrade/{types,meta,journal,detect}.rs`, `crates/awiki-cli/src/doctor/mod.rs`, `crates/awiki-cli/src/app.rs` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test workspace_upgrade_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test doctor_contract --locked`; Go `go test ./internal/upgrade -run 'TestUpgradeIfNeededSkipsEmptyWorkspace|TestUpgradeIfNeededStampsCurrentWorkspaceMetadata|TestLoadLegacySettingsRejectsSplitServiceURLs' -count=1`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/core/test_basic_commands.py::test_doctor_and_config_show_report_workspace_upgrade_meta_and_journal -q`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/core/test_basic_commands.py -ra -q` | Translates Go's workspace schema version `3`, upgrade path resolution, `Meta`, `Journal`, `Detection`, and `Inspection` JSON shapes, missing-file semantics, pretty JSON saves, local config/index/SQLite/legacy detection, doctor warning precedence, and `config show` upgrade snapshot. The `awiki-system-test` selector seeds isolated `upgrade/meta.json` with migration warnings and `upgrade/upgrade_journal.json` with a running journal, then verifies `doctor` warning summaries/details and `config show` path/meta/journal/detection embedding through the real Rust subprocess. Full `UpgradeIfNeeded`, lock/backup handling, identity replacement RPC, legacy DB import execution, and cleanup migrations remain separate workspace-migration slices. No dependency was added. |
| `awiki-cli/internal/upgrade/{lock.go,lock_nonwindows.go,lock_windows.go}` | workspace upgrade file lock acquisition, metadata persistence, legacy lock compatibility, and OS lock release | `crates/awiki-cli/src/upgrade/lock.rs`, `crates/awiki-cli/src/upgrade/types.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test workspace_upgrade_contract --locked`; Go `go test ./internal/upgrade -run 'TestAcquireFileLock' -count=1` | Translates the local `AcquireFileLock` boundary with a Rust guard: required path error, upgrade dir/file creation, nonblocking OS lock before metadata checks, persistent `upgrade.lock` anchor, pretty `lockMetadata` JSON with trailing newline, residual `os_file_lock_v1` metadata overwrite, corrupt metadata ignore, dead/old legacy PID ignore, and fresh live legacy PID rejection. Unix uses direct std FFI for `flock` and `kill(0)`; Windows FFI mirrors `LockFileEx`/`UnlockFileEx` byte-range locking for future Windows validation. No file-lock crate, OpenSSL/native-tls, HTTP/TLS, platform service-manager, or new dependency was added. |
| `awiki-cli/internal/upgrade/migration_v0_to_v1.go` local helpers, v0->v1 config apply/import branches, v0->v1 validation, and local apply wiring | resolved config refresh after legacy import, target-store schema ensure, SQLite health pragmas, config-file branch, legacy local import branch, imported legacy k1 replacement handoff, local apply composition of `workspaceV0ToV1Migration.Apply`, and `workspaceV0ToV1Migration.Validate` | `crates/awiki-cli/src/{config,upgrade}/`, `crates/awiki-cli/tests/workspace_migration_v0_to_v1_contract.rs`, `crates/awiki-cli/tests/workspace_upgrade_if_needed_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test workspace_migration_v0_to_v1_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test workspace_upgrade_if_needed_contract --locked`; `cargo +1.79.0 test -p awiki-cli --locked`; Go `go test ./internal/upgrade -run 'TestUpgradeIfNeededMigratesLegacyConfigJSON|TestUpgradeIfNeededStampsCurrentWorkspaceMetadata|TestLoadLegacySettingsRejectsSplitServiceURLs|TestUpgradeIfNeededReplacesAllImportedLegacyK1Handles' -count=1` | Translates local v0->v1 behavior: `refreshResolvedConfig`, `ensureTargetStoreSchema`, and `validateSQLiteHealth`; existing `config.yaml` schema stamping; legacy `config.json` parse/write/remove; legacy `settings.json` minimal config creation; local legacy identity import; legacy SQLite import with identity-owner lookup; target schema ensure; and resolved-config refresh after imported identities. The local apply helper is wired into default `Migration::apply`, exercises legacy config migration through `UpgradeIfNeeded`, and now hands imported summaries to the shared k1 replacement helper so handle-shaped imported k1 identities can be replaced through the existing Rust identity-service path. Non-handle imported k1 identities are converted into Go-shaped warnings and do not stop the migration. The v0->v1 `validate` path checks config schema version, SQLite schema version/health, and post-legacy-identity import sanity through the default migration chain. Rollback remains deferred; v2->v3 existing-workspace DID replacement is covered in the upgrader/k1 row. No dependency was added. |
| `awiki-cli/internal/upgrade/migration_v0_to_v1.go` v1->v2 cleanup migration | `workspaceV1ToV2Migration.Apply`, legacy skill directory removal, legacy listener command cleanup, and heartbeat section removal | `crates/awiki-cli/src/upgrade/migration_v1_to_v2.rs`, `crates/awiki-cli/src/upgrade/upgrader.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli migration_v1_to_v2 --locked`; `cargo +1.79.0 test -p awiki-cli --test workspace_upgrade_contract --locked`; Go `go test ./internal/upgrade -run 'TestUpgradeIfNeededCleansLegacySkillArtifactsForExistingWorkspace|TestUpgradeIfNeededImportsLegacyWorkspace' -count=1` | Translates Go's local v1->v2 cleanup helper in a dedicated Rust module: missing-context guard, warning accumulation, legacy skill paths under `.openclaw`, platform listener artifact gates and command argv for Linux/macOS/Windows, `OPENCLAW_WORKSPACE`, `XDG_CONFIG_HOME`, `LOCALAPPDATA`, heartbeat marker removal only when the marked section references `awiki-agent-id-message`, and blank-line collapse. `Migration::apply` for 1->2 and its Go no-op `validate` are wired, but full `UpgradeIfNeeded` phase execution remains deferred. Uses std-only filesystem/process APIs; no platform service-manager crate or TLS dependency was added. |
| `awiki-cli/internal/upgrade/migration_v0_to_v1.go` legacy settings helper | legacy `settings.json` normalization for future workspace migration | `crates/awiki-cli/src/upgrade/settings.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test workspace_upgrade_contract workspace_upgrade_legacy_settings_parser_matches_go_contract --locked`; Go `go test ./internal/upgrade -run TestLoadLegacySettingsRejectsSplitServiceURLs -count=1` | Translates the pure `loadLegacySettings` boundary: reads/parses legacy settings JSON, normalizes trailing slashes on `user_service_url` and `molt_message_url`, rejects split service URLs with the Go message shape, keeps DID domain as-is, maps `message_transport.receive_mode == websocket` to `websocket`, and defaults all other receive modes to `http`. It does not write config or execute v0->v1 migration yet. No dependency was added. |
| `awiki-cli/config.template.yaml` | canonical user config template | `config.template.yaml` | `unit_verified` | `cmp -s ../awiki-cli/config.template.yaml config.template.yaml`; `AWIKI_CLI_WORKSPACE_HOME_DIR=<tmp> target/debug/awiki-cli config show --format json` with the template copied to `<tmp>/config.yaml` | Copied byte-for-byte from the Go repository so the Rust port carries the same public config template for `schema_version`, identity, runtime/listener/host-notify defaults, output defaults, service endpoints, tenant DID-domain guidance, ANP service defaults, CA bundle, and `mail_service_url`. This is a repository asset parity slice, not a config parser rewrite. Full Go `yaml.v3` parser/serializer parity remains tracked separately. |
| `awiki-cli/scripts/install.js`, `awiki-cli/scripts/run.js`, `awiki-cli/package.json` | npm package install/run wrapper | `scripts/install.js`, `scripts/run.js`, `package.json` | `system_verified` | `tests_v2/update/test_install_script.py` | Copied from the Go repository for 1:1 npm package behavior. The script contract still uses Node plus external `curl` and `tar`/PowerShell exactly like Go packaging; this is not part of the Rust binary dependency graph. |
| `awiki-cli/scripts/hermes_notify_adapter.py`, `awiki-cli/scripts/test_hermes_notify_adapter.py` | Hermes notify adapter runtime asset and Python unit tests | `scripts/hermes_notify_adapter.py`, `scripts/test_hermes_notify_adapter.py` | `unit_verified` | `python3 -m py_compile scripts/hermes_notify_adapter.py`; `python3 -m unittest discover -s scripts -p 'test_hermes_notify_adapter.py'`; Go repo same Python compile/unit checks; Rust Hermes bridge CLI/service contract tests | Copied byte-for-byte from the Go repository so the Rust bridge adapter script resolver can find the same stdlib-only runtime asset after resolving Go's candidate order. This does not add a Rust dependency and does not rewrite the adapter in Rust. Release archive inclusion remains to be verified or wired in the release-packaging lane because this repository currently only has the npm install/run wrappers, not an inspected release archive manifest. |
| `awiki-cli/scripts/host_notify_webhook_server.py` | host-notify webhook fan-out helper for OpenClaw/Hermes local testing | `scripts/host_notify_webhook_server.py` | `unit_verified` | `cmp -s ../awiki-cli/scripts/host_notify_webhook_server.py scripts/host_notify_webhook_server.py`; `python3 -m py_compile scripts/host_notify_webhook_server.py`; Go repo same Python compile check | Copied byte-for-byte from the Go repository as the same stdlib-only local host-notify webhook/callback helper used by Go's Hermes host-notify architecture notes and local test records. This is a dev/test script asset, not a Rust binary dependency and not a rewrite in Rust. It does not implement listener foreground dispatch, platform service-manager behavior, Hermes bridge orchestration, or release archive verification. |
| `awiki-cli/internal/docs/topics.go` | docs topics | `crates/awiki-cli/src/docs/mod.rs` | `system_verified` | `crates/awiki-cli/tests/core_contract.rs`; `tests_v2/core/test_basic_commands.py` | Core docs topics and error paths verified. Full docs inventory remains incremental. |
| `awiki-cli/internal/doctor/doctor.go` | doctor report contract and local diagnostics | `crates/awiki-cli/src/doctor/mod.rs`, `crates/awiki-cli/src/app.rs` | `system_verified` | `crates/awiki-cli/tests/doctor_contract.rs`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/core/test_basic_commands.py::test_doctor_and_config_show_report_workspace_upgrade_meta_and_journal -q`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/core/test_basic_commands.py -ra -q` | Translates Go's 10-check report shape, counts/summary precedence, config/env/ANP service/runtime/identity/sqlite/legacy diagnostics, workspace-upgrade meta/journal/detection check integration, and local `anp-mls system version --json-in -` compatibility probe without adding network/TLS dependencies. The core system-test selector now proves the workspace-upgrade warning precedence and details for `doctor` using isolated meta and journal files. Runtime listener status follows the current Rust local-listener parity lane, and full Go YAML parse-error parity remains deferred. |
| `awiki-cli/internal/cli/init.go` | init command and workspace bootstrap | `crates/awiki-cli/src/app.rs` | `system_verified` | `crates/awiki-cli/tests/core_contract.rs`; `tests_v2/core/test_basic_commands.py` | Dry-run behavior and non-dry-run workspace bootstrap implemented. Non-dry-run init now creates the real SQLite schema through the store lane. |
| `awiki-cli/internal/cli/id.go` | `id create`, live phone/email `id register`, live `id bind`, live `id recover`, `id list`, `id current`, `id use`, `id status`, `id import-v1`, live `id refresh-token`, live `id replace-did`, live `id profile set/get`, live `id resolve` | `crates/awiki-cli/src/app.rs`, `crates/awiki-cli/src/app/id_recover_handlers.rs`, `crates/awiki-cli/src/app/id_replace_did_handlers.rs`, `crates/awiki-cli/src/cli/mod.rs`, `crates/awiki-cli/src/cmdmeta/mod.rs`, `crates/awiki-cli/src/identity/*.rs` | `system_verified` | `crates/awiki-cli/tests/identity_contract.rs`; `crates/awiki-cli/tests/identity_live_contract.rs`; `crates/awiki-cli/tests/identity_register_email_live_contract.rs`; `crates/awiki-cli/tests/identity_recover_live_contract.rs`; `crates/awiki-cli/tests/identity_replace_did_live_contract.rs`; `crates/awiki-cli/tests/identity_wire_contract.rs`; full `cargo +1.79.0 test -p awiki-cli --locked`; focused `tests_v2/id/test_identity_cli.py` selectors for create/use/import/register/refresh/profile/bind/recover/replace-did validation plus the register/profile/resolve/bind/recover flow; `tests_v2/page` | Local identity store behavior now writes the Go v2 layout, uses local `../anp/rust` for e1 DID/key generation, sanitizes public `user_id`, supports flat/indexed v1 identity import, exposes the public dangerous replace-did dry-run plan, and preserves Go profile/bind/recover/replace-did validation before service execution. Live `id register --handle --phone` sends `/user-service/handle/rpc` `send_otp` without local writes; live `id register --handle --phone --otp` generates a handle-path e1 DID, posts `/user-service/did-auth/rpc` `register`, saves handle/full_handle/user_id/JWT, and unblocks page lifecycle system tests. Live `id register --handle --email` now preserves Go email status/send activation bootstrap without local writes; `--wait` reuses the same status polling boundary, skips send when already verified, then generates the handle-path e1 DID and completes `did-auth.register` with `method=email`. Live `id bind` preserves dry-run plan shapes, exactly-one phone/email validation, active identity/JWT auth requirements, authenticated phone bind send/verify REST calls, authenticated email status/send REST calls, 404-as-unverified email status, email no-wait send, wait pending, and already-verified completed result shapes. Live `id recover` preserves Go OTP-send and full recovery execution: `handle.send_otp`, handle-path recovered DID generation, recover backup creation, `did-auth.recover_handle`, temporary recovered identity persistence, recovered-state SQLite merge, final promotion, public-field cleanup, archived-identity warning, and `--identity` ignored warning. Live `id replace-did` preserves Go danger warning, handle-path e1 DID/key regeneration, `.legacy-backup/replace-did` identity backup, authenticated `did-auth.replace_did`, no-JWT `get_me` bootstrap, local identity writeback, old identity directory removal, SQLite owner-DID rebind counts, E2EE cleanup counts, and backup-failure-before-remote ordering. Live `id refresh-token` loads the selected/default identity, bypasses the stored bearer, sends signed DID-auth `get_me`, accepts body or header tokens through the shared authsdk path, persists the fresh JWT to `auth.json`, and renders the Go result shape. Live profile set/get and resolve are wired through the shared auth/session and user-service wire builders. Service profile timeouts and shared RPC/JWT trace phases now flow through the shared Rustls HTTP path; deeper handle lookup and local DB trace phases remain separate trace-depth work. |
| `awiki-cli/internal/cli/msg.go`, `awiki-cli/internal/message/{types.go,secure_commands.go}` | `msg` dry-run and CLI-boundary surface: `send`, `attachment download`, `inbox`, `history`, `mark-read`, `secure status/init/repair/failed/retry/drop` | `crates/awiki-cli/src/app/msg_handlers.rs`, `crates/awiki-cli/src/cli/mod.rs`, `crates/awiki-cli/src/cmdmeta/mod.rs` | `system_verified` | `crates/awiki-cli/tests/msg_contract.rs`; `crates/awiki-cli/tests/group_contract.rs`; `tests_v2/core/test_output_contracts_cli.py` representative output contract; `tests_v2/cli/test_awiki_cli_direct_local.py::test_awiki_cli_msg_and_attachment_commands_validate_local_arguments`; Go `go test ./internal/cli -run 'TestMsg' -count=1` | Translates Go dry-run plan shapes, command catalog, shared identity-module bare-handle completion, attachment send/download metadata, secure direct command plans, Cobra-like required-flag failures, and handler-level attachment download target validation before live service execution. The local parser now preserves Go's string-valued `--secure on` / `--secure=on` behavior instead of treating `secure` as a boolean flag, and `run_msg_send` passes `SecureMode` into the service request. The message/group CLI exit mapper now preserves Go's `unsupported_mode` hints for unsupported secure direct and PR-A group E2EE self-leave plus `transport_unavailable` for websocket bridge failures; Rust-only `AttachmentNotSupported`/`GroupNotSupported` remain `not_implemented` because Go has no matching sentinels. Ordinary direct text, non-E2EE group text, direct/group attachment live HTTP execution, direct secure send HTTP execution, production `msg secure retry` HTTP execution, production `msg secure init` HTTP execution, and production `msg secure repair` HTTP execution are covered by dedicated live slices below. Ordinary direct text and ordinary direct mark-read also have Go WebSocket/local bridge production slices below; remaining WebSocket inbox/history/group/cache fallback execution remains separate. |
| `awiki-cli/internal/message/secure_commands.go`, `awiki-cli/internal/cli/msg.go` | secure status/failed/drop local command subset | `crates/awiki-cli/src/message/secure_commands.rs`, `crates/awiki-cli/src/app/msg_handlers.rs`, `crates/awiki-cli/tests/message_secure_commands_contract.rs`, `crates/awiki-cli/tests/msg_secure_status_failed_live_contract.rs`; `tests_v2/cli/test_awiki_cli_direct_local.py` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test message_secure_commands_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_secure_status_failed_live_contract --locked`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/cli/test_awiki_cli_direct_local.py::test_awiki_cli_msg_secure_status_failed_and_drop_use_local_outbox_without_services -q`; Go `go test ./internal/message -run 'TestServiceSecureStatusReturnsSessionAndOutboxSummary|TestServiceSecureFailedAndDropOperateOnOutbox' -count=1` | Translates the local SQLite/filesystem-backed live behavior for `msg secure status`, `msg secure failed`, and `msg secure drop`: active/ready identity gate, optional `--with` DID/handle resolution, `p5-e2ee-sessions/*.json` object loading, peer filtering, session sort by `peer_did`, Go status session redaction with `skipped_key_count`, store open/schema, outbox owner/credential listing, status peer filtering, blank `local_status` counted as `unknown`, status outbox row redaction, failed-row listing with full unredacted Go row shape, drop existence check before mutation, `local_status=dropped` update, Go summary/data shapes, live CLI command-boundary routing, and missing-row `query returned no rows` internal-error parity. The awiki-system-test selector seeds ready local identities and `e2ee_outbox` rows directly in isolated SQLite, verifies subprocess `status` redaction/order/counts, `failed` active-identity filtering and unredacted rows, and `drop` mutation without message-service, user-service, mail-service, registered identity, listener, or service-manager dependencies. This row covers only local status/failed/drop; `SecureRetry`, `SecureInit`, `SecureRepair`, HTTP sender execution, WebSocket/local bridge execution, runtime listener E2EE processing, and full secure-direct acceptance are tracked separately. No dependency was added beyond the existing approved `rusqlite + bundled` store lane. |
| `awiki-cli/internal/message/secure_commands.go`, `awiki-cli/internal/message/secure_control.go` | `SecureRetry` production HTTP sender wiring and `FlushQueuedSecureOutbox` queued-row listing/action execution | `crates/awiki-cli/src/message/{secure_commands,secure_outbox_flush}.rs`, `crates/awiki-cli/src/message/mod.rs`, `crates/awiki-cli/src/app/msg_handlers.rs`, `crates/awiki-cli/tests/{message_secure_commands_contract,message_secure_outbox_flush_contract,msg_live_contract}.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test message_secure_commands_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test message_secure_outbox_flush_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_live_contract --locked`; Go `go test ./internal/message -run 'TestServiceSecureRetryMarksQueuedRecordSent|TestFlushQueuedSecureOutboxSendsCipherAfterConfirmation' -count=1` | Translates the store-backed and production HTTP portions of Go `SecureRetry` and `FlushQueuedSecureOutbox`: active identity gate, local store open/schema, get-before-mutate missing-row behavior, selected row status reset to `queued`, peer filter from the selected row, sender initialization before queued-row send execution, sender-initialization failure as warning with the row left queued, queued-row list by owner/credential/status, stable `created_at` order, text/json request shape with outbox ID used as operation/message ID, invalid JSON failure before sender invocation, `MessageServiceE2EEClient::send_text/send_json` over the existing Rustls/std authenticated `/im/rpc` client, session lookup only after send success, send failure mutation with `send_failed`/`retry`, success metadata with `target_did`/`operation_id`/`delivery_state`/`flushed_from`, sent row marking, message-store persistence after successful mark-sent, warning compaction, selected-row reload with null fallback, and Go summary/data shape. The CLI non-dry-run path now calls this production wrapper; the live smoke proves `msg secure retry <OUTBOX_ID>` sends `application/anp-direct-cipher+json`, marks the row sent, and stores a local E2EE outbound message. `SecureRepair` production command wiring is covered by its dedicated row; WebSocket/local bridge retry execution and awiki-system-test secure-direct acceptance remain deferred. No dependency was added; files remain under the 1200-line review-size cap. |
| `awiki-cli/internal/message/secure_commands.go`, `awiki-cli/internal/message/secure_control.go`, `awiki-cli/internal/cli/msg.go` | `SecureInit` production HTTP sender wiring and manual secure-init session creation | `crates/awiki-cli/src/message/secure_commands.rs`, `crates/awiki-cli/src/message/mod.rs`, `crates/awiki-cli/src/app/msg_handlers.rs`, `crates/awiki-cli/tests/{message_secure_commands_contract,msg_live_contract}.rs`; `tests_v2/cli/test_awiki_cli_secure_init_local.py` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test message_secure_commands_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_live_contract --locked`; Go `go test ./internal/message -run 'TestServiceSecureInitCreatesPendingSession' -count=1`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_BINARY=/home/ecs-user/awiki-space/awiki-cli-rs2/target/debug/awiki-cli AWIKI_CLI_UPDATE_CACHE_ONLY=1 PYTHONDONTWRITEBYTECODE=1 uv run pytest tests_v2/cli/test_awiki_cli_secure_init_local.py::test_awiki_cli_msg_secure_init_command_sends_manual_direct_init -ra -q` | Translates and system-verifies production Go `SecureInit`: active/ready identity gate, key-material requirement with exact Go error text, required target handling, DID/handle resolution, warning-only local prekey publish before session reuse, redacted existing-session reuse result with `reused=true`, authenticated Rustls/std `/im/rpc` E2EE client construction, generated `secure-init-<operation>` message/operation ID, `BuildSecureInitPayload` manual-init JSON sent through `MessageServiceE2EEClient::send_json`, delivery fallback fields, post-send redacted session reload with ignored reload errors, Go summary/data/warning shapes, and CLI non-dry-run wiring with the Go error hint. The Rust fake-server live smoke proves the production RPC sequence and pending session file creation; the `awiki-system-test` selector proves a real Rust subprocess `msg secure init --with <bob_did>` against the configured message service emits a direct-init wire message, returns the Go-shaped initialized result with pending initiator session redaction, and leaves raw service history encrypted as `application/anp-direct-init+json`. `SecureRetry` and `SecureRepair` production command system acceptance, WebSocket/local bridge secure execution, runtime listener live `ProcessIncoming`, and full secure-direct acceptance remain separate. No dependency was added; files remain below the 1200-line review-size cap. |
| `awiki-cli/internal/message/secure_commands.go`, `awiki-cli/internal/cli/msg.go` | `SecureRepair` production reset plus new secure-init execution | `crates/awiki-cli/src/message/secure_commands.rs`, `crates/awiki-cli/src/message/mod.rs`, `crates/awiki-cli/src/app/msg_handlers.rs`, `crates/awiki-cli/tests/msg_secure_repair_live_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test msg_secure_repair_live_contract --locked`; Go `go test ./internal/message -run 'TestServiceSecureRepairResetsFailedOutboxAndStartsNewInit' -count=1` | Translates production Go `SecureRepair`: shared `prepareSecurePeerAction` active identity/key-material/target/prekey-warning behavior, peer session lookup and deletion from `p5-e2ee-sessions`, failed E2EE outbox listing by owner/identity, same-peer failed-row reset to `queued`, reset-count accumulation, second Go-equivalent `SecureInit` call after reset, `data.repair.peer_did/peer_handle/reset_records`, `Repaired secure session with <peer>` summary, warning compaction with prepare warnings before init warnings, and CLI non-dry-run wiring with the Go repair error hint. The live fake-server smoke proves old-session removal, same-peer outbox requeue, other-peer failed row preservation, production direct-init send, and new pending initiator session creation. WebSocket/local bridge secure execution, runtime listener live `ProcessIncoming`, and awiki-system-test secure-direct acceptance remain deferred. No dependency was added; the new focused test file stays below the 1200-line review-size cap. |
| `awiki-cli/internal/message/secure.go`, `awiki-cli/internal/message/service.go` | best-effort secure prekey publishing before direct `Inbox` and `History` reads | `crates/awiki-cli/src/message/{service,inbox,history,secure_commands}.rs`, `crates/awiki-cli/tests/{msg_secure_prekey_read_live_contract,msg_live_contract,msg_ws_inbox_live_contract,msg_ws_history_live_contract}.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test msg_secure_prekey_read_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_ws_inbox_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_ws_history_live_contract --locked`; Go source parity plus adjacent `go test ./internal/message -run 'TestServiceSendSecureDirectUsesP5KeyServiceTargetAndPersistsPendingSession|TestServiceSecureInitCreatesPendingSession|TestWSProxyTransportCallsLocalBridgeAndDecodesResponses|TestWSProxyTransportWrapsBridgeFailures|TestAllInboxMergesLocalMailNotifications|TestReadHistoryFromCacheByPeerDIDs' -count=1` | Wires Go `maybePublishSecurePrekeys` behavior into direct read paths without changing transport semantics: secure-capable identities attempt two `direct.e2ee.publish_prekey_bundle` RPCs before remote `inbox.get`, handle lookup, or `direct.get_history`; missing E2EE/key1 material remains a no-op; auth, publisher construction, and publish failures are compacted as Go-shaped warnings and do not block the read. Direct remote inbox/history warnings are seeded before WebSocket HTTP fallback, persist/decrypt/contact-sync, and handle-history warnings; unresolved-handle and WebSocket local-cache early returns still drop prekey warnings like Go; `scope=all` appends the outer publish warnings after `allInbox` returns, including the Go duplicate-publish quirk when `allInbox` recursively calls direct inbox. `scope=group` remains local-cache-only and does not publish because Go returns before active-identity/prekey handling. The focused live test proves CLI RPC order and warning-only failure behavior, while existing live tests were updated for the new pre-read publish sequence. No dependency was added; this reuses the existing Rustls/std message client, local ANP Rust E2EE adapter, and approved `rusqlite + bundled` store lane. Source and new focused test files stay below the 1200-line cap; `msg_live_contract.rs` remains exactly 1200 lines and is kept only for legacy fixture updates. |
| `awiki-cli/internal/message/secure.go`, ANP SDK `direct_e2ee/client.go`, `prekey_manager.go` | `NewSecureE2EEClientForRecord`, high-level `MessageServiceE2EEClient` publish/send adapter, and local DID resolver boundary | `crates/awiki-cli/src/message/secure_client.rs`, `crates/awiki-cli/src/message/mod.rs`, `crates/awiki-cli/tests/message_secure_client_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test message_secure_client_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test anpsdk_contract --locked`; Go `go test ./internal/message -run 'TestServiceSendSecureDirectUsesP5KeyServiceTargetAndPersistsPendingSession|TestServiceSecureInitCreatesPendingSession' -count=1`; Go `go test ../anp/golang/direct_e2ee -run 'TestClientSendAndPendingHistoryProcessing|TestClientFallsBackToSignedPrekeyWhenOPKUnavailable' -count=1` | Extends the previous preparation boundary into a narrow Rust high-level direct-E2EE client adapter around the local ANP Rust primitives. Preserves manager/record required errors, identity path lookup, DID signing key and E2EE agreement key parsing with Go error prefixes, Go P5 store roots (`p5-e2ee-sessions`, `p5-signed-prekeys`, `p5-one-time-prekeys`), owner DID and key IDs (`#key-1`, `#key-3`), current-record DID document precedence, local manager fallback, remote DID resolver fallback, and local message-service DID extraction errors. Adds injected-RPC parity for `PublishPrekeyBundle`, `SendText`, and `SendJSON`: signed prekey `spk-initial`, expiry `2030-01-01T00:00:00Z`, 16 one-time prekeys, Go's explicit publish double-call quirk, exact RPC method names (`direct.e2ee.publish_prekey_bundle`, `direct.e2ee.get_prekey_bundle`, `direct.send`), prekey service target shape, OPK retry markers, operation/message ID equality validation, init send content type, saved pending-confirmation initiator sessions, and pending follow-up error `invalid field: session pending confirmation`. Later service/command rows wire `msg send --secure on`, `msg secure retry`, `msg secure init`, and `msg secure repair` to this adapter over the existing Rustls/std HTTP client. Real WebSocket transport and awiki-system-test secure-direct acceptance remain deferred. No dependency was added; the adapter reuses the existing local ANP Rust SDK and existing pure Rust crypto/base64/rand stack, and source/test files stay below the 1200-line review-size cap. |
| ANP SDK `direct_e2ee/client.go`, `session.go`, `models.go`, `store.go` and `awiki-cli/internal/message/secure_incoming.go` | high-level `MessageServiceE2EEClient` incoming `ProcessIncoming` and `DecryptHistoryPage` adapter boundary | `crates/awiki-cli/src/message/secure_client.rs`, `crates/awiki-cli/src/anpsdk.rs`, `crates/awiki-cli/tests/message_secure_client_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test message_secure_client_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test anpsdk_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test runtime_listener_secure_normalize_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test runtime_listener_secure_sync_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test runtime_listener_secure_ack_in_process_contract --locked`; Go `go test ./direct_e2ee -run 'TestClientSendAndPendingHistoryProcessing|TestJSONRoundTripForPendingResult|TestSessionInitAndFollowUpRoundTrip|TestSkippedMessageKeySurvivesFailedAuthentication|TestClientFallsBackToSignedPrekeyWhenOPKUnavailable|TestDirectInitBodyOmitsAbsentOPKAndLegacyStaticField|TestSharedP5Vectors|TestSharedP5VectorAADDoesNotContainApplicationContentType' -count=1` | Adds adapter parity for Go ANP SDK v0.8.7 incoming direct E2EE behavior: Go-style inbound map parsing, direct init decrypt through sender DID document resolution, signed-prekey load, optional OPK load/delete, responder session save, decrypted plaintext result shape, cipher session-load error queuing keyed by sender DID, pending result shape, decrypt-failure `undecryptable` result with session save, successful follow-up decrypt with session save, pending replay in insertion order after successful init, unsupported content-type error text, and `DecryptHistoryPage` copy/stable-sort/process behavior. Records the Go v0.8.7 fact that history results are returned in sorted processing order after `server_seq` ascending and `meta.message_id` tie-break; there is no final reverse in that SDK implementation. Production `msg send --secure on`, production `msg secure retry`, production `msg secure init`, production `msg secure repair`, inbox/history secure decrypt application, and polling ACK/flush side effects are covered by later rows; runtime listener real `ProcessIncoming` and awiki-system-test secure-direct acceptance remain deferred. No dependency was added; source/test files stay below the default 1200-line review-size cap. |
| `awiki-cli/internal/message/secure_incoming.go`, `awiki-cli/internal/message/service.go` | direct E2EE inbox/history scoped system flow: real secure wire decrypt, display filtering, handle-history raw-wire suppression, and mark-read returned-row behavior | `crates/awiki-cli/src/message/secure_incoming.rs`, `crates/awiki-cli/src/message/service.rs`, `crates/awiki-cli/tests/message_secure_incoming_contract.rs`, `crates/awiki-cli/tests/msg_live_contract.rs`; `tests_v2/cli/test_awiki_cli_direct_local.py` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test message_secure_incoming_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test message_secure_client_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test message_secure_outbox_flush_contract --locked`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 PYTHONDONTWRITEBYTECODE=1 uv run pytest tests_v2/cli/test_awiki_cli_direct_local.py::test_awiki_cli_can_send_secure_direct_messages_with_manual_reply_confirmation tests_v2/cli/test_awiki_cli_direct_local.py::test_awiki_cli_secure_direct_handle_queries_hide_raw_wire_cache_and_mark_read -ra -q`; Go `go test ./internal/message -run 'TestPollingInboxDecryptsDirectInitAndSendsSecureAck|TestFlushQueuedSecureOutboxSendsCipherAfterConfirmation|TestServiceSendSecureDirectUsesP5KeyServiceTargetAndPersistsPendingSession|TestServiceSendSecureDirectQueuesFollowUpWhilePendingConfirmation' -count=1` | Adds scoped system evidence on top of the verified E2EE client adapter and service application layer. The awiki-system-test selectors prove real subprocess `msg inbox` decrypts an `application/anp-direct-init+json` secure direct message, `msg history` decrypts secure direct cipher replies/follow-ups, displayed rows carry plaintext plus `secure=true`, raw service history remains encrypted, handle-based inbox/history queries suppress stale raw secure wire cache rows, and `--mark-read` marks returned secure rows read. Contract tests still cover exact secure wire content-type gating, Go notification shape, numeric-only `server_seq`, processing order, skipped/decrypt-failed warning text, decrypted text/json/binary rewrites, secure control hiding, pending-state display behavior, undecryptable/failed/empty-state filtering, persistence, and cache merge filtering. The scoped system evidence does not claim explicit `msg secure retry/init/repair`, WebSocket/local bridge secure execution, runtime listener live `ProcessIncoming`, or ACK send-failure warning edges. No dependency was added; source/test files stay below the default 1200-line review-size cap. |
| `awiki-cli/internal/message/secure_incoming.go`, `awiki-cli/internal/message/service.go` | direct E2EE polling ACK/flush side-effect boundary | `crates/awiki-cli/src/message/secure_incoming.rs`, `crates/awiki-cli/src/message/service.rs`, `crates/awiki-cli/tests/message_secure_incoming_contract.rs`, `crates/awiki-cli/tests/message_secure_outbox_flush_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test message_secure_incoming_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test message_secure_client_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test message_secure_outbox_flush_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_live_contract --locked`; Go `go test ./internal/message -run 'TestPollingInboxDecryptsDirectInitAndSendsSecureAck|TestFlushQueuedSecureOutboxSendsCipherAfterConfirmation' -count=1` | Preserves Go's polling side-effect order inside `maybeDecryptDirectE2EEMessages`: process incoming, run ACK/flush side effects, then apply the decrypted result to the row. Covers `maybeFlushPollingSecureAck`, `maybeAckPollingDirectInit`, and `directInitSessionIDFromMessage`: decrypted secure ACK plaintext flushes queued secure outbox rows for the original sender, decrypted polling direct-init sends `ack-<session_id>` via `SendJSON` and flushes queued rows only after a successful ACK send, self/blank peers are ignored, ACK send failures produce Go's warning text without flushing, and session IDs are extracted only from Go-equivalent object/string-object content. The direct secure system selectors above exercise the successful secure-init/reply/follow-up path, but explicit ACK failure/warning edges, runtime listener live `ProcessIncoming`, and WebSocket/local bridge secure execution remain contract-only or separate parity slices. No dependency was added. |
| `awiki-cli/internal/message/secure.go`, `awiki-cli/internal/message/service.go` | `sendSecureDirect` production direct send path, pending-confirmation queue result, and secure sent-message persistence | `crates/awiki-cli/src/message/service.rs`, `crates/awiki-cli/src/app/msg_handlers.rs`, `crates/awiki-cli/src/cli/mod.rs`, `crates/awiki-cli/tests/{message_secure_send_contract,msg_contract,msg_live_contract}.rs`; `tests_v2/cli/test_awiki_cli_direct_local.py` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test message_secure_send_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test message_secure_client_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test message_secure_outbox_flush_contract --locked`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 PYTHONDONTWRITEBYTECODE=1 uv run pytest tests_v2/cli/test_awiki_cli_direct_local.py::test_awiki_cli_can_send_secure_direct_messages_with_manual_reply_confirmation tests_v2/cli/test_awiki_cli_direct_local.py::test_awiki_cli_secure_direct_handle_queries_hide_raw_wire_cache_and_mark_read -ra -q`; Go `go test ./internal/message -run 'TestServiceSendSecureDirectUsesP5KeyServiceTargetAndPersistsPendingSession|TestServiceSendSecureDirectQueuesFollowUpWhilePendingConfirmation|TestPollingInboxDecryptsDirectInitAndSendsSecureAck|TestFlushQueuedSecureOutboxSendsCipherAfterConfirmation' -count=1` | Wires the previously injected secure direct boundary into production direct message sends over the existing Rustls/std HTTP client: Go entry-level target/text validation before `SecureMode == "on"` routing, active identity and key-material gate inside the secure path, target resolution before auth/E2EE initialization, authenticated `/im/rpc` bridge for arbitrary direct E2EE methods, best-effort prekey publish warnings, high-level `MessageServiceE2EEClient::send_text`, generated `msg-` operation/message IDs, success result fallback fields, `request.secure_mode="on"` persistence, `message.secure=true`, and local `messages.is_e2ee=1`. The live fake-server smoke proves CLI `msg send --secure on` emits the Go RPC sequence (`direct.e2ee.publish_prekey_bundle`, `direct.e2ee.get_prekey_bundle`, `direct.send`) and stores a secure outbound row; the awiki-system-test selectors prove real `msg send --secure on` init, reply, and follow-up messages against `awiki.info` are delivered as encrypted wire messages and displayed as secure plaintext by the recipient's inbox/history. Production secure retry, secure init, and secure repair sender wiring are covered by their dedicated rows; WebSocket/local bridge secure execution, runtime listener live `ProcessIncoming`, and full secure-direct acceptance beyond these focused selectors remain separate. No dependency was added; files remain under the 1200-line review-size cap. |
| `awiki-cli/internal/message/ws_proxy_client.go` | local WebSocket bridge proxy transport helper for direct, inbox/history/mark-read, and non-E2EE group methods | `crates/awiki-cli/src/message/ws_proxy.rs`, `crates/awiki-cli/src/message/mod.rs`, `crates/awiki-cli/tests/message_ws_proxy_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test message_ws_proxy_contract --locked`; Go `go test ./internal/message -run 'TestWSProxyTransportCallsLocalBridgeAndDecodesResponses|TestWSProxyTransportWrapsBridgeFailures|TestDecodeMapIntoHandlesNilAndTypedDestination' -count=1` | Translates Go `WSProxyTransport` as a helper on top of `runtime::bridge::call_local_bridge`: `direct.send`, `group.send`, `inbox.get`, `direct.get_history`, `inbox.mark_read`, `group.create/get_info/join/add/remove/leave/get/list/list_members/list_messages/update_profile/update_policy` bridge method mapping, positive `skip` preservation for history/list messages, `ErrTransportUnavailable` wrapping as `MessageError::TransportUnavailable`, and Go `decodeMapInto` zero-value tolerance for send-result structs. This does not wire the real message service/CLI execution path to WebSocket mode, implement foreground listener `handleBridgeRequest`, add WebSocket crates, or change HTTP fallback/cache behavior. No dependency was added. |
| `awiki-cli/internal/message/service.go` direct `Send` WebSocket transport branch | ordinary direct text `msg send --to --text` production local bridge execution and HTTP fallback in `runtime.mode=websocket` | `crates/awiki-cli/src/message/service.rs`, `crates/awiki-cli/tests/msg_ws_proxy_live_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test msg_ws_proxy_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test message_ws_proxy_contract --locked`; `cargo +1.79.0 check -p awiki-cli --locked`; Go `go test ./internal/message -run 'TestWebSocketFallbackWarnings|TestWSProxyTransportCallsLocalBridgeAndDecodesResponses|TestWSProxyTransportWrapsBridgeFailures' -count=1` | Wires the previously translated `WSProxyTransport` into the ordinary direct text send path only: after active identity and target resolution, `runtime.mode=websocket` calls local bridge method `direct.send` with Go params `target/text/type` and identity name; successful bridge results are persisted through the same direct-send cache path and do not add a Go-absent `data.source` field. If the bridge call fails, Rust attempts the same HTTP `/im/rpc direct.send` fallback, records trace fallback `websocket_to_http`, appends Go's visible `websocketHTTPFallbackWarning`, and returns the original bridge error when HTTP fallback also fails. Attachments, secure direct E2EE, direct inbox/history WebSocket execution, group send/messages WebSocket execution, local cache fallback, and foreground listener bridge dispatch remain separate parity slices. No dependency was added. `service.rs` is 976 lines after this correction, under the default 1200-line cap, and the Go counterpart `internal/message/service.go` is 1372 lines. |
| `awiki-cli/internal/message/service.go` `MarkRead` WebSocket transport branch | ordinary direct `msg mark-read` production local bridge execution, HTTP fallback warning, and local read mutation in `runtime.mode=websocket` | `crates/awiki-cli/src/message/{mark_read,service}.rs`, `crates/awiki-cli/tests/msg_ws_mark_read_live_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test msg_ws_mark_read_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_ws_proxy_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test message_ws_proxy_contract --locked`; `cargo +1.79.0 check -p awiki-cli --locked`; Go `go test ./internal/message -run 'TestBuildMarkReadRPCParamsValidatesMessageIDs|TestBuildMarkReadRPCParamsRequiresMessageIDs|TestWSProxyTransportCallsLocalBridgeAndDecodesResponses|TestWSProxyTransportWrapsBridgeFailures' -count=1`; Go `go test ./internal/store -run 'TestMessageQueryHelpersLookupAndMarkReadRespectOwner|TestListDirectMessagesByPeerDIDsFiltersUnreadInboxOnlyAndDeduplicates' -count=1` | Wires Go `Service.MarkRead` behavior through a dedicated Rust `message::mark_read` module to keep `service.rs` under the 1200-line file-size guideline. The slice preserves active identity gating, local store open/schema best effort, Go classification of requested IDs into direct/group/local-only mail notification rows, remote transport only for direct IDs, local bridge method `inbox.mark_read` with `message_ids`, HTTP `/im/rpc inbox.mark_read` fallback on bridge failure, trace fallback `websocket_to_http`, Go's visible WebSocket HTTP fallback warning for mark-read, original bridge error on bridge+HTTP double failure, post-success local SQLite `is_read` mutation for direct/group/local-only IDs, Go `updated_count` fallback/addition rules, and compacted warnings. Direct inbox/history WebSocket execution, group send/messages WebSocket execution, local cache fallback, foreground listener bridge serving, attachments, and secure direct E2EE remain separate parity slices. No dependency was added. `service.rs` is 1151 lines and the new `mark_read.rs` is 157 lines, both below the default 1200-line cap. |
| `awiki-cli/internal/message/service.go` `History` WebSocket transport/cache branch | ordinary direct `msg history --with` production local bridge execution, local cache fallback, HTTP fallback warning, and handle-history cache merge in `runtime.mode=websocket` | `crates/awiki-cli/src/message/{history,service}.rs`, `crates/awiki-cli/tests/msg_ws_history_live_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test msg_ws_history_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_ws_proxy_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_ws_mark_read_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test message_ws_proxy_contract --locked`; `cargo +1.79.0 check -p awiki-cli --locked`; Go `go test ./internal/message -run 'TestWSProxyTransportCallsLocalBridgeAndDecodesResponses|TestWSProxyTransportWrapsBridgeFailures|TestReadHistoryFromCacheByPeerDIDs|Test.*History' -count=1`; Go `go test ./internal/store -run 'TestListDirectMessagesByPeerDIDsFiltersUnreadInboxOnlyAndDeduplicates|TestMessageQueryHelpersLookupAndMarkReadRespectOwner' -count=1` | Extracts Go `Service.History` into a dedicated Rust `message::history` module to keep `service.rs` below the 1200-line default cap. The slice preserves target validation/default limit, active identity gating, handle target resolution, handle-resolution-failure fallback to `local_handle_history_cache`, WebSocket bridge method `direct.get_history`, source defaulting to `local_ws_cache` in websocket mode, local websocket cache fallback before HTTP, secure wire-row suppression for cache fallback, `local_ws_cache_fallback` summary/warning/source, signed HTTP `/im/rpc direct.get_history` fallback with trace fallback `websocket_to_http` and visible WebSocket HTTP fallback warning, Go's history double-failure behavior returning the HTTP-side error, history persistence, direct E2EE display filtering, and handle-history cache merge with `resolved_dids`. Direct inbox WebSocket execution, group send/messages WebSocket execution, foreground listener bridge serving, attachments, secure direct E2EE WebSocket execution, and full `awiki-system-test` acceptance remain separate parity slices. No dependency was added. `service.rs` is 1082 lines, `history.rs` is 318 lines, and the focused test file is 696 lines, all below the default 1200-line cap. |
| `awiki-cli/internal/message/group_service.go` `sendGroup` and `GroupMessages` WebSocket transport/cache branches | ordinary non-E2EE group text send and group message listing production local bridge execution, group message local cache fallback, HTTP fallback warning, and source defaulting in `runtime.mode=websocket` | `crates/awiki-cli/src/message/{group_service,group_ws,ws_proxy}.rs`, `crates/awiki-cli/tests/msg_ws_group_live_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test msg_ws_group_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test group_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test group_e2ee_send_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test group_e2ee_decrypt_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test message_ws_proxy_contract --locked`; `cargo +1.79.0 check -p awiki-cli --locked`; Go `go test ./internal/message -run 'TestWSProxyTransportCallsLocalBridgeAndDecodesResponses|TestWSProxyTransportWrapsBridgeFailures|TestHTTPTransportGroupMethodsUseExpectedRPCMethods|TestBuildGroupMessagesRPCParams' -count=1`; Go `go test ./internal/message -run 'TestTransportSource|TestSourceWithDefault|TestWebSocketFallbackWarnings' -count=1` | Extracts ordinary group websocket orchestration into `message::group_ws` so `group_service.rs` stays below the 1200-line default cap. The slice preserves Go `sendGroup` routing after the existing group E2EE auto-upgrade guard: websocket mode calls local bridge `group.send`, successful sends persist through the existing group-send cache path and return source `local_ws_cache`, bridge failures fall back to signed HTTP `/im/rpc group.send`, record trace fallback `websocket_to_http`, append the visible websocket HTTP fallback warning, and return source `remote_http`. If HTTP auth/client preparation fails after a bridge send failure, Rust returns the original bridge error like Go; HTTP RPC param build and RPC failures after preparation remain HTTP-side errors. It also preserves Go `GroupMessages` routing: websocket mode calls local bridge `group.list_messages`, successful results still run group E2EE decrypt, persistence, and cache projection before returning source default `local_ws_cache`; bridge failure first returns nonempty local group message cache as `source=local_ws_cache_fallback` with summary `Loaded group messages from local cache` and no HTTP request; empty cache falls back to signed HTTP `/im/rpc group.list_messages` with trace fallback and visible warning, then decrypts/persists/projects like the HTTP path. If HTTP auth/client preparation fails after a bridge list failure, Rust returns the original bridge error like Go. Group lifecycle/control commands still use HTTP even in websocket mode. Group E2EE websocket transport, foreground listener bridge serving, attachments, secure direct E2EE WebSocket execution, and full `awiki-system-test` acceptance remain separate parity slices. No dependency was added. `group_service.rs` is 1105 lines, `group_ws.rs` is 367 lines, and the focused test file is 880 lines, all below the default 1200-line cap. |
| `awiki-cli/internal/message/service.go` direct `Inbox` WebSocket transport/cache branch | ordinary direct/non-`all`/non-`group` `msg inbox --scope direct` production local bridge execution, local cache fallback, HTTP fallback warning, handle-history cache merge, and mark-read result update in `runtime.mode=websocket` | `crates/awiki-cli/src/message/{inbox,service}.rs`, `crates/awiki-cli/tests/msg_ws_inbox_live_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test msg_ws_inbox_live_contract --locked`; `cargo +1.79.0 check -p awiki-cli --locked`; Go reference selectors include `TestWSProxyTransportCallsLocalBridgeAndDecodesResponses`, `TestWSProxyTransportWrapsBridgeFailures`, `TestAllInboxMergesLocalMailNotifications`, `TestReadInboxFromCacheExcludesMailNotificationsForDirectInbox`, and `TestReadUnifiedDirectInboxFromCacheIncludesNewStyleMailMetadataRows` | Extracts ordinary direct `Service.Inbox` into a dedicated Rust `message::inbox` module to keep `service.rs` below the 1200-line default cap. The direct/non-`all` slice preserves default limit/scope normalization, active identity gating before direct transport, direct DID/handle target resolution, handle-resolution-failure fallback to `local_handle_history_cache`, WebSocket bridge method `inbox.get`, source defaulting to `local_ws_cache` in websocket mode, local direct inbox cache fallback before HTTP, secure wire-row suppression for cache fallback, `local_ws_cache_fallback` summary/warning/source, signed HTTP `/im/rpc inbox.get` fallback with trace fallback `websocket_to_http` and visible WebSocket HTTP fallback warning, Go's inbox double-failure behavior returning the HTTP-side error, inbox persistence, direct E2EE display filtering, handle-history cache merge, direct inbox filtering, and `--mark-read` returned-message mutation after successful remote rows. Scopes `all` and `group` are tracked in adjacent rows because Go gives them separate local-cache semantics. No dependency was added. |
| `awiki-cli/internal/message/service.go` `allInbox`, `readUnifiedDirectInboxFromCache`, `readAllLocalMailNotifications`; `awiki-cli/internal/message/group_service.go` `readAllLocalGroupInbox`; `awiki-cli/internal/store/query.go` group/mail inbox helpers | default `msg inbox` / `scope=all` production local-cache merge for unified direct, group, and mail notification inbox rows in `runtime.mode=websocket`, plus fallback direct/mail merge and mark-read result mutation | `crates/awiki-cli/src/message/inbox.rs`, `crates/awiki-cli/src/store/{messages,groups}.rs`, `crates/awiki-cli/tests/msg_all_inbox_live_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test msg_all_inbox_live_contract --locked`; `cargo +1.79.0 check -p awiki-cli --locked`; Go `go test ./internal/message -run 'TestAllInboxMergesLocalMailNotifications|TestReadInboxFromCacheExcludesMailNotificationsForDirectInbox|TestReadUnifiedDirectInboxFromCacheIncludesNewStyleMailMetadataRows|TestNormalizeMailNotificationMessageRecognizesMetadataSourceKind' -count=1`; Go `go test ./internal/store -run 'TestListGroupInboxMessages|TestListNotificationInboxMessages|TestListDirectMessagesByPeerDIDsFiltersUnreadInboxOnlyAndDeduplicates|TestMessageQueryHelpersLookupAndMarkReadRespectOwner' -count=1`; Go `go test ./internal/cli -run 'TestMsgDryRunPlansRenderStableContracts' -count=1` | Translates Go `Service.allInbox` for default empty scope and explicit `scope=all`: local group inbox cache is read first with warning-on-error; websocket mode reads unified direct inbox cache with local mail notifications included and does not call bridge/HTTP if that cache read succeeds; non-websocket or direct-cache-failure path recursively calls direct inbox, reads local mail notifications, merges direct/mail/group rows, and emits source `remote_http+local_group_cache+local_mail_cache`; websocket local-cache success emits source `local_direct_cache+local_group_cache`. Mail notification rows are recognized by `content_type='mail.notification'` or metadata `source_kind=mail`, normalized to Go's Chinese title/content shape, and sorted with group/direct rows by `sent_at` falling back to `stored_at` before limit truncation. `--unread` filters all local sources, and `--mark-read` calls Go-equivalent `MarkRead`, sends only direct IDs remotely, locally marks direct/group/mail rows when mark-read succeeds, and marks returned rows read regardless of mark-read errors like Go. Explicit Go `scope=group` is tracked in the adjacent group inbox row. No dependency was added; `service.rs` is 979 lines, `inbox.rs` is 695 lines, `store/messages.rs` is 390 lines, `store/groups.rs` is 569 lines, and the focused test file is 578 lines, all below the default 1200-line cap for this slice. |
| `awiki-cli/internal/message/service.go` `groupInbox`; `awiki-cli/internal/message/group_service.go` `readGroupInboxFromCache`; `awiki-cli/internal/store/query.go` `ListGroupInboxMessages` | explicit `msg inbox --scope group` local group inbox cache read, optional group filter, unread filter, and mark-read local mutation | `crates/awiki-cli/src/message/inbox.rs`, `crates/awiki-cli/tests/msg_all_inbox_live_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test msg_all_inbox_live_contract --locked`; `cargo +1.79.0 check -p awiki-cli --locked`; Go `go test ./internal/message -run 'Test.*Inbox|TestMergeInboxMessagesSortsAndLimits' -count=1`; Go `go test ./internal/store -run 'TestListGroupInboxMessages|TestMessageQueryHelpersLookupAndMarkReadRespectOwner' -count=1`; Go `go test ./internal/cli -run 'TestMsgDryRunPlansRenderStableContracts' -count=1` | Translates Go `Service.groupInbox` as a local-cache-only path: after default limit/scope normalization and active identity gating, explicit `scope=group` reads `ListGroupInboxMessages` through `groupStorageKey(request.Group)`, returns source `local_group_cache`, includes `group: request.Group`, and emits summary `Loaded N group inbox messages`. Empty `request.Group` returns all local group inbox rows like Go because the storage key is blank; nonempty `request.Group` filters by `group_did` or `group_id`. `--unread` is delegated to the store helper. `--mark-read` collects returned IDs, calls `MarkRead` best-effort only when at least one ID exists, marks all returned rows read when IDs existed, and relies on the existing local group-row mark-read classification to mutate SQLite without sending group IDs remotely. Go CLI parity is preserved for the subtle route boundary: `--group <did>` without `--scope group` still defaults to `scope=all` and does not implicitly route to `groupInbox`. No dependency was added; `inbox.rs` is 761 lines and `msg_all_inbox_live_contract.rs` is 859 lines after this slice, both below the default 1200-line cap. |
| `awiki-cli/internal/cli/msg.go`, `awiki-cli/internal/message/{service.go,http_client.go,wire.go,contact_sync.go}`, `awiki-cli/internal/store/{dao.go,query.go}` | ordinary direct message live HTTP execution, direct contact sync, handle-history cache expansion, and direct-message trace-depth call sites: `msg send --to --text/--text-file`, `msg inbox`, `msg history --with`, `msg mark-read` | `crates/awiki-cli/src/app/msg_handlers.rs`, `crates/awiki-cli/src/message/{client,service,contact_sync,wire,types,inbox,history}.rs`, `crates/awiki-cli/src/store/{messages,contacts}.rs`, `crates/awiki-cli/tests/{msg_live_contract,store_contact_contract,traceutil_contract}.rs`, `crates/awiki-cli/tests/msg_contract.rs` | `unit_verified` | `cargo +1.79.0 check -p awiki-cli --locked`; `cargo +1.79.0 test -p awiki-cli --test store_contact_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test message_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test traceutil_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_ws_inbox_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_ws_history_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test authsdk_contract --locked`; `cargo +1.79.0 run --bin xtask --locked -- check-structure`; Go `go test ./internal/store ./internal/message ./internal/traceutil -run 'TestUpsertContactRebindsCurrentHandleAndPreservesHistory|TestListDIDsByHandleFallsBackToContactsWithoutHistoryBindings|TestListDirectMessagesByPeerDIDsFiltersUnreadInboxOnlyAndDeduplicates|TestSyncPeerHandle|TestReadHistoryFromCacheByPeerDIDsAggregatesHistoricalBindings|TestTraceRunRecordsPhasesAndFallbacks' -count=1` | Translates the Go ordinary direct-message service path through the existing authsdk session and Rustls/std HTTP client: active messaging identity gate, stored JWT seeding and refresh, handle-to-DID lookup, `/im/rpc` `direct.send`, `inbox.get`, `direct.get_history`, `inbox.mark_read`, Go-shaped result summaries/data, service error mapping, outbound/inbound SQLite cache upserts, local inbox/history filters, and local mark-read mutation. The contact-sync slice adds Go-equivalent `ResolveContactHandleByDID`, `ListDIDsByHandle`, `UpsertContact`, `ListDirectMessagesByPeerDIDs`, inbox/history peer handle sync, DID->handle lookup fallback, `wba://`/domain handle normalization, contact handle rebinding/currentness history, `resolved_dids` expansion for handle history, and local handle-history cache merging for `msg history --with <handle>`. Direct send remains Go-compatible and does not upsert contacts. Direct-message trace-depth now records the Go labels for target handle lookup, DID-to-handle contact-sync lookup, direct-send persistence, inbox/history persistence, nested post-store contact sync, direct/unified/mail/history cache reads, and handle-history peer-DID cache reads. Service profile timeouts flow through the shared Rustls HTTP request timeout cap, including message `1401` retry refresh as `AuthRefresh`; shared RPC/JWT trace phases cover service calls, transport-level `message_service_retry`, and service-level `message_fallback_refresh` for direct send, inbox, history, and mark-read fallback guards. This slice intentionally excludes secure direct E2EE, WebSocket/local bridge/runtime listener transport, OpenClaw host notify, runtime listener incoming contact sync, and full remote `awiki-system-test` acceptance. Direct attachment send/download is covered by the dedicated attachment live HTTP row below, non-E2EE group lifecycle/messages are covered by the dedicated group live row below, and group plus attachment local DB trace-depth labels are covered by the traceutil row. No dependency was added; it reuses existing Rustls, authsdk, local ANP proof, local traceutil, and approved `rusqlite + bundled`. |
| `awiki-cli/internal/message/secure_control.go` | secure control payload/plaintext helpers, queued outbox local enqueue/current-session helpers, and `FlushQueuedSecureOutbox` queued-row loop planning subset | `crates/awiki-cli/src/message/{secure_control,secure_outbox_flush}.rs`, `crates/awiki-cli/src/message/mod.rs`, `crates/awiki-cli/tests/message_secure_outbox_flush_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test message_secure_outbox_flush_contract --locked`; Go source parity plus `go test ./internal/message ./internal/runtime/listener -run 'TestServiceSendSecureDirectUsesP5KeyServiceTargetAndPersistsPendingSession|TestServiceSendSecureDirectQueuesFollowUpWhilePendingConfirmation|TestHandleNotificationDecryptsSecureDirectIncomingAndStoresPlaintext|TestDeliverLocalSecureAckInProcessPromotesPendingInitiatorSession|TestFlushQueuedSecureOutboxSendsCipherAfterConfirmation' -count=1` | Translates the secure-control helper boundary for `BuildSecureAckPayload`, `BuildSecureInitPayload`, `IsSecureAckPlaintext`, `IsSecureInitPlaintext`, `secureAckSessionID`, and `isPendingConfirmationError`, including trimmed ack payload IDs, manual-init payload shape, exact `application/json` content-type checks, Go `mapFromAny` object-or-JSON-string payload parsing, string-only field extraction, ack/init system type checks, and case-insensitive pending-confirmation substring detection. Adds the local `currentSecureSessionID` and `queueSecureOutboxRecord` helper boundary: nil-equivalent manager/record handling, identity path lookup, `p5-e2ee-sessions` file-store lookup, trimmed returned session ID, missing/error-to-empty behavior, identity-record-required error, store open/schema, `QueueE2EEOutbox` insert with current session ID, blank `original_type` defaulting to text, queued local status, credential name, plaintext, raw peer DID, and `{"reason":"pending_confirmation"}` metadata. Also translates the deterministic row-loop boundary of Go `FlushQueuedSecureOutbox` after queued rows, current secure session IDs, send outcomes, mark-sent outcomes, and store-message outcomes are injected. Preserves stable `created_at` sort, peer-filter trimming with exact row comparison, silent malformed-row skips, text vs JSON send action selection, invalid JSON drop failure, unsupported type drop failure, send retry failure, success metadata shape, returned message-id fallback, mark-sent warning/skip-store behavior, store-message warning behavior, outgoing E2EE `MessageRecord` shape, Go's current `json` content type fallback to `text/plain`, direct thread ID construction, and compact warning trimming/deduplication. Later secure retry, secure init, and secure repair rows add production command execution through the verified E2EE sender; this row still excludes WebSocket RPC and foreground listener execution. No dependency was added beyond existing approved `rusqlite + bundled` and the existing local ANP facade. Files stay under the default 1200-line review-size cap. |
| `awiki-cli/internal/cli/msg.go`, `awiki-cli/internal/message/{service.go,attachment.go,attachment_wire.go,service_discovery.go,http_client.go,group_service.go}` | direct and group attachment live HTTP execution: `msg send --to/--group --file`, `msg attachment download --with/--group` | `crates/awiki-cli/src/app/msg_handlers.rs`, `crates/awiki-cli/src/message/{attachment_service,attachment,service,group_service,client,types}.rs`, `crates/awiki-cli/tests/attachment_live_contract.rs` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test attachment_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --locked`; `cargo +1.79.0 run --bin xtask --locked -- check-structure`; focused remote `awiki-system-test` selector `tests_v2/cli/test_awiki_cli_group_local.py::test_awiki_cli_can_send_and_download_group_attachments` passed against `awiki.info` | Implements direct/group attachment upload and download on the existing authsdk session plus Rustls/std HTTP client: create slot, upload object with HTTP `PUT`, commit object, send attachment manifests through `direct.send` or `group.send`, select attachments from direct/group history pages, request download tickets, download object bytes, write the requested output, and update local direct/group SQLite caches where applicable. Attachment send/download deliberately use HTTP transport even when `runtime.mode` is `websocket` and emit websocket-mode warnings; both non-dry-run warning strings are covered by focused fake-server tests. JSON-RPC `1401` refresh/retry, attachment service DID resolution, Go-shaped service error mapping, Rustls close-delimited response compatibility, shared profile timeout caps, shared RPC/JWT trace phases, and attachment local DB/handle trace-depth call sites are covered by local focused tests and the remote group attachment system selector. No dependency was added; dependency policy remains pure-Rust/cross-platform where possible, TLS Rustls-first, and SQLite on the approved `rusqlite + bundled` path. WebSocket/local bridge attachment transport, secure direct E2EE, group E2EE/MLS, and optimization/refactor work remain deferred until after 1:1 parity evidence. |
| `awiki-cli/internal/message/{types.go,wire.go,attachment.go,attachment_wire.go,service_discovery.go,warnings.go}` | pure message request foundation: inbox/history/mark-read params, direct text payload shape, attachment control params/manifest/selection, attachment service discovery, WebSocket fallback warnings | `crates/awiki-cli/src/message/{types,wire,attachment,service_discovery,warnings}.rs`, `crates/awiki-cli/src/lib.rs` | `unit_verified` | `crates/awiki-cli/tests/message_contract.rs` | Translates pure/local Go message helpers needed before real service execution. This foundation slice intentionally excludes `authsdk` session refresh, HTTP transport, WebSocket proxy transport, cache mutation, and secure direct E2EE; direct/group attachment HTTP execution is covered by the dedicated live attachment row. Group RPC builders and non-E2EE live execution are covered by group-specific rows. No dependency was added; Rustls HTTP/WS selection remains deferred for remaining WebSocket work. |
| `awiki-cli/internal/message/{auth.go,proof.go}` | local private-key loading, DID-document verification-method selection, and RFC9421 origin-proof auth value for signed direct payloads | `crates/awiki-cli/src/message/proof.rs` | `unit_verified` | `crates/awiki-cli/tests/message_contract.rs` | Translates the local proof generation boundary without authsdk session refresh or service execution. Uses the existing local ANP Rust SDK proof API and key parser, preserves Go's first `authentication` fallback to first `verificationMethod.id`, emits `anp-rfc9421-origin-proof-v1` auth shape, and verifies `contentDigest`, `signatureInput`, `signature`, canonical digest, and DID-document verification. No HTTP/TLS or OpenSSL/native-tls dependency was added. |
| `awiki-cli/internal/message/{wire.go,attachment_wire.go}` | signed direct send and signed direct/group attachment manifest request params | `crates/awiki-cli/src/message/{wire,attachment,proof}.rs` | `unit_verified` | `crates/awiki-cli/tests/message_contract.rs` | Wires the verified local origin-proof helper into Go-equivalent `BuildDirectSendRPCParams`, `BuildDirectAttachmentSendRPCParams`, and `BuildGroupAttachmentSendRPCParams` shapes. Preserves auth scheme, absence of `sender_proof`, direct/group target metadata, attachment manifest content type, body payload shape, and proof verification. Attachment manifest HTTP execution is covered by the dedicated live attachment row; this wire row itself adds no WebSocket execution, authsdk session refresh, cache mutation, or TLS dependency. |
| `awiki-cli/internal/message/{types.go,group_wire.go}` | non-E2EE group base/local RPC request builders | `crates/awiki-cli/src/message/{types,group_wire,proof}.rs` | `system_verified` | `crates/awiki-cli/tests/message_group_wire_contract.rs`; full `cargo +1.79.0 test -p awiki-cli --locked`; accepted `awiki-system-test` selector set | Translates Go request params for `BuildGroupCreateRPCParams`, `BuildGroupGetInfoRPCParams`, `BuildGroupJoinRPCParams`, `BuildGroupAddRPCParams`, `BuildGroupRemoveRPCParams`, `BuildGroupLeaveRPCParams`, `BuildGroupUpdateProfileRPCParams`, `BuildGroupUpdatePolicyRPCParams`, `BuildGroupSendRPCParams`, and local `BuildGroupGet/List/Members/MessagesRPCParams`. Preserves origin-proof auth shape, service/group targets, group policy defaults and permissions, E2EE policy profile override, group local profile metadata without generated operation fields, default limits, trim/optional-field behavior, and Go validation messages. This is still request construction only: no HTTP/WebSocket execution, authsdk session refresh, cache mutation, non-dry-run lifecycle orchestration, or MLS provider execution was added. |
| `awiki-cli/internal/message/{types.go,group_wire.go}` | group E2EE RPC request builders and sanitizers | `crates/awiki-cli/src/message/{types,group_e2ee_wire,group_wire,proof}.rs` | `system_verified` | `crates/awiki-cli/tests/message_group_e2ee_wire_contract.rs`; full `cargo +1.79.0 test -p awiki-cli --locked`; accepted `awiki-system-test` selector set | Translates Go request params for `BuildGroupE2EECreateRPCParams`, `Add`, `Remove`, `LeaveRequest`, `Leave`, `Send`, `PublishKeyPackage`, `GetKeyPackage`, `GetRecoveryKeyPackage`, `GetUpdateKeyPackage`, `RecoverMember`, `UpdateMember`, `Notice`, and `Head`. Preserves E2EE/transport security-profile split, caller-provided operation/message ids, cipher and KeyPackage sanitization, `group_state_ref` augmentation, recovery/update target-object body shape, default/capped notice limits, and origin-proof verification. This remains request construction only: no ANP MLS provider execution, hidden P6 service calls, transport, JWT refresh, or cache mutation was added. |
| `awiki-cli/internal/cli/group.go` | non-E2EE group dry-run lifecycle: `create/get(show)/join/add/remove(kick)/leave/update/list/members/messages` | `crates/awiki-cli/src/app/group_handlers.rs`, `crates/awiki-cli/src/cli/mod.rs`, `crates/awiki-cli/src/cmdmeta/mod.rs` | `system_verified` | `crates/awiki-cli/tests/group_contract.rs`; `tests_v2/cli/test_awiki_cli_group_local.py::test_awiki_cli_group_commands_support_dry_run_for_extended_policy_fields` | Translates Go dry-run plan shapes for the non-E2EE group command surface, including canonical alias behavior (`show` -> `group.get`, `kick` -> `group.remove`), PascalCase request fields, `member_handle` completion through the shared identity-module bare-handle helper, default list limits, `Skip: 0` in message list plans, default create values, and explicit bool/int pointer behavior as JSON null vs value. Non-dry-run non-E2EE HTTP execution is covered by the dedicated live row; group attachment send/download is covered by the attachment live HTTP row; E2EE/MLS lifecycle remains deferred. |
| `awiki-cli/internal/cli/group.go`, `awiki-cli/internal/message/{group_service.go,group_wire.go,service.go}`, `awiki-cli/internal/store/{dao.go,query.go}` | non-E2EE group live HTTP execution: `group create/get(show)/join/add/remove(kick)/leave/update/list/members/messages` and `msg send --group --text` | `crates/awiki-cli/src/app/group_handlers.rs`, `crates/awiki-cli/src/app/msg_handlers.rs`, `crates/awiki-cli/src/message/{group_service,group_wire,service,client}.rs`, `crates/awiki-cli/src/store/groups.rs`, `crates/awiki-cli/tests/{group_live_contract,store_groups_contract,message_group_wire_contract}.rs` | `system_verified` | `cargo +1.79.0 fmt --check`; `cargo +1.79.0 check -p awiki-cli --locked`; `cargo +1.79.0 test -p awiki-cli --test group_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test store_groups_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test message_group_wire_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test msg_live_contract --locked`; `cargo +1.79.0 run --bin xtask --locked -- check-structure`; focused remote `awiki-system-test` group selectors against `awiki.info` | Translates Go non-E2EE group service execution through the existing authsdk session and Rustls/std HTTP client: active messaging identity gate, stored JWT seeding and refresh, handle/DID member resolution, `/im/rpc` `group.create/get/join/add/remove/leave/update_profile/update_policy/list/list_members/list_messages/send`, Go-shaped summaries/data/error mapping, group/member/message SQLite cache writes, owner leave guard, `show`/`kick` aliases, and group text message id mapping. Message signed timestamps are emitted as Go-compatible second-precision RFC3339 to match message-service typed-meta origin-proof verification. Group attachment send/download is covered by the dedicated attachment live HTTP row; shared profile timeout caps, shared RPC/JWT trace phases, and group local DB trace-depth labels now apply through the message client and traceutil row. This group lifecycle slice still excludes group E2EE/MLS execution, WebSocket/local bridge/runtime listener fallback, OpenClaw host notify, and remaining handle/fallback trace wiring. No dependency was added. |
| `awiki-cli/internal/message/group_service.go`, `awiki-cli/internal/message/group_e2ee_service.go`, `awiki-cli/internal/message/group_e2ee_provider.go`, `awiki-cli/internal/message/http_client.go` | live `group create --e2ee` hidden MLS bootstrap and `group.e2ee.create` delivery | `crates/awiki-cli/src/message/{group_create,group_e2ee_create,group_e2ee_provider,group_e2ee_transport,group_e2ee_wire,group_service}.rs`, `crates/awiki-cli/tests/group_e2ee_create_contract.rs`; `tests_v2/cli/test_awiki_cli_group_e2ee_*.py` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test group_e2ee_create_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test group_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test group_e2ee_publish_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test message_group_e2ee_wire_contract --locked`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 AWIKI_GROUP_E2EE_CONTRACT_TEST=1 PYTHONDONTWRITEBYTECODE=1 uv run pytest tests_v2/cli/test_awiki_cli_group_e2ee_local.py tests_v2/cli/test_awiki_cli_group_e2ee_lifecycle_local.py tests_v2/cli/test_awiki_cli_group_e2ee_recovery_local.py tests_v2/cli/test_awiki_cli_group_e2ee_update_rejoin_local.py tests_v2/cli/test_awiki_cli_group_e2ee_negative_local.py -ra -q`; Go `go test ./internal/message -run 'TestBuildGroupE2EECreateRPCParams|TestBuildGroupE2EEPublishKeyPackageRPCParamsStripsProviderOnlyFields|TestMLSExecProviderCommands|TestHTTPTransportGroupMethods' -count=1`; Go `go test ./internal/cli -run 'TestGroupDryRunPlansRenderStableContracts' -count=1` | Translates the Go `CreateGroup` E2EE branch after normal P4 `group.create`: active identity and base group create remain shared with the group live row, then `group_request_uses_e2ee` detects `--e2ee` or `message_security_profile=group-e2ee`, invokes `anp-mls group create --json-in -`, attaches a cached `group_state_ref`, signs hidden `group.e2ee.create`, and persists Go-shaped group E2EE summary metadata into SQLite. The focused system selectors prove real Rust subprocess `group create --message-security-profile group-e2ee` bootstraps a usable MLS group in the Alice/Bob happy path and in recovery/update/rejoin setup flows, with no contract-test markers and no private MLS state in the business SQLite DB. Contract tests still cover provider/delivery warning downgrade shapes, dry-run/schema detail, and request builders. This scoped system row still excludes WebSocket/local bridge group E2EE transport, foreground listener group E2EE handling, mail, and full repository-wide acceptance. No dependency was added; source and test files remain below the default 1200-line cap. |
| `awiki-cli/internal/message/group_service.go`, `awiki-cli/internal/message/group_e2ee_service.go`, `awiki-cli/internal/message/group_e2ee_provider.go`, `awiki-cli/internal/message/http_client.go`, `awiki-cli/internal/cli/group_e2ee.go` | live `group add --e2ee` hidden MLS add-member flow and scoped rejoin-through-readd behavior | `crates/awiki-cli/src/message/{group_service,group_e2ee_add,group_e2ee_create,group_e2ee_provider,group_e2ee_transport,group_e2ee_wire}.rs`, `crates/awiki-cli/src/app/group_e2ee_handlers.rs`, `crates/awiki-cli/tests/group_e2ee_add_contract.rs`; `tests_v2/cli/test_awiki_cli_group_e2ee_*.py` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test group_e2ee_add_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test group_e2ee_create_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test group_e2ee_publish_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test group_e2ee_status_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test group_e2ee_pending_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test group_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test group_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test message_group_e2ee_wire_contract --locked`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 AWIKI_GROUP_E2EE_CONTRACT_TEST=1 PYTHONDONTWRITEBYTECODE=1 uv run pytest tests_v2/cli/test_awiki_cli_group_e2ee_local.py tests_v2/cli/test_awiki_cli_group_e2ee_lifecycle_local.py tests_v2/cli/test_awiki_cli_group_e2ee_recovery_local.py tests_v2/cli/test_awiki_cli_group_e2ee_update_rejoin_local.py tests_v2/cli/test_awiki_cli_group_e2ee_negative_local.py -ra -q`; Go `go test ./internal/message -run 'TestAddGroupMemberE2EEUsesOnlyServiceLeasedKeyPackageForMLS|TestLocalIdentityByDIDFindsStoredMemberForWelcomeProcessing|TestGroupE2EEWelcomeDeviceIDUsesPublicKeyPackageDevice|TestMLSExecProviderCommands|TestHTTPTransportGroupMethods' -count=1`; Go `go test ./internal/cli -run 'TestGroupDryRunPlansRenderStableContracts' -count=1` | Translates the Go `AddGroupMember` E2EE branch after normal P4 `group.add`: pre-mutation snapshot, P4 add/sync, hidden KeyPackage lease, external `anp-mls group add-member --json-in -`, hidden `group.e2ee.add`, summary metadata persistence, and local welcome processing for local identities. The focused system selectors prove live owner add into an E2EE group, Bob welcome repair/decryptability, and removed-member rejoin through a fresh owner `group add --e2ee`/normal re-add path with post-rejoin plaintext visibility and removed-member denial before re-add. Contract tests still cover warning downgrade branches, hidden wrapper result shape, dry-run/schema detail, and local welcome edge cases. This scoped system row does not claim a separately invoked hidden `group e2ee rejoin` command beyond the documented rejoin-through-owner-readd behavior, nor WebSocket/local bridge, foreground listener, mail, or full repository-wide acceptance. No dependency was added; all changed Rust source/test files remain below the default 1200-line cap. |
| `awiki-cli/internal/message/group_service.go`, `awiki-cli/internal/message/group_e2ee_service.go`, `awiki-cli/internal/message/group_e2ee_provider.go`, `awiki-cli/internal/message/http_client.go`, `awiki-cli/internal/cli/group_e2ee.go` | live `group remove --e2ee`, live `group leave --e2ee`, and hidden `group e2ee process-leave-request` | `crates/awiki-cli/src/message/{group_service,group_e2ee_remove,group_e2ee_provider,group_e2ee_transport,group_e2ee_wire}.rs`, `crates/awiki-cli/src/app/group_e2ee_handlers.rs`, `crates/awiki-cli/tests/group_e2ee_remove_leave_contract.rs` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test group_e2ee_remove_leave_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test group_e2ee_add_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test group_e2ee_repair_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test message_group_e2ee_wire_contract --locked`; Go `go test ./internal/message -run 'TestLeaveGroupRejectsActiveOwnerFromCachedSnapshot|TestLeaveGroupE2EECreatesLeaveRequestWithoutLocalMLSLeave|TestUnsupportedGroupE2EESelfLeaveReasonDetectsNonAdvancingEpoch|TestGroupMemberMutationUsesPreMutationE2EESnapshot|TestShouldAbortGroupE2EEPendingCommitOnlyForDeterministicServiceRejection|TestPendingCommitTerminalParamsDoNotReusePrepareOperationID|TestBuildGroupE2EERemoveRPCParams|TestHTTPTransportGroupMethods|TestMLSExecProviderCommands' -count=1`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 AWIKI_GROUP_E2EE_CONTRACT_TEST=1 PYTHONDONTWRITEBYTECODE=1 uv run pytest tests_v2/cli/test_awiki_cli_group_e2ee_lifecycle_local.py -ra -q` | Translates Go's E2EE removal and leave-request flows. `group remove --e2ee` records the pre-mutation snapshot, resolves the member DID, bypasses normal P4 `group.remove`, prepares `anp-mls group remove-member --json-in -`, submits hidden `group.e2ee.remove`, finalizes the pending commit with `group commit-finalize`, persists summary metadata, syncs `group.get`/`group.list_members`, and returns Go service data while the public CLI preserves the Go summary override `Removed member from group`. `group leave --e2ee` rejects cached active owners, does not run local MLS leave or P4 `group.leave`, and creates only hidden `group.e2ee.leave_request` with `transport-protected` security plus owner-processing guidance. Hidden live `group e2ee process-leave-request` defaults the reason to `leave request processed by owner`, delegates to the same epoch-advancing E2EE remove path with trimmed `leave_request_id`, syncs group state, and inserts the Go plan into live result data. The focused lifecycle system-test file proves owner remove revokes Bob, same-epoch self-leave stays fail-closed, PR-B1 leave-request owner processing revokes Bob and repairs remaining members, removed members cannot read/send post-remove plaintext, and tested storage boundaries keep application plaintext/private MLS material out. Pending finalize/abort params intentionally do not reuse the prepare `operation_id`; deterministic service rejection abort policy matches Go's HTTP 4xx/RPC >= 2000 rule. Rust's current `Result` error model cannot return Go-style side-channel warnings/data on failed submit/abort paths, so this slice still records failed-submit/abort enrichment as a future Rust API-shape decision. Recover-member, update-key, rejoin, WebSocket/local bridge group E2EE transport, foreground listener group E2EE handling, and full repository-wide system acceptance remain separate rows. No dependency was added; all changed Rust source/test files remain below the default 1200-line cap. |
| `awiki-cli/internal/message/group_e2ee_service.go`, `awiki-cli/internal/message/group_e2ee_provider.go`, `awiki-cli/internal/message/http_client.go`, `awiki-cli/internal/cli/group_e2ee.go` | live `group e2ee recover-member` active-member crypto recovery | `crates/awiki-cli/src/message/{group_e2ee_recover,group_e2ee_add,group_e2ee_remove,group_e2ee_provider,group_e2ee_transport,group_e2ee_wire}.rs`, `crates/awiki-cli/src/app/group_e2ee_handlers.rs`, `crates/awiki-cli/tests/group_e2ee_recover_member_contract.rs`; `tests_v2/cli/test_awiki_cli_group_e2ee_recovery_local.py` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test group_e2ee_recover_member_contract --locked`; adjacent group-E2EE/group/wire tests; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 AWIKI_GROUP_E2EE_CONTRACT_TEST=1 PYTHONDONTWRITEBYTECODE=1 uv run pytest tests_v2/cli/test_awiki_cli_group_e2ee_local.py tests_v2/cli/test_awiki_cli_group_e2ee_lifecycle_local.py tests_v2/cli/test_awiki_cli_group_e2ee_recovery_local.py tests_v2/cli/test_awiki_cli_group_e2ee_update_rejoin_local.py tests_v2/cli/test_awiki_cli_group_e2ee_negative_local.py -ra -q`; Go `go test ./internal/message -run 'TestRecoverGroupE2EEMemberUsesRecoverMemberWithoutGroupAdd|TestMLSExecProviderCommands|TestHTTPTransportGroupMethods' -count=1`; Go `go test ./internal/cli -run 'TestGroupDryRunPlansRenderStableContracts' -count=1` | Translates Go `RecoverGroupE2EEMember`: active identity and target DID resolution, default `device_id=default`, hidden `group.e2ee.head` preflight with `actor_recovery_eligible`, recovery KeyPackage leasing through hidden `group.e2ee.get_key_package`, external `anp-mls group recover-member-prepare --json-in -`, hidden `group.e2ee.recover_member` delivery without P4 membership fields, generic finalize, local E2EE summary persistence, optional welcome processing, and Go live plan insertion. The focused recovery system selectors prove same-device active-member state replacement without P4 `group.add`, unchanged P4 membership fingerprint, post-recovery send/decrypt continuity, and removed-member recovery rejection. This scoped system row still excludes update-key, exhaustive repair/finalize failure edges, WebSocket/local bridge group E2EE transport, foreground listener group E2EE handling, mail, and full repository-wide acceptance. No dependency was added; all changed Rust source/test files remain below the default 1200-line cap. |
| `awiki-cli/internal/message/group_e2ee_service.go`, `awiki-cli/internal/message/group_e2ee_provider.go`, `awiki-cli/internal/message/http_client.go`, `awiki-cli/internal/cli/group_e2ee.go` | live `group e2ee update-key` active-member key rotation | `crates/awiki-cli/src/message/{group_e2ee_update,group_e2ee_add,group_e2ee_remove,group_e2ee_provider,group_e2ee_transport,group_e2ee_wire}.rs`, `crates/awiki-cli/src/app/group_e2ee_handlers.rs`, `crates/awiki-cli/tests/group_e2ee_update_key_contract.rs`; `tests_v2/cli/test_awiki_cli_group_e2ee_update_rejoin_local.py` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test group_e2ee_update_key_contract --locked`; adjacent group-E2EE/group/wire tests; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 AWIKI_GROUP_E2EE_CONTRACT_TEST=1 PYTHONDONTWRITEBYTECODE=1 uv run pytest tests_v2/cli/test_awiki_cli_group_e2ee_local.py tests_v2/cli/test_awiki_cli_group_e2ee_lifecycle_local.py tests_v2/cli/test_awiki_cli_group_e2ee_recovery_local.py tests_v2/cli/test_awiki_cli_group_e2ee_update_rejoin_local.py tests_v2/cli/test_awiki_cli_group_e2ee_negative_local.py -ra -q`; Go `go test ./internal/message -run 'TestUpdateGroupE2EEKeyUsesUpdateMethodWithoutGroupAdd|TestMLSExecProviderCommands|TestHTTPTransportGroupMethods' -count=1`; Go `go test ./internal/cli -run 'TestGroupDryRunPlansRenderStableContracts' -count=1` | Translates Go `UpdateGroupE2EEKey`: active identity and target DID resolution, default `device_id=default`, hidden head preflight, update KeyPackage leasing with `purpose=update`, external `anp-mls group update-member-prepare --json-in -`, hidden `group.e2ee.update` delivery without P4 membership fields, update-specific finalize/abort, local summary persistence, optional local welcome processing, and Go live plan insertion. The focused update/rejoin system selector proves owner-only update-key rotation, absence of non-goal recover-member behavior, Bob repair after update, and post-update encrypted send/decrypt continuity. Contract tests still cover controller-ineligible checks, request shape, and warning/failure branches. This scoped system row still excludes exhaustive abort/finalize failure side effects, WebSocket/local bridge group E2EE transport, foreground listener group E2EE handling, mail, and full repository-wide acceptance. No dependency was added; all changed Rust source/test files remain below the default 1200-line cap. |
| `awiki-cli/internal/message/group_e2ee_service.go`, `awiki-cli/internal/message/group_e2ee_provider.go`, `awiki-cli/internal/message/http_client.go`, `awiki-cli/internal/cli/group_e2ee.go` | live `group e2ee repair` pending notice replay and local recovery diagnosis | `crates/awiki-cli/src/message/{group_e2ee_repair,group_e2ee_provider,group_e2ee_transport,group_e2ee_status,group_e2ee_create,group_e2ee_remove}.rs`, `crates/awiki-cli/src/app/group_e2ee_handlers.rs`, `crates/awiki-cli/tests/group_e2ee_repair_contract.rs`; `tests_v2/cli/test_awiki_cli_group_e2ee_*.py` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test group_e2ee_repair_contract --locked`; adjacent group-E2EE/group/wire tests; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 AWIKI_GROUP_E2EE_CONTRACT_TEST=1 PYTHONDONTWRITEBYTECODE=1 uv run pytest tests_v2/cli/test_awiki_cli_group_e2ee_local.py tests_v2/cli/test_awiki_cli_group_e2ee_lifecycle_local.py tests_v2/cli/test_awiki_cli_group_e2ee_recovery_local.py tests_v2/cli/test_awiki_cli_group_e2ee_update_rejoin_local.py tests_v2/cli/test_awiki_cli_group_e2ee_negative_local.py -ra -q`; Go `go test ./internal/message -run 'TestInspectGroupE2EEStatusComparesLocalEpochToServiceHead|TestGroupE2EEStatusForRecoveryScansNonDefaultDevice' -count=1`; Go `go test ./internal/cli -run 'TestGroupDryRunPlansRenderStableContracts' -count=1` | Translates Go `RepairGroupE2EENotices` and CLI repair wiring: hidden head preflight warning, accepted local pending-commit finalization, hidden notice pull/mark-delivered, commit replay, welcome/recovery/update welcome replay, already-applied/restored fallbacks, final local status scan, recovery diagnosis/artifact reuse, Go live plan insertion, and `Replayed group E2EE pending notices` result shape. The focused system selectors prove Bob welcome repair in the Alice/Bob loop, idempotent second repair with no remaining welcome notices, stale pending-notice diagnosis and repair after a missed add commit, post-recovery repair, post-update repair, post-rejoin repair, and remaining-member repair after leave-request processing. Contract tests still cover narrower request/result and failure edges. This scoped system row still excludes exhaustive notice-type/finalize/abort failure coverage, WebSocket/local bridge group E2EE transport, foreground listener group E2EE handling, mail, and full repository-wide acceptance. No dependency was added; all changed Rust source/test files remain below the default 1200-line cap. |
| `awiki-cli/internal/message/group_service.go`, `awiki-cli/internal/message/group_e2ee_service.go`, `awiki-cli/internal/message/group_e2ee_provider.go`, `awiki-cli/internal/message/http_client.go` | live group E2EE outbound text send via `msg send --group --secure on --text` and cached/local E2EE auto-upgrade | `crates/awiki-cli/src/message/{group_service,group_e2ee_send,group_e2ee_provider,group_e2ee_transport,group_e2ee_wire}.rs`, `crates/awiki-cli/tests/group_e2ee_send_contract.rs` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test group_e2ee_send_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test group_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test message_group_e2ee_wire_contract --locked`; Go `go test ./internal/message -run 'TestBuildGroupE2EESendRPCParamsSendsOnlyOpaqueCipherObject|TestMLSExecProviderCommands|TestHTTPTransportGroupMethodsUseExpectedRPCMethods' -count=1`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 AWIKI_GROUP_E2EE_CONTRACT_TEST=1 PYTHONDONTWRITEBYTECODE=1 uv run pytest tests_v2/cli/test_awiki_cli_group_e2ee_local.py -ra -q` | Translates Go `sendGroup` routing and `sendGroupE2EE` outbound text path: explicit `--secure on` requires a cached group snapshot using `group-e2ee`, ordinary group text send auto-upgrades when the cached snapshot uses group E2EE or local MLS status is active/pending/has a crypto group id, state sync refreshes `group.get`, provider status selects a recovery-capable device with default `default`, `anp-mls message encrypt --json-in -` receives Go-shaped plaintext/cipher metadata, hidden `group.e2ee.send` posts only the sanitized opaque cipher object through the existing signed Rustls/std transport, service-omitted `group_did`/`message_id`/`operation_id` are backfilled, the local SQLite message row is stored as E2EE, and output is decorated with `message.secure=true`, `message.security_profile=group-e2ee`, `data.e2ee.encrypted=true`, `group_state_ref`, `cipher_object_sent=true`, and summary `Sent a group text message with group E2EE`. The focused Alice/Bob system loop now proves real MLS encrypted send through the Rust CLI subprocess with no resident `anp-mls` process and no private MLS state in the business SQLite DB. The service epoch-mismatch repair/retry path is translated using the existing repair helper but still needs broader edge-case/system coverage. This slice still excludes WebSocket/local bridge group E2EE transport, foreground listener group E2EE handling, and full repository-wide system acceptance. No dependency was added; all changed Rust source/test files remain below the default 1200-line cap. |
| `awiki-cli/internal/message/group_service.go`, `awiki-cli/internal/message/group_e2ee_service.go`, `awiki-cli/internal/message/group_e2ee_provider.go` | HTTP `group messages` group E2EE decrypt before local cache projection | `crates/awiki-cli/src/message/{group_service,group_e2ee_decrypt,group_e2ee_provider}.rs`, `crates/awiki-cli/tests/group_e2ee_decrypt_contract.rs` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test group_e2ee_decrypt_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test group_e2ee_send_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test group_live_contract --locked`; Go `go test ./internal/message -run 'TestMLSExecProviderCommands|TestHTTPTransportGroupMethodsUseExpectedRPCMethods' -count=1`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 AWIKI_GROUP_E2EE_CONTRACT_TEST=1 PYTHONDONTWRITEBYTECODE=1 uv run pytest tests_v2/cli/test_awiki_cli_group_e2ee_local.py -ra -q` | Translates Go `maybeDecryptGroupMessages` in the HTTP `GroupMessages` path: `group.list_messages` results are decrypted before `persistGroupMessages`, cipher objects are discovered from top-level `group_cipher_object`, `content.group_cipher_object`/direct cipher content, or `body.group_cipher_object`, candidate MLS devices default to `default`, `anp-mls message decrypt --json-in -` receives Go AAD fields (`group_state_ref`, `sender_did`, cipher content type, `security_profile=group-e2ee`, `message_id`, `operation_id`), successful `application_plaintext` rewrites `content`, `content_type`, and `decrypted=true`, decrypt failures become compact Go-shaped warnings, and the decrypted message is what the SQLite cache projection returns to CLI output. The focused Alice/Bob system loop now proves Bob can read Alice's real MLS-encrypted group message through `group messages` after welcome repair and encrypted send. This slice covers the HTTP `group messages` fetch/display path; WebSocket/local bridge group message receive/decrypt, foreground listener group E2EE handling, broader decrypt edge cases, and full repository-wide system acceptance remain deferred. No dependency was added; all changed Rust source/test files remain below the default 1200-line cap. |
| `awiki-cli/internal/cli/group_e2ee.go` dry-run/schema subset | group E2EE dry-run diagnostics, hidden command metadata, and command schema surface | `crates/awiki-cli/src/app/group_e2ee_handlers.rs`, `crates/awiki-cli/src/cli/mod.rs`, `crates/awiki-cli/src/cmdmeta/mod.rs`, `crates/awiki-cli/tests/{group_contract,message_group_e2ee_wire_contract}.rs`; `tests_v2/cli/test_awiki_cli_group_local.py` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test group_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test message_group_e2ee_wire_contract --locked`; Go `go test ./internal/cli -run 'TestGroupDryRunPlansRenderStableContracts' -count=1`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_BINARY=/home/ecs-user/awiki-space/awiki-cli-rs2/target/debug/awiki-cli AWIKI_CLI_UPDATE_CACHE_ONLY=1 PYTHONDONTWRITEBYTECODE=1 uv run pytest tests_v2/cli/test_awiki_cli_group_local.py::test_awiki_cli_group_e2ee_dry_run_and_schema_commands_are_stable -ra -q` | Translates and system-verifies static dry-run/schema contracts for `status`, `publish-key-package`, `pending`, `repair`, `process-leave-request`, `recover-member`, hidden `update-key`, and hidden `rejoin`, preserving provider metadata, `mls_data_dir`, discovery-hidden guard, `--recovery` alias, hidden command metadata, publish purpose choices, and orchestration text. The system selector runs real Rust subprocesses in an isolated workspace and does not require P6 APIs, `anp-mls`, message-service, mail-service, platform service-manager, or tenant site admin configuration. This row remains dry-run/schema scoped and does not add live P6/MLS execution evidence beyond the separate live rows. |
| `awiki-cli/internal/message/{group_e2ee_service.go,group_e2ee_provider.go,http_client.go}`, `awiki-cli/internal/cli/group_e2ee.go` status/pending/publish subset | non-dry-run `group e2ee status` recovery inspection, `group e2ee pending` notice pull, and `group e2ee publish-key-package` KeyPackage publish | `crates/awiki-cli/src/app/group_e2ee_handlers.rs`, `crates/awiki-cli/src/message/{group_e2ee_provider,group_e2ee_publish,group_e2ee_status,group_e2ee_wire}.rs`, `crates/awiki-cli/tests/{group_e2ee_status_contract,group_e2ee_pending_contract,group_e2ee_publish_contract,message_group_e2ee_wire_contract}.rs`; `tests_v2/cli/test_awiki_cli_group_e2ee_*.py` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test group_e2ee_status_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test group_e2ee_pending_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test group_e2ee_publish_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test message_group_e2ee_wire_contract --locked`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 AWIKI_GROUP_E2EE_CONTRACT_TEST=1 PYTHONDONTWRITEBYTECODE=1 uv run pytest tests_v2/cli/test_awiki_cli_group_e2ee_local.py tests_v2/cli/test_awiki_cli_group_e2ee_lifecycle_local.py tests_v2/cli/test_awiki_cli_group_e2ee_recovery_local.py tests_v2/cli/test_awiki_cli_group_e2ee_update_rejoin_local.py tests_v2/cli/test_awiki_cli_group_e2ee_negative_local.py -ra -q`; Go `go test ./internal/message -run 'TestInspectGroupE2EEStatusComparesLocalEpochToServiceHead|TestGroupE2EEStatusForRecoveryScansNonDefaultDevice|TestMLSExecProviderCommands|TestHTTPTransportGroupMethods' -count=1`; Go `go test ./internal/cli -run 'TestGroupDryRunPlansRenderStableContracts' -count=1` | Covers Go-shaped non-dry-run `status`, `pending`, and `publish-key-package` execution through the existing Rustls/std transport and `anp-mls` exec provider. The focused system selectors prove live normal/recovery/update KeyPackage publishing, pending welcome/leave notices, pending-notice status diagnosis required for repair/recovery, and unsupported transition-candidate command boundaries falling through to `group e2ee pending`. Contract tests still cover status ranking/tie-breaks, provider details, request shape, and warning/error branches. This scoped system row still excludes service-head mutation outside the exercised flows, MLS cache mutation beyond provider-command parity, WebSocket/local bridge, foreground listener, mail, and full repository-wide acceptance. No dependency was added; all changed Rust source/test files remain under the default 1200-line cap. |
| `awiki-cli/internal/cli/mail.go` | mail command tree, argument validation, dry-run plans, local notification read, and non-dry-run mail RPC command wiring | `crates/awiki-cli/src/app/mail_handlers.rs`, `crates/awiki-cli/src/mail/*.rs`, `crates/awiki-cli/src/cli/mod.rs`, `crates/awiki-cli/src/cmdmeta/mod.rs` | `unit_verified` | `crates/awiki-cli/tests/mail_contract.rs`; `crates/awiki-cli/tests/mail_live_contract.rs`; `cargo +1.79.0 test -p awiki-cli --locked` | Initial mail CLI slice translated command catalog, Go validation messages, dry-run plan shapes, mail address splitting, and `mail notify` local SQLite cache reading/normalization. The later live mail slice now wires non-dry-run `mail inbox/read/mark-read/account/send/attachment download` through the shared authsdk Rustls HTTP execution layer, preserves Go service error exit-code mapping, and implements attachment base64 decode plus file write. Attachment downloads now preserve Go local filesystem modes on Unix: newly created parent directories are requested with `0700`, newly created downloaded files are requested with `0600`, and overwritten existing files are truncated while keeping existing file permissions; non-Unix keeps std filesystem behavior until host validation exists. Shared profile timeout caps and RPC/JWT trace phases now apply to live mail RPC and auth refresh, including `mail_bootstrap` without nested `business_rpc:get_me`. Full remote mail system acceptance remains later system-test work. |
| `awiki-cli/internal/mail/{types.go,helpers.go,service.go}` | mail request/result types, helper coercions, local notification normalization | `crates/awiki-cli/src/mail/{types,service}.rs`, `crates/awiki-cli/src/store/query.rs` | `unit_verified` | `crates/awiki-cli/tests/mail_contract.rs`; `cargo +1.79.0 test -p awiki-cli --locked` | Local-only service behavior is translated for `Notifications`: active identity resolution, messaging-readiness gate, SQLite query predicate for `content_type='mail.notification'` or `metadata.source_kind=mail`, and Chinese notification content rebuilding. Remote RPC/auth session behavior is recorded for a later one-to-one authsdk/mail client slice. |
| `awiki-cli/internal/mail/{types.go,helpers.go,client.go,service.go}` | remote mail RPC endpoint, method/profile/param builders, summaries, service error display, client construction, auth session bootstrap, JWT refresh, HTTP execution, trace phases, and attachment result handling | `crates/awiki-cli/src/mail/{client,types,wire,service}.rs`, `crates/awiki-cli/tests/{mail_wire_contract,mail_live_contract}.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test mail_wire_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test mail_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test mail_live_contract --locked`; Go `go test ./internal/mail ./internal/cli -run 'TestNewClientRequiresMailServiceURL|TestServiceSendValidatesRequiredFields|TestServiceAttachmentValidatesIndex|TestMail' -count=1` | Translates the remote mail contract for `mail.getInbox`, `mail.getMessage`, `mail.markRead`, `mail.getMailbox`, `mail.getAttachment`, and `mail.send`: `/mail/rpc` endpoint, Go transport profiles, default inbox folder/limit, params, validation errors, service summaries, `ServiceError` display/conversion, `NewClient` URL/CA bundle behavior, active-identity/messaging-readiness gate, DID-auth session construction with remembered service scopes, stored JWT bearer seeding for service/did-auth/mail scopes, empty-token `get_me` bootstrap, persisted JWT update, shared Rustls HTTP execution, attachment content decode/writeback, `RPCPhase` method labels, and `EnsureJWTPhase` `mail_bootstrap`. Full remote mail service acceptance remains pending awiki-system-test coverage. |
| `awiki-cli/internal/cli/page.go` | handle-level page CLI dry-run/local-validation and non-dry-run content RPC command wiring: `create/list/get/update/rename/delete` | `crates/awiki-cli/src/app/page_handlers.rs`, `crates/awiki-cli/src/cli/mod.rs`, `crates/awiki-cli/src/cmdmeta/mod.rs` | `system_verified` | `crates/awiki-cli/tests/page_contract.rs`; `crates/awiki-cli/tests/page_live_contract.rs`; `crates/awiki-cli/tests/content_wire_contract.rs`; Go `go test ./internal/content ./internal/cli -run 'Test.*Page|Test.*Content|TestService' -count=1`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 PYTHONDONTWRITEBYTECODE=1 uv run pytest tests_v2/page/test_page_cli.py -ra -q` | Translates Go dry-run plan shapes for all handle-level page commands, including `/content/rpc` metadata, trim behavior, `changed_fields`, markdown-file byte counting, create slug/title validation, and markdown source conflict handling. Go dry-run does not validate visibility choices or reject empty page updates, so Rust preserves that boundary. Non-dry-run page commands call the translated content service through the shared authsdk Rustls HTTP execution layer, render Go-shaped content results, and map content service validation/auth/not-found/conflict errors to the same CLI exit codes. The focused page system-test file now proves real subprocess page lifecycle coverage against the configured `awiki.info` services: create/list/get/update/rename/delete, structured local validation failures, `unlisted` visibility, dry-run page create planning, duplicate conflict, and missing-page not-found mapping. Shared profile timeout caps apply to content RPC and auth refresh. Trace phase assertions, tenant site page commands, mail, and full repository-wide system acceptance remain separate work. |
| `awiki-cli/internal/content/{types.go,service.go}` | handle-level content RPC wire contract, live-service validation, auth bootstrap, JWT persistence, and HTTP execution | `crates/awiki-cli/src/content/{client,service,types,wire}.rs`, `crates/awiki-cli/tests/{content_wire_contract,page_live_contract}.rs` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test content_wire_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test page_live_contract --locked`; Go `go test ./internal/content -count=1`; `tests_v2/page` | Translates the `/content/rpc` contract for `create`, `list`, `get`, `update`, `rename`, and `delete`: endpoint constants, request structs, Go validation errors, visibility normalization, transport profiles, params, summaries, identity/page/list result shapes, and service-level update/visibility rejection that differs from dry-run planning. The live slice now adds `Client::new`, active identity resolution, `authsdk::Session` construction, remembered service and DID-auth scopes, stored JWT bearer seeding, empty-token `get_me` bootstrap with persisted JWT update, shared Rustls HTTP execution, service error conversion, and per-profile request timeout caps. Focused page system tests now pass against `awiki.info` through live registration plus content RPC. Trace phases remain deferred. No dependency was added. |
| `awiki-cli/internal/cli/site.go`, `awiki-cli/internal/site/{types.go,service.go}` | tenant bare-domain site CLI dry-run/local-validation and non-dry-run command wiring: `site root get/set`, `site page list/get/create/update/rename/delete` | `crates/awiki-cli/src/app/site_handlers.rs`, `crates/awiki-cli/src/cli/mod.rs`, `crates/awiki-cli/src/cmdmeta/mod.rs` | `unit_verified` | `crates/awiki-cli/tests/site_contract.rs`; `crates/awiki-cli/tests/site_live_contract.rs` | Translates Go dry-run plan shapes for `/site/rpc`, command catalog, required flags, body-source validation, local markdown-file byte counting, and domain/slug trim behavior. Non-dry-run site commands now call the translated site service through the shared authsdk Rustls HTTP execution layer, render Go-shaped site results, preserve active-identity-before-validation ordering, and map site service errors to `invalid_argument`, `auth_required`, `forbidden`, `not_found`, `conflict`, and `internal_error` exits. Focused system evidence for authenticated non-admin read commands is tracked in the adjacent scoped row; tenant-admin CRUD success remains contract/unit covered only. |
| `awiki-cli/internal/site/{types.go,service.go}` | tenant site RPC wire contract, live-service validation, auth bootstrap, JWT persistence, HTTP execution, and trace phases | `crates/awiki-cli/src/site/{client,service,types,wire}.rs`, `crates/awiki-cli/tests/{site_wire_contract,site_live_contract}.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test site_wire_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test site_live_contract --locked`; Go `go test ./internal/site ./internal/cli -run 'Test.*Site|TestGetRootCallsSiteRPC|TestDeletePageMapsRPCError|TestNormalizeDomainRejectsURLs' -count=1` | Translates the `/site/rpc` contract for `get_root`, `set_root`, `list_pages`, `get_page`, `create_page`, `update_page`, `rename_page`, and `delete_page`: endpoint constants, request structs, Go `NormalizeDIDDomain` domain normalization, trim-only slug validation, empty body allowance for explicit live params, transport profiles, params, summaries, identity/root/page/list/rename/delete result shapes, active identity loading, `authsdk::Session` construction, remembered service and DID-auth scopes, stored JWT bearer seeding, empty-token `get_me` bootstrap with persisted JWT update, shared Rustls HTTP execution, service error conversion, per-profile request timeout caps, shared RPC method trace phases, and `site_bootstrap` JWT phase labeling. Focused `awiki-system-test` coverage now exists only for authenticated non-admin `get_root`/`list_pages` error mapping in the adjacent scoped row; tenant-admin CRUD success remains not system-covered. No dependency was added. |
| `awiki-cli/internal/cli/site.go`, `awiki-cli/internal/site/service.go` | scoped live tenant-site `/site/rpc` system behavior: authenticated non-admin `site root get` and `site page list` forbidden mapping | `crates/awiki-cli/src/app/site_handlers.rs`, `crates/awiki-cli/src/site/{client,service,wire}.rs`; `tests_v2/site/test_site_cli.py` | `system_verified` | `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_BINARY=/home/ecs-user/awiki-space/awiki-cli-rs2/target/debug/awiki-cli AWIKI_CLI_UPDATE_CACHE_ONLY=1 PYTHONDONTWRITEBYTECODE=1 uv run pytest tests_v2/site/test_site_cli.py -ra -q` | Adds direct system evidence that a freshly registered handle identity can execute real Rust subprocess `site root get --domain anpclaw.com` and `site page list --domain anpclaw.com`, authenticate through the configured service, reach live `/site/rpc`, and receive Go-shaped non-admin envelopes with exit code 4, `error.code=forbidden`, `service rpc error -32001`/tenant-admin messaging, `ok=false`, and `meta.dry_run=false`. This is intentionally scoped negative coverage for the current backend where no tenant site admins are configured for the test domain; it does not claim `site root set`, page create/get/update/rename/delete, or tenant-admin successful CRUD. |
| `awiki-cli/internal/cli/runtime.go` local runtime/host-notify CLI behavior | runtime mode validation, setup/apply/status JSON, listener config validation/dry-run and local lifecycle state, host-notify config/token/enablement persistence, and local listener-refresh warnings | `crates/awiki-cli/src/app/runtime_handlers.rs`, `crates/awiki-cli/src/app/runtime_host_notify_refresh.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/src/config/mod.rs`, `crates/awiki-cli/src/cli/mod.rs`, `crates/awiki-cli/src/cmdmeta/mod.rs`, `crates/awiki-cli/tests/runtime_host_notify_enable_disable_contract.rs` | `system_verified` | `crates/awiki-cli/tests/runtime_contract.rs`; `crates/awiki-cli/tests/runtime_host_notify_enable_disable_contract.rs`; Go `go test ./internal/cli -run 'TestRuntimeDryRunPlansCoverStableActions|TestRuntimeValidationErrorsUseStableCodes|TestHostNotifyConfigViewRedactsOpenClawTokenValue|TestRefreshListenerForHostNotifyChange' -count=1`; `AWIKI_ENABLE_LISTENER_SERVICE_TESTS=1 AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 PYTHONDONTWRITEBYTECODE=1 uv run pytest tests_v2/runtime/test_runtime_cli.py -ra -q` | Local runtime CLI slice translated and system-verified through Rust subprocess tests. Supports `runtime status/apply/setup/mode get/set`, invalid-mode errors, dry-run `runtime_setup`/`runtime_apply` plans, listener config show/set validation and dry-run `listener_config_set` plans, listener enable/disable/status local lifecycle output, host-notify config show/set validation for `noop`/`log`/`file`/`openclaw`, OpenClaw hook/token set/clear with redaction, host-notify enable/disable sink preservation and Go-shaped `host_notify_enable_toggle` dry-run plans, and local refresh warnings. The full runtime CLI file passed with `AWIKI_ENABLE_LISTENER_SERVICE_TESTS=1` and no skips, so gated selectors exercised the subprocess interface in a host where `systemctl --user` was available; the Rust implementation still records local listener state rather than executing real platform service-manager operations. This row excludes OpenClaw route registry confirmation details, Hermes rows, real platform service install/start/stop/restart/uninstall execution, real foreground listener restart after host-notify changes, mail notification delivery, and mail-system acceptance. |
| `awiki-cli/internal/cli/runtime_host_notify_routes.go`, `awiki-cli/internal/runtime/openclawnotify/{routes.go,webhook.go}` CLI route boundary | OpenClaw host-notify route add/list/remove, session-key parsing, dry-run plans, duplicate suppression, and route-add loopback confirmation webhook | `crates/awiki-cli/src/runtime/openclaw_routes.rs`, `crates/awiki-cli/src/runtime/openclaw_webhook.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/src/app/runtime_handlers.rs`, `crates/awiki-cli/src/cli/mod.rs`, `crates/awiki-cli/src/cmdmeta/mod.rs` | `system_verified` | `cargo +1.79.0 test -p awiki-cli openclaw_routes --locked`; `cargo +1.79.0 test -p awiki-cli openclaw_webhook --locked`; `cargo +1.79.0 test -p awiki-cli --test runtime_contract host_notify_openclaw --locked`; Go `go test ./internal/runtime/openclawnotify -count=1`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 PYTHONDONTWRITEBYTECODE=1 uv run pytest tests_v2/runtime/test_runtime_cli.py::test_runtime_host_notify_openclaw_route_add_list_remove_uses_local_webhook -ra -q` | Translates the route CLI and local registry boundary: route input normalization, `--session-key` parsing, add/list/remove persistence, dry-run add/remove plans, duplicate add without a second POST, token redaction, bearer-token forwarding, and one Go-shaped route-add confirmation payload through a loopback webhook capture. This system evidence uses a local capture server and does not require or prove a real OpenClaw process, foreground listener notification delivery, mail/group notification delivery, Hermes delivery, or platform service-manager behavior. |
| `awiki-cli/internal/cli/runtime.go` platform service-manager and real listener restart residual | real platform service-manager install/start/stop/restart/uninstall execution and `refreshListenerForHostNotifyChange` restarting a live foreground listener after host-notify changes | `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/src/runtime/listener_service.rs`, `crates/awiki-cli/src/app/runtime_host_notify_refresh.rs` | `deferred_with_reason` | `crates/awiki-cli/tests/runtime_contract.rs`; `crates/awiki-cli/tests/runtime_listener_service_contract.rs`; Go `go test ./internal/cli -run 'TestRefreshListenerForHostNotifyChange' -count=1`; Go listener service focused tests | Current Rust intentionally avoids adopting a platform service-manager library during this translation pass and uses local listener-state files for the CLI lifecycle surface. Pure helper/service-plan behavior is covered by Rust tests, and system tests verify subprocess-visible state and warnings, but real platform service-manager execution, real `systemctl`/launchd/Windows-service calls, real foreground listener restart triggered by host-notify config writes, and cross-platform service-manager parity remain deferred until a dedicated dependency-reviewed service lane. This preserves the pure-Rust/cross-platform dependency constraint and avoids adding platform libraries without explicit parity evidence. |
| `awiki-cli/internal/runtime/config.go` | runtime resolved JSON shape and mode/host-notify normalization | `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/src/config/mod.rs` | `system_verified` | `crates/awiki-cli/tests/runtime_contract.rs`; `tests_v2/runtime/test_runtime_cli.py` | Matches the verified `data.runtime` shape with `listener` and `host_notify` nested objects. Host-notify `webhook` alias normalizes to `hermes`; OpenClaw and Hermes defaults are represented. Socket path resolution now delegates to the split runtime bridge helper for Go-style default and normalization behavior, including deterministic Windows named-pipe default and blank-path normalization shape. |
| `awiki-cli/internal/runtime/bridge_unix.go`, `awiki-cli/internal/runtime/bridge_windows.go`, `awiki-cli/internal/runtime/config.go` bridge helper subset | local websocket bridge endpoint defaulting, socket-path normalization, endpoint preparation, Unix availability check, Windows named-pipe helper validation, bridge request/response/error shapes, Unix listen/probe/dial/call I/O | `crates/awiki-cli/src/runtime/bridge.rs`, `crates/awiki-cli/src/runtime/{mod,listener}.rs`, `crates/awiki-cli/tests/runtime_bridge_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_bridge_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test runtime_contract --locked`; Go `go test ./internal/runtime -run 'TestResolveShortensLongSocketPath|TestResolveKeepsShortSocketPath|TestResolveDefaultsToWebSocketMode' -count=1`; Go `go test ./internal/message -run 'TestWSProxyTransportCallsLocalBridgeAndDecodesResponses|TestWSProxyTransportWrapsBridgeFailures' -count=1` | Translates the local bridge boundary: Unix default `<state_dir>/message-daemon.sock` with workspace runtime fallback, Windows named-pipe default/blank-normalization/prefix-validation helper rules, Unix long socket path shortening to temp `awiki-cli-<sha256-prefix>.sock`, `prepare_bridge_endpoint` directory creation with Go error prefix and bare-filename parent `"."`, Unix endpoint availability helper, Go-shaped bridge JSON defaults/error display strings, Unix `ListenBridge` stale-socket replacement, `BridgeHealthProbe`, `CallLocalBridge` two-connection health-probe-then-request flow with bridge phase errors and transportcfg timeouts, and Go listener `handleConn` server-side newline framing as an injected-dispatch helper. Windows named-pipe dial/listen/health/availability I/O remains deferred to avoid adding a platform dependency before a dedicated dependency review; foreground WebSocket listener service execution and message service WS proxy wiring remain later slices. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` bridge connection framing helper subset | `handleConn` one-request local bridge framing before `handleBridgeRequest` | `crates/awiki-cli/src/runtime/bridge.rs`, `crates/awiki-cli/tests/runtime_bridge_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_bridge_contract --locked`; Go source parity for `handleConn` | Translates Go `handleConn` framing without wiring foreground listener execution: read one request with newline-required `ReadBytes('\n')` semantics, reject EOF-before-newline with `error.message="EOF"`, decode a single `BridgeRequest`, ignore bytes after the first frame, dispatch through an injected closure, and encode one newline-terminated `BridgeResponse`. Read, JSON, and dispatch errors use `ok=false` plus `error.message` and no code/result. Windows named-pipe endpoint helper parity is covered by the bridge helper row; this does not implement `Supervisor`, `acceptLoop`, real socket lifecycle ownership, `handleBridgeRequest`, WebSocket sessions, or Windows named-pipe listener/connection I/O. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` bridge connection execution adapter | injected `handleConn` to `handleBridgeRequest` execution order, including RPC send and best-effort local mark-read callback | `crates/awiki-cli/src/runtime/listener_bridge_connection.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_bridge_connection_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_bridge_connection_contract --locked`; adjacent bridge framing/dispatch/service-DID tests | Adds a small trait-driven adapter that composes the already translated bridge framing and dispatch builders in Go order: `ensureSession` is called first; missing record or client returns `websocket session is not connected for identity <identity>`; `group.create` fetches the message-service DID before RPC building; build errors return before `SendRPC`; send errors return before local mark-read; and successful `inbox.mark_read` invokes a best-effort local mark-read callback after the successful RPC while ignoring callback errors like Go's discarded `store.MarkMessagesRead` result. This still does not implement real `Supervisor`, session locks/timers/goroutines, real `WSClient.SendRPC`, actual SQLite mark-read mutation, foreground socket accept ownership, notification dispatch, or Windows named-pipe listener I/O. No dependency was added; both new Rust files are below the default 1200-line cap. |
| `awiki-cli/internal/runtime/hermesbridge/hermes_config.go` pure helper boundary, read-only route inspection, and local `EnsureRoute` mutation | Hermes bridge defaults, local notify URL validation, deliver/home-channel helpers, `.env` parsing, route cleanup helpers, notify prompt replacement predicates, `InspectRoute` status fields, and pure local `EnsureRoute` route YAML mutation | `crates/awiki-cli/src/runtime/hermes_bridge.rs`, `crates/awiki-cli/src/runtime/hermes_bridge/route.rs`, `crates/awiki-cli/tests/runtime_hermes_bridge_contract.rs`, `crates/awiki-cli/tests/runtime_hermes_ensure_route_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_hermes_bridge_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test runtime_hermes_ensure_route_contract --locked`; Go `go test ./internal/runtime/hermesbridge -count=1` | Translates the deterministic helper subset and local route mutation used by Hermes setup: `DEFAULT_WEBHOOK_PORT=8644`, `notify`, default local notify URL, default `feishu` deliver target, loopback-only `http` notify URL validation, supported deliver targets, home-channel env key mapping, display names, Go-like `.env` parsing, `deliver_extra` fixed-target cleanup, legacy single `skills: ["notify"]` cleanup, prompt migration predicates, missing/configured `RouteState` fields, route secret generation/preservation, home-channel readiness, Feishu env credential detection, route warnings, durable same-directory temp write with Unix `0700` config dir and `0600` temp file, creation of missing route config, prompt migration to the current Chinese mail-aware default, custom prompt/custom skills preservation, fixed `deliver_extra` target removal with unrelated extra keys preserved, existing `events` preservation, and preservation of unmanaged top-level/platform/route blocks recognized by the narrow writer. This intentionally does not implement listener restart/refresh, bridge `Apply`, platform service-manager status, or owned health probing. It also does not claim full Go `yaml.v3` round-trip parity for comments, anchors, complex scalars, or arbitrary YAML formatting; that remains a separate parser/serializer decision. No dependency was added. |
| `awiki-cli/internal/runtime/hermesbridge/service.go` local helper subset | Hermes bridge service naming, display names, service config shape, adapter command plan, Python executable lookup, adapter script lookup, adapter child process lifecycle helper, hidden service-run adapter loop, status aggregation, lifecycle branch planning, hidden service-run command detection, injected health/readiness polling, and pure `Apply` branch planning | `crates/awiki-cli/src/runtime/hermes_bridge.rs`, `crates/awiki-cli/src/runtime/hermes_bridge/service.rs`, `crates/awiki-cli/tests/runtime_hermes_bridge_service_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_hermes_bridge_service_contract --locked`; `cargo +1.79.0 test -p awiki-cli runtime::hermes_bridge::tests --lib --locked`; adjacent Hermes bridge/CLI/setup tests; Go `go test ./internal/runtime/hermesbridge -count=1` | Translates the deterministic local helper boundary from Go `service.go` without adopting a `kardianos/service` equivalent: hashed service names using `awiki-cli-hermes-bridge-<sha256 12 hex>`, nil/default workspace handling, display names from workspace basename, Go service config fields (`runtime host-notify hermes bridge service-run`, user service, keepalive, restart-on-failure, log output/log directory, workspace/HERMES_HOME env, Windows empty working directory), Go `serviceProgram.Start` adapter command shape as a pure plan (resolved Python executable path, adapter script, `--host`, `--port`, `--notify-secret`, `--hermes-webhook-url`, `--hermes-route-secret`, `--log-level INFO`, optional `HERMES_HOME`, parent stdout/stderr), local adapter process helpers that spawn that plan, inherit parent env/stdout/stderr semantics, expose wait/try-wait/running state, and stop by killing then waiting with the Go 15-second timeout/error text, a `RunService`-shaped local adapter loop that starts the adapter, waits for shutdown, and stops the child, Unix SIGTERM/SIGINT shutdown handling for the hidden CLI entry, Python lookup with `python3` before `python` and Unix executable-bit filtering, adapter script lookup in Go candidate order (`../scripts`, `scripts`, `../../scripts`) with canonicalized existing file paths, injected `StatusFor` aggregation for config errors, service status warnings, platform/service-name replacement, running-only health probing and health warning, route warning propagation, current Rust `rust-local` no-service boundary, pure lifecycle operation plans for `EnsureInstalled`, `StartService`, `StopService`, `RestartService`, `Uninstall`, and `Apply`, hidden command service-run detection, 2xx-only health probe boundary through an injected status getter, and `waitForStatus` semantics that require `running && bridge_available` when starting and return the last status on timeout while ignoring transient status errors. This intentionally does not implement platform service install/start/stop/restart/uninstall, real platform service status lookup, lifecycle operation execution, owned HTTP client health probes, Windows SCM stop-control cleanup, Windows full `PATHEXT` lookup parity, or non-dry-run setup bridge application. No dependency was added. |
| `awiki-cli/internal/cli/runtime.go` Hermes host-notify read views and hidden service-run | `runtime host-notify hermes guide`, `runtime host-notify webhook guide`, `runtime host-notify hermes status`, `runtime host-notify webhook status`, and hidden `runtime host-notify hermes bridge service-run` local adapter execution | `crates/awiki-cli/src/app/runtime_hermes_handlers.rs`, `crates/awiki-cli/src/cli/mod.rs`, `crates/awiki-cli/src/cmdmeta/mod.rs`, `crates/awiki-cli/src/runtime/{hermes_bridge,hermes_host_notify,mod}.rs`, `crates/awiki-cli/tests/runtime_hermes_cli_contract.rs` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_hermes_cli_contract --locked`; adjacent runtime/Hermes/OpenClaw tests; Go `go test ./internal/cli -run 'TestBuildHermesHostNotifyGuideViewPrefersHomeChannelGuidance|TestHostNotifyConfigViewRedactsHermesSecretValue|TestRuntimeDryRunPlansCoverStableActions' -count=1`; Go `go test ./internal/runtime/hermesbridge -count=1`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/runtime/test_runtime_cli.py::test_runtime_host_notify_hermes_guide_status_and_config_commands_work -q`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/runtime/test_runtime_cli.py -ra -q` | Adds the read-only Hermes CLI views without mixing setup/service side effects: command metadata and parser aliases, guide deliver override and unsupported-target error, Go guide data/warnings/recommended commands, Hermes secret-source detection from config or env only when a notify URL is configured, host-notify config view secret metadata, status readiness booleans, read-only local Hermes route inspection, bridge status/config warning aggregation, `ready=false` default, and ready summary shape. The `awiki-system-test` selector verifies guide/status through a real Rust subprocess with isolated `HERMES_HOME`, including invalid deliver rejection, readiness booleans, sink/secret transitions through config commands, and secret redaction. The hidden bridge `service-run` entry now mirrors the Go `RunService` entry by resolving bridge config, building the adapter command plan, starting the adapter child, waiting until service shutdown, stopping the child, and returning success without a CLI envelope; missing Hermes notify secret/route secret/Python/script errors still surface before process start. The Hermes app handlers were split into `runtime_hermes_handlers.rs` to keep source files under the default 1200-line cap. This slice intentionally excludes platform service-manager install/start/status/control and owned bridge health probing. No dependency was added. |
| `awiki-cli/internal/cli/runtime.go` Hermes host-notify local config writes | `runtime host-notify hermes set`, `runtime host-notify webhook set`, `runtime host-notify hermes set-secret`, `runtime host-notify webhook set-secret`, `runtime host-notify hermes clear-secret`, and `runtime host-notify webhook clear-secret` | `crates/awiki-cli/src/app/runtime_hermes_handlers.rs`, `crates/awiki-cli/src/app/runtime_host_notify_refresh.rs`, `crates/awiki-cli/src/cli/mod.rs`, `crates/awiki-cli/src/cmdmeta/mod.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_hermes_config_write_contract.rs` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_hermes_config_write_contract --locked`; adjacent runtime/Hermes/OpenClaw tests; Go focused CLI/config writer and listener-refresh tests; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/runtime/test_runtime_cli.py::test_runtime_host_notify_hermes_guide_status_and_config_commands_work -q`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/runtime/test_runtime_cli.py -ra -q` | Adds the local Hermes write commands without mixing setup or route/service side effects: command metadata and `webhook` aliases, missing changed-flag error and Go hint, dry-run `host_notify_hermes_set` plan before live deliver validation, live unsupported-deliver rejection, persistent `notify_url`/`deliver` writes through the existing Hermes/webhook config writer, `set-secret` and `clear-secret` redacted outputs, Go-shaped summaries, and shared local listener-refresh context. The `awiki-system-test` selector switches the sink to Hermes, verifies `set` returns the configured Hermes runtime object, verifies `set-secret` and `clear-secret` summaries and redaction, and verifies `config show` exposes `secret_configured`/`secret_source` without a plaintext secret field. Go parity detail: `set` does not switch the host-notify sink by itself; the selector explicitly sets the sink first to cover the configured-Hermes path. This slice still excludes full YAML parser/serializer selection, platform service-manager status, bridge health probing, and real service-manager execution. No dependency was added. |
| `awiki-cli/internal/cli/runtime.go` Hermes host-notify setup local transaction | `runtime host-notify hermes setup`, `runtime host-notify hermes setup --dry-run`, and `runtime host-notify webhook setup --dry-run` | `crates/awiki-cli/src/app/runtime_hermes_handlers.rs`, `crates/awiki-cli/src/app/runtime_host_notify_refresh.rs`, `crates/awiki-cli/src/cli/mod.rs`, `crates/awiki-cli/src/cmdmeta/mod.rs`, `crates/awiki-cli/tests/runtime_hermes_setup_dry_run_contract.rs` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_hermes_setup_dry_run_contract --locked`; adjacent runtime/Hermes tests; Go focused CLI/config/Hermes bridge/listener-refresh tests; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/runtime/test_runtime_cli.py::test_runtime_host_notify_hermes_setup_writes_local_files -q`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/runtime/test_runtime_cli.py -ra -q` | Adds setup command routing and the local-file half of Go non-dry-run setup: canonical `hermes setup` command output for the `webhook` alias, loopback-only local notify URL validation, supported deliver validation, default `notify_url`/`deliver`, raw config fallback for `runtime.host_notify.hermes.notify_url`, legacy `runtime.host_notify.webhook.notify_url`, and `runtime.host_notify.hermes.deliver` even when the current sink is not Hermes, secret-source reporting for flag/config/legacy config/environment/generated values with secret redaction, malformed-config error handling for setup secret resolution, dry-run `host_notify_hermes_setup` plan fields, and dry-run no-write assertions. The `awiki-system-test` selector verifies dry-run plan fields and no local Hermes write, then verifies non-dry-run writes isolated awiki config plus isolated `HERMES_HOME/config.yaml`, redacts the explicit secret from output, returns host-notify/local-Hermes/passive-bridge/next-step data, and preserves the deferred bridge-service warning. Bridge `Apply`, platform service install/start/restart, real service status, and owned health probing remain deferred. No dependency was added. |
| `awiki-cli/internal/runtime/listener/{types.go,files.go,status_helpers.go}` and narrow `manager.go` status merge | listener status shape, runtime file helpers, session warnings, saved status merge | `crates/awiki-cli/src/runtime/listener.rs`, `crates/awiki-cli/src/runtime/mod.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli listener --locked`; `cargo +1.79.0 test -p awiki-cli --test runtime_contract listener_status_merges_saved_sessions_and_host_notify_state --locked`; `go test ./internal/runtime/listener -run 'TestSessionWarnings|TestHasDisconnectedSessions|TestMergeSavedRuntimeStatus' -count=1` | Translates local listener `Status`, `SessionStatus`, and `HostNotifyStatus` JSON shape; `listener.pid`, `listener.status.json`, and `listener.expected-boot-id` helpers with Unix `0600` writes; disconnected-session warnings; and Go's saved-status merge rules including PID mismatch skip and running-only host-notify override. `runtime listener status` now merges saved sessions/boot/start/host-notify state. Service install/start/stop/restart/uninstall still use Rust local state instead of platform service-manager calls; native platform service control remains deferred and dependency-reviewed. |
| `awiki-cli/internal/runtime/listener/server.go` host-notify status mutation helper subset | `setHostNotifyError` and `clearHostNotifyError` changed-only `HostNotify.LastError` persistence | `crates/awiki-cli/src/runtime/listener.rs`, `crates/awiki-cli/tests/runtime_listener_service_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_service_contract --locked`; Go source parity for `dispatchHostNotification`, `setHostNotifyError`, and `clearHostNotifyError` | Translates Go's host notification delivery status helper semantics without wiring the foreground `Supervisor`: sink failures set `host_notify.last_error` and write `listener.status.json` only when the error string changes; successful delivery clears a non-empty error and writes once; repeated same errors and repeated clears are no-op writes; and status-file write errors are ignored like Go's `_ = writeStatus(...)`. The Rust helper returns the changed/no-op branch for contract tests and later foreground wiring. This does not implement `Supervisor` locks, foreground WebSocket/session ownership, actual host notification dispatch wiring, direct-secure decrypt/ack execution, local secure-ack queue delivery, platform service execution, Windows named-pipe I/O, or awiki-system-test listener acceptance. No dependency was added. |
| `awiki-cli/internal/runtime/listener/service.go`, `run_foreground_unix.go`, `run_foreground_windows.go`, `sysproc_unix.go`, `sysproc_windows.go` local helper subset | listener service naming, display names, `newService` config shape, `serviceStatusFor` result mapping, `EnsureInstalled` install-error decision, `RunService` outer action plan, service-mode detection, boot-id helpers, readiness polling, artifact cleanup, foreground signal platform plan, detached child-process platform plan, service lifecycle branch plans, and `serviceProgram.Start`/`Stop` state/action plans | `crates/awiki-cli/src/runtime/listener_service.rs`, `crates/awiki-cli/src/runtime/listener.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_service_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_service_contract --locked`; Go `go test ./internal/runtime/listener -run 'TestWaitForServiceStatusWithWaitsForBridgeAvailability|TestWaitForServiceStatusWithWaitsForExpectedBootID|TestStartServiceAutoInstallsWhenMissing' -count=1` | Translates deterministic local helpers from Go `service.go` plus the small platform file rules from `run_foreground_*` and `sysproc_*` without adopting `kardianos/service`: hashed service name (`awiki-cli-listener-<sha256 12 hex>`), display name from workspace basename, Go `newService` config shape (`runtime listener service-run` arguments, realtime description, workspace/log/state-derived paths, service mode/workspace env vars, RunAtLoad/KeepAlive/UserService/DelayedAutoStart/StartType/OnFailure/OnFailureDelayDuration/LogOutput/LogDirectory/PIDFile options, and Windows empty working directory), pure `serviceStatusFor` mapping for new-service errors, running/non-running statuses, `ErrNotInstalled`, other status errors, platform string, and service-name return shape, Go `EnsureInstalled` install-error tolerance for case-insensitive `exists` substrings, pure `RunService` outer sequencing for service-program creation, `newService` short-circuiting, `svc.Run` invocation, and run-error propagation, `AWIKI_LISTENER_SERVICE_MODE`/`runtime listener service-run` detection, `boot-<nanos>-<8 hex>` boot ID shape with time fallback, expected boot-id write/read/resolve behavior, cleanup of pid/status/socket/expected-boot artifacts, `waitForServiceStatusWith`/`serviceStatusReady` polling semantics including bridge and boot-id gates, foreground signal selection (`os.Interrupt` on Windows, `os.Interrupt` plus `syscall.SIGTERM` elsewhere), child-process `setsid=true` on Unix with Windows no-op, pure branch plans for `EnsureInstalled`, `StartService`, `StopService`, `RestartService`, `Uninstall`, and `ApplyRuntimePolicy`, and pure `serviceProgram.Start`/`Stop` plans. Lifecycle plans preserve Go ordering for websocket-mode validation, missing-service auto-install/recheck, boot-id preparation before start/restart, cleanup before wait-for-stopped, not-installed errors, and auto-install/auto-start policy branching. The `serviceProgram` plans preserve Go lock/idempotent start/new-supervisor error/cancel-context/done-channel/store/spawn/unlock ordering, run-loop result-send then cleanup then close-done ordering, stop lock/snapshot/clear/unlock ordering, cancel-before-close, close-before-wait, and the 15-second stop timeout error. This does not implement platform service install/start/stop/restart/uninstall execution, real `servicepkg.New`/`newService` execution, real platform `svc.Status()`/`svc.Platform()` calls, real `svc.Install()` execution, real `svc.Run()` execution, real `serviceProgram` execution, process supervision, actual `signal.NotifyContext`, actual `exec.Cmd.SysProcAttr` mutation, foreground WebSocket/session execution, or OS service-manager integration. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` supervisor initialization helper subset | `NewSupervisor` initialization ordering, failure cleanup, initial status fields, and map initialization | `crates/awiki-cli/src/runtime/listener_supervisor_init.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_supervisor_init_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_supervisor_init_contract --locked`; Go source parity for `NewSupervisor`; adjacent listener service/host-notify/shutdown context | Translates the pure initialization/cleanup order of Go `NewSupervisor`: store open runs first; schema errors close the store; boot-id, paths, and host-notify sink errors close the store; remote-client errors close the store then the host-notify sink; success creates the identity manager, initializes the sessions and local-notifications maps, and builds an initial running status from the resolved mode, listener service mode, boot ID, runtime paths, process PID, started-at timestamp, and host-notify status. This does not implement real SQLite open/schema execution, real boot-id/path filesystem calls, real host-notify sink ownership, real remote client construction, real identity manager I/O, actual `os.Getpid`/`time.Now` capture, or foreground `Supervisor` resource ownership. No dependency was added; both new Rust files are below the default 1200-line cap. |
| `awiki-cli/internal/runtime/listener/server.go` foreground orchestration helper subset | `Supervisor.Run`, `startSocket`, and `acceptLoop` startup ordering, early-return errors, bridge availability, watcher spawn, accept-loop branch, and context wait | `crates/awiki-cli/src/runtime/listener_foreground.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_foreground_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_foreground_contract --locked`; Go source parity for `Run`/`startSocket`/`acceptLoop`; adjacent Go `TestStartSocketPersistsBridgeAvailability` context | Translates the pure foreground startup/control-loop flow before real `Supervisor` execution: non-websocket mode returns `runtime mode must be websocket before starting the listener` before PID/status/socket work; PID write errors stop before status write; status write errors stop before socket start; `startSocket` listens first and on listen error stops before storing the listener, setting bridge availability, or spawning the accept loop; listen success stores the listener, sets bridge availability true, and spawns `acceptLoop`; `Run` starts known sessions after socket startup, returns known-session errors before spawning `watchNewIdentities`, then spawns identity watch and waits for context cancellation before returning nil; `acceptLoop` spawns `handleConn` and continues for accepted connections, and exits without surfacing the accept error. This does not implement real `writePID`, `writeStatus`/`refreshStatus`, `runtime.ListenBridge`, listener ownership, goroutine/task spawning, context blocking, real session discovery, real accept loop/socket iteration, real bridge connection handling, or foreground service runtime. No dependency was added; the Rust source/test remain below the default 1200-line cap. |
| `awiki-cli/internal/runtime/listener/server.go`, `awiki-cli/internal/runtime/listener/service.go` foreground runtime entrypoints | `runtime listener run` and hidden `runtime listener service-run` foreground WebSocket listener execution boundary | `crates/awiki-cli/src/runtime/listener_supervisor_run.rs`, `crates/awiki-cli/src/runtime/listener_ws_transport.rs`, `crates/awiki-cli/src/app/runtime_handlers.rs`, `crates/awiki-cli/src/cli/mod.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/src/message/mod.rs` | `system_verified` | `cargo +1.79.0 fmt --check`; `cargo +1.79.0 check -p awiki-cli --locked`; focused runtime listener contracts including `runtime_listener_service_contract`, `runtime_bridge_contract`, `runtime_listener_bridge_connection_contract`, and `runtime_listener_foreground_contract`; `awiki-system-test` `tests_v2/cli/test_awiki_cli_runtime_listener_local.py`; `awiki-system-test` `tests_v2/cli/test_awiki_cli_service_run_local.py`; structure/whitespace checks recorded in `docs/verification/` | Rust now dispatches `runtime.listener.run` and `runtime.listener.service-run` to executable foreground listener entrypoints. The runtime owner writes PID/status/boot artifacts, starts the Unix local bridge, accepts bridge requests, runs per-identity WebSocket session loops with reconnect, performs one-shot WebSocket RPC for local bridge calls, persists direct/mail/group/group-state notifications through the existing listener notification handler, normalizes/decrypts secure direct notifications, performs local secure ACK recovery with the local ANP Rust SDK, flushes queued secure outbox rows, and polls/replays unread secure direct backlog. The WebSocket transport is a dependency-free std/Rustls implementation (`ws`/`wss` handshake, masked client frames, JSON text/binary frames, ping/pong/close) that avoids `tungstenite`, OpenSSL, `native-tls`, async runtimes, extra HTTP clients, YAML crates, platform service-manager libraries, or new SQLite dependencies. `listener_supervisor_run.rs` is a documented 1529-line translation-time exception because Go `server.go` is 1802 lines; future splitting is an optimization/refactor item, not mixed into this parity slice. Residual parity risks: foreground cleanup still relies mostly on process termination rather than an explicit OS signal handler, and bridge RPC currently opens a one-shot WebSocket connection instead of reusing Go's persistent session client. |
| `awiki-cli/internal/runtime/listener/wsclient.go` pure helper subset and `awiki-cli/internal/config/config.go` WebSocket URL helper | WebSocket endpoint derivation, DID-auth endpoint derivation, request ID numeric coercion, int64 coercion, URL host extraction helpers, JSON-RPC envelope/result helpers, injected JSON frame read/write helpers, dial-error body formatting, and injected Connect bearer refresh/retry decision flow | `crates/awiki-cli/src/runtime/listener_wsclient.rs`, `crates/awiki-cli/src/config/mod.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_wsclient_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_wsclient_contract --locked`; Go `go test ./internal/runtime/listener -run 'TestNewWSClientDerivesIMWebSocketEndpointFromServiceBaseURL|TestWSClientConnectRefreshesExpiredBearerBeforeRetryingWebSocket|TestWSClientConnectBootstrapsBearerBeforeOpeningWebSocket' -count=1`; Go/Rust probes for `fmt("%.0f")` and `url.Parse` edge behavior | Translates deterministic helpers before selecting a WebSocket transport crate: `JoinBaseURL` empty-path parity, `DeriveWebSocketURL` `https`→`wss` and `http`→`ws` conversion with case-sensitive scheme matching, `/im/ws` request/WebSocket endpoint derivation, `/user-service/did-auth/rpc` endpoint derivation, Go's empty-service-base boundary where `JoinBaseURL("", "/im/ws")` yields `/im/ws`, `NewWSClient` remembered-scope input order, `dialBearer` bearer header trimming, `refreshBearer` precondition errors, Go `Connect` branch decisions for initial JWT dial, 401-only refresh/retry, no-token bootstrap, refresh error wrapping, empty refreshed-token error, and retry dial-error formatting, `requestIDFromAny` string/int/float `%.0f` behavior including half-even rounding, `int64FromAny` truncation toward zero, `hostForURL` raw parse/no-trim fallback semantics, `SendRPC` JSON-RPC request envelope params omission/presence behavior, object-only JSON-RPC error detection, object-only result extraction, `failPending` response shape, `readLoop` response-vs-notification classification by raw `id` presence, `wsjsonWrite` marshal-before-text-frame-write behavior, `wsjsonRead` read-before-unmarshal behavior, and `formatDialError` nil/body/4096-byte/trimmed-body formatting. This does not implement real `WSClient`, WebSocket transport crate selection, HTTP response ownership, actual JWT refresh HTTP execution, pending RPC channels, notification channel buffering, foreground listener session execution, local bridge I/O, host-notify dispatch, or SQLite listener side effects. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` connect-session helper subset | `connectSession` identity load/readiness/path/auth-session/JWT-seeding/connect-close/writeback decisions before real WebSocket transport | `crates/awiki-cli/src/runtime/listener_connect_session.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_connect_session_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_connect_session_contract --locked`; Go source parity plus `go test ./internal/runtime/listener -run 'TestSessionLoopReconnectsAndStoresNotifications|TestWSClientConnectRefreshesExpiredBearerBeforeRetryingWebSocket|TestWSClientConnectBootstrapsBearerBeforeOpeningWebSocket' -count=1` and `go test ./internal/cli -run TestIdentityGatingUsesFrozenErrorCode -count=1` | Translates the deterministic pre-transport boundary of Go `connectSession`: identity manager load error returns before readiness/path work; stored identity readiness uses Go registration/handle missing-state rules and Go-shaped registration error text; paths lookup happens only after readiness; auth-session plan captures DID document path, key-1 private path, identity name, DID, and initial JWT; nonblank-after-trim stored JWT seeds exactly three bearer scopes in Go order using the original token string (`service_base_url`, DID-auth RPC URL, and `/im/ws` request URL); blank stored JWT seeds no bearer scopes; `NewWSClient` errors return without closing an unconstructed client; connect uses a 15-second timeout; connect errors close the constructed client and return the error; success writes back `authSession.CurrentJWT()` into the returned record. This does not implement real `identity.Manager`, `authsdk.NewSession`, JWT update callback execution, `NewWSClient`, `WSClient.Connect`, real timeout/context ownership, WebSocket transport selection, foreground session loop, SQLite writes, host-notify dispatch, local bridge I/O, or new dependency selection. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` consume-notifications helper subset | `consumeNotifications` ping/context/channel-close/notification-dispatch control flow before real WebSocket notification consumption | `crates/awiki-cli/src/runtime/listener_notification_consume.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_notification_consume_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_notification_consume_contract --locked`; Go source parity plus adjacent Go listener reconnect guard `go test ./internal/runtime/listener -run TestSessionLoopReconnectsAndStoresNotifications -count=1` | Translates the pure step boundary of Go `consumeNotifications`: the exported ping interval remains 60 seconds, ping timeout remains 15 seconds, context cancellation exits with the context error and no side effects, ping ticks plan timeout creation and always cancel after `client.Ping`, ping failures wrap as `websocket ping failed: <err>`, received notifications plan `handleNotification` and continue, closed notification channels prefer `client.ReaderError()`, and a clean closed channel returns `websocket notification loop closed`. This does not implement real ticker/context ownership, `WSClient.Ping`, notification channel reads, `ReaderError`, `handleNotification` side effects, foreground WebSocket session execution, host-notify dispatch, SQLite writes, or new dependency selection. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` notification route-plan helper subset | `handleNotification` branch ordering and side-effect planning after injected secure normalization | `crates/awiki-cli/src/runtime/listener_notification_plan.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_notification_plan_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_notification_plan_contract --locked`; Go source parity plus `go test ./internal/runtime/listener -run 'TestHandleNotificationDispatchesHostNotificationToSink|TestHandleNotificationStoresMessageWhenHostNotifyFails|TestHandleNotificationDispatchesMailNotificationToSink|TestHandleNotificationDecryptsSecureDirectIncomingAndStoresPlaintext|TestDeliverLocalSecureAckInProcessPromotesPendingInitiatorSession|TestRecordsFromGroupStateChangedBuildsMemberAndSystemMessage' -count=1` | Translates the pure route/action-planning boundary of Go `handleNotification`: direct-secure normalization is represented as an injected keep/replace/drop outcome and always runs first; host notification normalization is computed once on the post-secure notification; branch order is direct, mail, group, then group-state; direct/group branches plan contact sync before handle enrichment, store, and host dispatch; contact-sync errors become blank sender handles like Go's discarded error; mail plans store then dispatch with no contact sync or handle enrichment; group-state plans upsert group, optional member, store message, then dispatch; unknown notifications produce no actions; secure direct drop stops before host/store; secure replacements can intentionally suppress normal direct storage when the rewritten method is `direct.secure.*`; secure wire fallback stores as E2EE direct when normalization keeps the original. This does not implement real `normalizeDirectSecureNotification`, E2EE decrypt/ack/session-store mutation, `FlushQueuedSecureOutbox`, `SendJSON`, local notification queue mutation, SQLite writes, remote contact lookup, host sink delivery/status writes, foreground WebSocket session execution, or new dependency selection. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` notification side-effect execution subset | `handleNotification` single-notification SQLite/contact/host-notify execution after injected secure normalization, plus dispatch outcome status application | `crates/awiki-cli/src/runtime/listener_notification_execute.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_notification_execute_contract.rs`; `tests_v2/cli/test_awiki_cli_runtime_listener_local.py`; `tests_v2/cli/test_awiki_cli_host_notify_openclaw_local.py` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_notification_execute_contract --locked`; adjacent planner/contact/host-notify/store tests; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/cli/test_awiki_cli_runtime_listener_local.py tests_v2/cli/test_awiki_cli_host_notify_openclaw_local.py -ra -q`; Go `go test ./internal/runtime/listener ./internal/store -run 'TestHandleNotificationDispatchesHostNotificationToSink|TestHandleNotificationStoresMessageWhenHostNotifyFails|TestHandleNotificationDispatchesMailNotificationToSink|TestRecordsFromGroupStateChangedBuildsMemberAndSystemMessage|TestStoreMessageAndThreadView|Test.*Contact' -count=1` | Adds a local/injected executor for one already-received listener notification. It reuses the route planner, `runtime::listener_contact_sync`, `store::{store_message, upsert_group, upsert_group_member}`, and `HostNotifySink` to apply Go-ordered side effects: direct/group contact sync and handle enrichment before message storage and dispatch; mail storage before dispatch; group-state group/member/message mutation before dispatch. Store/upsert and sink errors are recorded in the returned execution summary without aborting later side effects, preserving Go's best-effort listener behavior where local persistence precedes host delivery and host notify failures do not undo stored messages. The executor reports Go's host-notify status mutation branch explicitly: successful real delivery maps to clear, failed delivery maps to set error, and skipped dispatch (`shouldNotify=false`/no event) maps to unchanged; `execute_listener_notification_with_status` applies that update to `listener::Status` through the changed-only helpers and reports whether status changed. The system selectors start the Rust foreground listener, receive a real direct notification from the configured message-service v2 stack, verify SQLite/inbox persistence, and verify OpenClaw host-notify delivery for a direct incoming event. Mail, group, and group-state side-effect branches remain covered by focused Rust/Go contract tests rather than system selectors; mail system tests are deferred by plan. No dependency was added; both Rust source/test files are well below the default 1200-line cap. |
| `awiki-cli/internal/runtime/listener/server.go` local notification handler subset | `Supervisor.handleNotification` local composition boundary after injected secure normalization, including optional host sink semantics | `crates/awiki-cli/src/runtime/listener_notification_handler.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_notification_handler_contract.rs`; `tests_v2/cli/test_awiki_cli_runtime_listener_local.py`; `tests_v2/cli/test_awiki_cli_host_notify_openclaw_local.py` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_notification_handler_contract --locked`; adjacent executor/status tests; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/cli/test_awiki_cli_runtime_listener_local.py tests_v2/cli/test_awiki_cli_host_notify_openclaw_local.py -ra -q`; Go source parity for `handleNotification` and `dispatchHostNotification` | Adds a small local handler boundary for foreground `Supervisor` wiring. It composes the single-notification executor with mutable `listener::Status`, preserving Go order of secure-normalization injection before local effects and host-notify status mutation. `Some(host_notify_sink)` applies the executor's set/clear/unchanged status branch through the changed-only helpers; `None` models Go's `s.hostNotify == nil` early return by preserving local SQLite/contact effects while leaving `HostNotify.LastError` and the status file unchanged, even for otherwise-notifiable events. Rust contract tests cover failed delivery set, repeated same failure no-op write, successful delivery clear, ignored/skipped notifications unchanged, injected `received_at`, and nil-sink local persistence without status mutation. The system selectors exercise this handler through the real foreground listener for direct incoming storage and OpenClaw delivery. Mail/group/group-state live notification delivery, Hermes delivery, Windows named-pipe I/O, and platform service-manager execution remain outside this scoped system claim. No dependency was added; both new Rust files are well below the default 1200-line cap. |
| `awiki-cli/internal/runtime/listener/server.go` bridge dispatch helper subset | `handleBridgeRequest` bridge method-to-message RPC parameter dispatch plus outer session/RPC/mark-read control flow | `crates/awiki-cli/src/runtime/listener_bridge_dispatch.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_bridge_dispatch_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_bridge_dispatch_contract --locked`; Go source parity plus existing Go `TestHandleBridgeRequestPreservesSkipForHistoryAndGroupMessages` coverage for history/group messages | Translates the pure dispatch boundary inside Go `handleBridgeRequest`: bridge request method names map to the same direct/inbox/mark-read/group message builder calls, Go-like weak JSON coercions are preserved for string/int/bool/pointer fields, unsupported methods return `unsupported websocket bridge method: <method>`, `inbox.mark_read` returns the filtered message IDs needed for Go's post-success local DB side effect, and `group.create` takes the message service DID as explicit helper input to match Go's prior `fetchMessageServiceDID` step. Also translates the outer flow as injected decisions/actions: `ensureSession` errors return before current record/client reads; nil record or client returns `websocket session is not connected for identity <identity>`; `group.create` fetches service DID before building RPC and returns fetch errors before build; build errors return before `client.SendRPC`; send errors return before local mark-read; successful non-mark-read methods return the RPC result; successful `inbox.mark_read` plans best-effort local `MarkMessagesRead` after `SendRPC`. This does not implement `Supervisor`, real `ensureSession`, real `currentRecord`/`currentClient` locks, `client.SendRPC`, foreground WebSocket sessions, real `fetchMessageServiceDID`, SQLite mark-read mutation, host-notify dispatch, notification storage, or WebSocket crate selection. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` message-service DID helper subset | `fetchMessageServiceDID` injected execution wrapper, request shape, service DID extraction, and disconnected-session error text | `crates/awiki-cli/src/runtime/listener_service_did.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_service_did_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_service_did_contract --locked`; Go source parity for `fetchMessageServiceDID`; adjacent Go `go test ./internal/message -run TestHTTPTransportGetMessageServiceDIDUsesConfiguredOrCapabilities -count=1` for capability method context | Translates the service-DID helper boundary used before Go listener `group.create`: disconnected sessions return `websocket session is not connected for identity <identity>` before RPC, connected sessions send `anp.get_capabilities` with empty params through an injected RPC trait, `SendRPC` errors propagate before decoding, `service_did` is accepted only when the result field is a string, missing/empty/non-string results return `message service capabilities response is missing service_did`, and whitespace-only strings are preserved as non-empty like Go `stringValue`. This intentionally does not implement real `Supervisor.currentClient` lock ownership, real `WSClient.SendRPC`, configured `anp_service_did` precedence from the separate HTTP transport, core-binding capability params, real WebSocket sessions, or bridge dispatch ownership. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` ensure-session bootstrap helper subset | `ensureSession` identity-name normalization, current-identity fallback, existing-session fast path, new-session bootstrap actions, init result, and timeout decision | `crates/awiki-cli/src/runtime/listener_session_bootstrap.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_session_bootstrap_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_session_bootstrap_contract --locked`; Go source parity for `ensureSession`; adjacent session-loop/connect-session tests | Translates the pure decision boundary of Go `ensureSession`: requested identity name is trimmed; blank requests resolve `manager.Current()` and use the returned `IdentityName` without retrimming; current lookup errors return before session-map lookup; existing sessions return immediately without creating a session or starting the loop; new sessions plan context/cancel/init-channel creation, session-map insertion before loop start, `runSessionLoop` start, and waiting up to 15 seconds for the initial result; init success returns the new session; init errors return the new session plus the original error text; timeout returns the new session plus `websocket session bootstrap timed out for identity <identity>`. This does not implement real `identity.Manager`, session locks, real context/cancel/channel allocation, goroutine spawning, timer ownership, `runSessionLoop`, WebSocket clients, or message RPC execution. No dependency was added; both new Rust files are below the default 1200-line cap. |
| `awiki-cli/internal/runtime/listener/server.go` session-state helper subset | `refreshStatus`, `recordSessionError`, `setBridgeAvailable`, and pure `session` mark-connected/disconnected snapshot behavior | `crates/awiki-cli/src/runtime/listener_session_state.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_session_state_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_session_state_contract --locked`; Go source parity plus existing Go `TestSessionWarnings|TestHasDisconnectedSessions|TestMergeSavedRuntimeStatus` coverage for adjacent status behavior | Translates the pure listener session-state boundary before real runtime wiring: connected sessions set DID and clear `last_error`; disconnected sessions preserve existing `last_error` for nil/empty/cancel-equivalent errors and set it for non-empty errors; recorded session errors create a missing disconnected session with initial DID, but do not replace an existing session record/DID; snapshots use the session map key as `identity_name`; existing warning helpers consume the snapshot; and bridge availability reports changed-only transitions. Rust uses `BTreeMap` for deterministic helper snapshots, which is stronger than Go's map iteration order and used only for test stability. This does not implement `Supervisor`, locks, status file writes, identity manager lookup, reconnect loops, WebSocket clients, or message RPC execution. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` session-method helper subset | `currentClient`, `secureRPC`, `currentRecord`, `snapshot`, `markConnected`, `markDisconnected`, `closeCurrentClient`, and `signalInitial` | `crates/awiki-cli/src/runtime/listener_session_methods.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_session_methods_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_session_methods_contract --locked`; Go source parity for `server.go:1714-1784`; adjacent session-state/session-loop checks | Translates the small Go `session` method boundary before real session ownership is wired: current record/client/snapshot accessors expose stored state; `secureRPC` prefers the injected override before current client and returns none without either source; `markConnected` closes only a present different previous client, then stores record/client, marks connected, and clears `lastError`; nil record or nil client still marks connected like Go; `markDisconnected` closes and clears the current client, marks disconnected, and records only non-cancel errors; `closeCurrentClient` closes and clears only the current client and leaves `lastError` untouched; `signalInitial` sends exactly once and closes the init-channel analog. This models Go pointer comparison with injected client IDs and an injected context-cancel reason. It does not implement real `sync.RWMutex`, real `WSClient` pointers or `SendRPC`, real `context.Canceled` error matching, actual channel send/close, foreground session loop ownership, WebSocket transport, SQLite writes, or host-notify dispatch. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` supervisor shutdown helper subset | `Supervisor.Close` session cancel/client close, listener close, host notify close, database close ordering and error policy | `crates/awiki-cli/src/runtime/listener_supervisor_shutdown.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_supervisor_shutdown_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_supervisor_shutdown_contract --locked`; Go source parity for `Supervisor.Close`; adjacent session-loop/host-notify sink context | Translates the pure resource-shutdown order of Go `Supervisor.Close`: sessions are locked first; sessions with a non-nil cancel function are cancelled before each current client is closed; sessions are unlocked before listener, host notify, and database shutdown; nil listener/host notify/database resources are skipped; listener and host-notify close errors are ignored; database close runs last and is the only close error returned. This does not implement real mutex ownership, real session cancellation, `WSClient.Close`, socket/listener close, host sink close, SQLite close, or foreground runtime ownership. No dependency was added; both new Rust files are below the default 1200-line cap. |
| `awiki-cli/internal/runtime/listener/server.go` identity discovery helper subset | `startKnownSessions` and `watchNewIdentities` identity list/cancel/ensure/error/refresh control flow before real session bootstrap | `crates/awiki-cli/src/runtime/listener_identity_watch.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_identity_watch_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_identity_watch_contract --locked`; Go source parity for `startKnownSessions` and `watchNewIdentities`; adjacent session-state/session-loop tests | Translates the pure identity discovery control flow before foreground execution: known-session startup returns manager list errors directly, checks context cancellation before each identity, ensures every listed identity in order, records ensure errors with the original error text plus summary DID, refreshes status immediately after each recorded ensure error like Go `recordSessionError`, and refreshes status once more after a completed pass; new-identity watch uses a 3-second interval, stops on context cancellation, ignores manager list errors without refresh, skips identities already present in the session map, ensures new identities, treats identities ensured earlier in the same tick as present for duplicate summaries, records ensure errors, refreshes immediately after each recorded error, and refreshes status after a successful list pass. This does not implement real `identity.Manager`, `ensureSession`, session locks, ticker/context/goroutine ownership, status-file writes, WebSocket clients, or message RPC execution. No dependency was added; both new Rust files are below the default 1200-line cap. |
| `awiki-cli/internal/runtime/listener/server.go` session-loop control helper subset | `runSessionLoop` retry/backoff decisions, initial signal one-shot, connected/consume action ordering, `retryPublishSecurePrekeys`, `sleepWithContext`, and `minDuration` | `crates/awiki-cli/src/runtime/listener_session_loop.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_session_loop_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_session_loop_contract --locked`; Go source parity plus `go test ./internal/runtime/listener -run TestSessionLoopReconnectsAndStoresNotifications -count=1` | Translates the pure control-flow boundary around the Go listener session loop before foreground execution: initial reconnect delay is one second; failed connect and post-consume failures sleep the current delay, then double and cap at 30 seconds; cancelled sleeps exit without doubling; a successful connect resets delay to one second before marking connected; connected actions preserve Go order for mark-connected, initial success, status refresh, queued local notification flush, prekey retry start, unread secure inbox polling start, and notification consumption; consume completion preserves child-task cancel, client close, mark-disconnected, and status refresh order; session cancellation after consume exits before sleep; initial bootstrap signaling is one-shot like `sync.Once`; secure-prekey retry stops on empty warnings and otherwise logs joined warnings then retries after one second. This does not implement `Supervisor`, real goroutine/task spawning, real context/timer ownership, `connectSession`, `WSClient.Connect`, `consumeNotifications`, secure inbox polling, `PublishSecurePrekeys`, SQLite writes, host-notify dispatch, local bridge I/O, or new dependency selection. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` local notification queue and flush adapter subset | `queueLocalNotification` and `flushQueuedLocalNotifications` in-memory queue behavior plus injected queued-notification handler dispatch | `crates/awiki-cli/src/runtime/listener_local_notifications.rs`, `crates/awiki-cli/src/runtime/listener_local_notification_flush.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_local_notifications_contract.rs`, `crates/awiki-cli/tests/runtime_listener_local_notification_flush_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_local_notifications_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test runtime_listener_local_notification_flush_contract --locked`; Go source parity plus existing Go `TestDeliverLocalSecureAckInProcessPromotesPendingInitiatorSession` integration guard | Translates the local queue boundary before foreground listener execution: blank-after-trim recipient DID and nil notification are skipped; nonblank recipient DID is stored under the original untrimmed key; repeated enqueue preserves append order; nil target-session/current-record analogs and blank-after-trim current DID flush nothing; flush looks up the exact current DID key, returns a copy-equivalent vector, deletes that exact queue, and subsequent flushes return empty. The new injected flush adapter models Go's `flushQueuedLocalNotifications` execution bridge: nil target session returns without touching the queue; missing/blank current record DID leaves the queue intact; exact current DID flushes and deletes before dispatch; queued notifications are handed to a void callback in append order with the supplied target session context. This does not implement real `Supervisor`, locks, `session.currentRecord`, `targetSession.ctx`, owned `handleNotification`, secure ack encryption/decryption, queued outbox flushing, real WebSocket sessions, SQLite writes, or host-notify dispatch. No dependency was added; both new Rust files are below the default 1200-line cap. |
| `awiki-cli/internal/runtime/listener/server.go` session DID lookup helper subset | `activeSessionByDID`, `recordByDID`, and `hasRuntimeSessionForDID` selection behavior for local secure ack routing | `crates/awiki-cli/src/runtime/listener_session_lookup.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_session_lookup_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_session_lookup_contract --locked`; Go source parity plus existing Go `TestDeliverLocalSecureAckInProcessPromotesPendingInitiatorSession` integration guard | Translates the pure lookup control flow before foreground listener execution: blank-after-trim DID returns nil/false without manager calls; active-session lookup scans sessions in order and only compares each session's current in-memory record DID; record lookup returns nil for nil manager/list failure, scans manager summaries in order, loads only the first matching DID summary, and returns nil if that load fails; runtime-session lookup scans sessions in order, returns true on current-record DID match, skips fallback loads when manager is nil, otherwise loads by session identity name and ignores load failures until a loaded record DID matches. This does not implement `Supervisor`, locks, identity filesystem manager, secure ack encryption/decryption, queued local notification delivery, real WebSocket sessions, SQLite writes, or host-notify dispatch. No dependency was added. |
| `awiki-cli/internal/runtime/listener/contact_sync.go` | listener incoming contact sync helper | `crates/awiki-cli/src/runtime/listener_contact_sync.rs`, `crates/awiki-cli/src/runtime/mod.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli listener_contact_sync --locked` | Translates the helper-only boundary used by Go foreground listener direct/group incoming notifications: owner/sender DID trimming, empty/self skip, local contact handle short-circuit, contact-handle-binding fallback through shared store lookup, optional remote DID-to-handle fallback, listener handle normalization (`wba://` stripping and domain truncation), remote nil/blank/error behavior, `messaged=true`, direct/group source metadata, second-precision UTC timestamps, and shared store upsert/binding side effects. This does not implement Go `server.go` foreground session/message loop, WebSocket notification processing, host-notify dispatch, or runtime listener/local bridge I/O. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` helper subset | `messageRecordFromDirectIncoming`, `messageRecordFromMailNotification`, `messageRecordFromGroupIncoming`, `recordsFromGroupStateChanged` notification-to-store parsers | `crates/awiki-cli/src/runtime/listener_message_records.rs`, `crates/awiki-cli/src/runtime/mod.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli listener_message_records --locked`; Go `go test ./internal/runtime/listener -run 'TestMessageRecordFromDirectIncomingUsesProtocolFieldsOnly|TestMessageRecordFromDirectIncomingRejectsNonDirectNotification|TestMessageRecordFromMailNotificationBuildsSystemMessage|TestMessageRecordFromGroupIncomingUsesProtocolFieldsOnly|TestRecordsFromGroupStateChangedBuildsMemberAndSystemMessage' -count=1` | Translates the pure parser boundary for Go foreground listener direct, mail, group incoming, and group state-change notifications: exact method and params-object gating, required owner/sender/group/mailbox fields, direct/group/mail thread IDs, Go `text/plain` defaults, direct current-time sent-at fallback, direct payload/payload_b64u/body content selection, mail summary/title/metadata/source-kind generation and local fallback IDs, group accepted-at/created-at sent-at selection, group self-sent direction/read status, string-only group fallback message ID with numeric-seq pitfall preserved, server-seq parsing, E2EE flags for direct notifications, group state group/member/message record construction, membership status/content-type inference, and metadata limited to the Go helper scope (`params` or `body`) rather than the top-level JSON-RPC/server wrapper. This does not implement Go `handleNotification`, direct secure decryption, message/group storage side effects, host-notify dispatch, foreground WebSocket session loop, or local bridge I/O. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` JSON helper subset | `structToMap` marshal/unmarshal object fallback boundary used by local secure ack notification bodies | `crates/awiki-cli/src/runtime/listener_json_helpers.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_json_helpers_contract.rs`, `crates/awiki-cli/src/runtime/listener_secure_ack_in_process.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_json_helpers_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test runtime_listener_secure_ack_in_process_contract --locked`; Go probe for `json.Marshal` plus `json.Unmarshal(..., &map[string]any)` non-object/null behavior | Translates Go `structToMap`: marshal failures return an empty object, JSON object values preserve fields including nulls, non-object JSON values that fail map unmarshalling return an empty object, and JSON null is preserved as the nil-map equivalent. The local secure ack in-process planner now routes encrypted ack body assembly through this helper to preserve Go's `structToMap(ackBody)` boundary. This does not implement real ANP `DirectCipherBody` encryption, real `json.Marshal`/`json.Unmarshal` type tags from the Go SDK, recipient `ProcessIncoming`, foreground sessions, SQLite writes, WebSocket RPC, or host-notify dispatch. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` secure-direct pure helper subset | `isDirectSecureIncomingNotification`, `isSecureDirectWireContentType`, `secureNotificationFromMessageView`, `plaintextBodyToNotificationBody` | `crates/awiki-cli/src/runtime/listener_secure_notifications.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_secure_notifications_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_secure_notifications_contract --locked`; Go indirect guard `go test ./internal/runtime/listener -run 'TestHandleNotificationDecryptsSecureDirectIncomingAndStoresPlaintext|TestDeliverLocalSecureAckInProcessPromotesPendingInitiatorSession' -count=1` | Translates the pure secure-direct notification helper boundary used by Go foreground listener processing: direct incoming notification detection, exact secure direct wire content-type recognition, message-view to notification conversion, and plaintext-body whitelist conversion. Go currently covers these helpers through secure listener integration tests rather than standalone helper tests; Rust adds direct contract coverage for the helper-only boundary. This intentionally excludes WebSocket runtime processing, direct secure decrypt/ack, SQLite/storage side effects, host-notify dispatch, local bridge I/O, and new dependency selection. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` secure direct normalization planning subset | `normalizeDirectSecureNotification` early returns, decrypted notification rewrite, secure ack/init method rewrite, and injected secure-init ack side-effect ordering | `crates/awiki-cli/src/runtime/listener_secure_normalize.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_secure_normalize_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_secure_normalize_contract --locked`; Go source parity plus existing secure listener/message guards | Translates the pure planning boundary of Go `normalizeDirectSecureNotification`: non-secure notifications, missing current record, missing secure RPC, client construction errors, `ProcessIncoming` errors, non-decrypted states, and missing plaintext all keep the original notification with only the Go-attempted actions represented; decrypted plaintext rewrites `meta.content_type`, `params.body`, `secure_state`, `secure_wire_content_type`, and `secure_wire_body`; secure ack plaintext flushes queued secure outbox for the sender and rewrites method to `direct.secure.ack`; secure init plaintext rewrites method to `direct.secure.init`; secure init wire notifications with both session/message IDs plan local in-process ack first, optional network `SendJSON`, optional local ack delivery on network success, and peer queued-outbox flush in Go order. This does not implement real `message.NewSecureE2EEClientForRecord`, `ProcessIncoming`, `FlushQueuedSecureOutbox`, `SendJSON`, `deliverLocalSecureAckInProcess`, `deliverLocalSecureAck` side effects, file session stores, SQLite writes, host-notify dispatch, or new dependency selection. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` local secure ack in-process planning subset | `deliverLocalSecureAckInProcess` skip ladder, encrypted ack notification shape, recipient process fallback ladder, sender/recipient session save ordering, active-session flush/log, local queue, and network fallback planning | `crates/awiki-cli/src/runtime/listener_secure_ack_in_process.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_secure_ack_in_process_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_secure_ack_in_process_contract --locked`; Go source parity plus existing secure listener/message guards | Translates the pure control-flow boundary of Go `deliverLocalSecureAckInProcess`: nil sender returns false before recipient lookup; missing local recipient returns false; sender path/store/session lookup/encrypt failures skip in order; the encrypted ack notification uses sender DID, recipient target, ack message ID, `anp.direct.e2ee.v1`, `direct-e2ee`, and `application/anp-direct-cipher+json`; recipient client init failure skips; recipient `ProcessIncoming` error or non-`decrypted` state runs the same fallback ladder for recipient paths, store, session load, marshal/unmarshal, decrypt, and recipient save; sender session save happens after recipient processing; active recipient sessions optionally flush queued secure outbox when secure RPC is available, log flush warnings, log delivered, and return true; managed inactive runtime sessions queue a `direct.incoming` wrapper and return true; unmanaged recipients fall back to network and return false. This does not implement real ANP SDK file session stores, E2EE encrypt/decrypt, `message.NewSecureE2EEClientForRecord`, `ProcessIncoming`, `FlushQueuedSecureOutbox`, `activeSessionByDID`, `hasRuntimeSessionForDID`, local queue mutation, SQLite writes, WebSocket RPC, host-notify dispatch, or new dependency selection. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` local secure ack delivery helper subset and `awiki-cli/internal/message/secure_control.go` ack payload builder | `deliverLocalSecureAck` active-session/body/message-id/notification dispatch planning and `BuildSecureAckPayload` trimming | `crates/awiki-cli/src/runtime/listener_secure_ack_delivery.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_secure_ack_delivery_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_secure_ack_delivery_contract --locked`; Go source parity plus existing secure listener/message guards | Translates the pure local-ack notification builder boundary before foreground listener execution: inactive target sessions skip without inspecting body; missing, non-object, or empty `ackResult.body` skips; `message_id` accepts only a nonblank string and otherwise falls back to the caller-provided ack ID; nonblank message IDs preserve Go's original untrimmed value; delivered acks build a `direct.incoming` notification with sender DID, target agent DID, `anp.direct.e2ee.v1`, `direct-e2ee`, `application/anp-direct-cipher+json`, and the ack body, then plan `handleNotification`. Also preserves `BuildSecureAckPayload` system type and trimmed session/message IDs. This does not implement `deliverLocalSecureAckInProcess`, E2EE encrypt/decrypt, file session stores, `ProcessIncoming`, `FlushQueuedSecureOutbox`, queue mutation, real active-session lookup, host-notify dispatch, SQLite writes, or new dependency selection. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` peer queued secure outbox flush trigger subset | `flushPeerQueuedSecureOutbox` session snapshot scan, first owner-session selection, secure-RPC nil return, flush trigger, and warning log planning | `crates/awiki-cli/src/runtime/listener_secure_outbox_flush.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_secure_outbox_flush_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_secure_outbox_flush_contract --locked`; Go source parity plus existing secure listener/message guards | Translates only the Supervisor-level trigger boundary of Go `flushPeerQueuedSecureOutbox`: given a sessions snapshot, nil current records and nonmatching owner DIDs are skipped; owner DID comparison is exact without trimming; the first matching owner session with nil secure RPC returns without scanning later sessions; the first matching owner session with secure RPC plans one `FlushQueuedSecureOutbox` call for the peer DID, captures injected warnings, logs `listener queued secure outbox flush owner_did=%s peer_did=%s warnings=%v`, and returns. This does not implement Go map iteration ordering beyond the supplied snapshot order, real session locks, real `secureRPC`, `message.FlushQueuedSecureOutbox` database/E2EE sending, `WSClient.SendRPC`, file session stores, SQLite writes, host-notify dispatch, or new dependency selection. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` secure inbox poll helper subset | `pollUnreadSecureDirectInbox` initial sync, ticker interval, tick sync ordering, and context-cancel exit before real secure inbox/history polling | `crates/awiki-cli/src/runtime/listener_secure_inbox_poll.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_secure_inbox_poll_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_secure_inbox_poll_contract --locked`; Go source parity plus existing secure listener integration guards | Translates the pure polling control-flow boundary before foreground listener execution: startup runs `syncUnreadSecureDirectInbox` then `syncPendingConfirmationSecureHistory` before starting the ticker; the ticker interval matches current Go source at 2 seconds; each tick runs the two sync helpers in the same order and continues; context cancellation stops the ticker and exits without another sync. This does not implement `WSClient.SendRPC`, real ticker/context ownership, unread inbox RPC, pending history RPC, secure replay filtering, SQLite lookup, `handleNotification`, secure decrypt/ack, host-notify dispatch, local bridge I/O, or new dependency selection. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` secure inbox/history sync planning subset | `syncUnreadSecureDirectInbox` and `syncPendingConfirmationSecureHistory` RPC planning, timeout/cancel ordering, and replay-candidate handoff | `crates/awiki-cli/src/runtime/listener_secure_sync.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_secure_sync_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_secure_sync_contract --locked`; Go source parity plus existing secure listener/message guards | Translates the pure sync planning boundary before foreground listener execution: missing current record skips; unread sync plans `inbox.get` with a 15-second context, `InboxRequest{Scope: "direct", UnreadOnly: true, Limit: 100}` and Go-compatible params where `scope`/`unread_only` are not serialized by `BuildInboxRPCParams`; unread sync always cancels the context after the planned RPC/replay work, matching Go `defer cancel`; pending history skips when no record or no peers; each pending peer creates a 15-second context, build errors cancel and continue, successful builds plan `direct.get_history` with limit 50, cancel after `SendRPC`, and only RPC successes hand messages to the existing secure replay filters. This does not implement real `WSClient.SendRPC`, context ownership, SQLite `GetMessageByID`, `handleNotification`, secure decrypt/ack, host-notify dispatch, local bridge I/O, or new dependency selection. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` secure replay filter helper subset | secure backlog/history replay filtering before `secureNotificationFromMessageView` in `syncUnreadSecureDirectInbox` and `syncPendingConfirmationSecureHistory` | `crates/awiki-cli/src/runtime/listener_secure_replay.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_secure_replay_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_secure_replay_contract --locked`; Go source parity plus existing Go secure-listener integration guards | Translates the pure replay filtering boundary before foreground listener execution: only exact secure direct wire content types pass; non-object message items are skipped; pending history skips self-sent secure messages before store lookup while unread inbox does not; dedupe lookup owner uses `receiver_did` or falls back to the local DID while notification conversion still requires the original `receiver_did`; store lookup outcomes are tri-state, with existing rows and non-NoRows errors skipped and NoRows continuing to conversion; malformed secure message views are skipped after lookup; accepted candidates preserve input order and reuse the already translated secure notification conversion. This does not implement `WSClient.SendRPC`, inbox/history RPC params, SQLite `GetMessageByID`, `handleNotification`, secure decrypt/ack, foreground polling, host-notify dispatch, local bridge I/O, or new dependency selection. No dependency was added. |
| `awiki-cli/internal/runtime/listener/server.go` pending secure-session scan helper subset | `pendingConfirmationPeerDIDs`, `readJSONFile` | `crates/awiki-cli/src/runtime/listener_secure_sessions.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_listener_secure_sessions_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_listener_secure_sessions_contract --locked`; Go fallback guard when no helper-specific Go tests exist: `go test ./internal/message ./internal/runtime/listener -run 'TestServiceSendSecureDirectUsesP5KeyServiceTargetAndPersistsPendingSession|TestServiceSendSecureDirectQueuesFollowUpWhilePendingConfirmation|TestHandleNotificationDecryptsSecureDirectIncomingAndStoresPlaintext|TestDeliverLocalSecureAckInProcessPromotesPendingInitiatorSession' -count=1` | Translates only the local pending-confirmation secure-session scan helpers used by Go `syncPendingConfirmationSecureHistory`: missing manager or blank identity returns no peers; identity path lookup or glob errors return no peers; `p5-e2ee-sessions/*.json` files are read as JSON; malformed/unreadable files are skipped; only `status="pending-confirmation"` entries with nonblank `peer_did` are returned; duplicate peer DIDs are suppressed while preserving first-seen glob order. The helper slice does not implement `syncPendingConfirmationSecureHistory`, WebSocket/RPC history fetch, secure decrypt/ack, SQLite/storage side effects, host-notify dispatch, local bridge I/O, or new dependency selection. No dependency was added. |
| `awiki-cli/internal/runtime/listener/host_notify.go` pure helper subset | `NormalizeHostNotification`, `ApplyHostNotificationHandles`, host notification data envelopes, fallback IDs, and mail/group event text inference | `crates/awiki-cli/src/runtime/host_notify.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_host_notify_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_host_notify_contract --locked`; Go `go test ./internal/runtime/listener -run 'TestNormalizeHostNotificationDirectIncomingKeepsMinimalFields|TestNormalizeHostNotificationGroupIncomingOmitsPayloadBody|TestNormalizeHostNotificationMailNotificationBuildsMailEvent|TestNormalizeHostNotificationGroupStateChangedInfersEventType' -count=1` | Translates the pure host-facing notification envelope boundary before foreground dispatch: `version=1.0`, exact accepted methods, UTC RFC3339 `received_at`, direct/mail/group/group-state data structs with Go JSON field omissions, minimal direct fields with `source_kind=im`, mail event reuse of `im.message.received` with `source_kind=mail`, group payload omission, string-like group sequence handling for host IDs, SHA-256 generated `hostevt-<16 hex>` fallback IDs, mail text fallback ordering, group-state event-type inference, required-field rejection, and handle enrichment for direct/group events only. This does not implement `newHostNotifySink`, file/log/OpenClaw/Hermes delivery, Go `handleNotification`, host-notify dispatch, message/group storage side effects, foreground WebSocket session loop, or local bridge I/O. No dependency was added; it reuses existing `serde`, `serde_json`, `sha2`, and `time`. |
| `awiki-cli/internal/runtime/listener/openclaw_host_notify.go` | OpenClaw host-notify hook request/event builders plus route-based delivery sink | `crates/awiki-cli/src/runtime/openclaw_host_notify.rs`, `crates/awiki-cli/src/runtime/openclaw_webhook.rs`, `crates/awiki-cli/tests/runtime_openclaw_host_notify_contract.rs`; `tests_v2/cli/test_awiki_cli_host_notify_openclaw_local.py` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_openclaw_host_notify_contract --locked`; `cargo +1.79.0 test -p awiki-cli openclaw_webhook --locked`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/cli/test_awiki_cli_host_notify_openclaw_local.py::test_awiki_cli_host_notify_openclaw_local_probe_succeeds -q`; Go `go test ./internal/runtime/listener -run 'Test(BuildOpenClawHookRequestIncludesChannelDelivery|BuildOpenClawEventTextUsesMainAgentSessionFormat|BuildOpenClawEventTextUsesMailFormat|BuildOpenClawHookRequestIncludesMailPrompt|NewOpenClawHostNotifySinkRejectsNonLoopbackHookURL|NewOpenClawHostNotifySinkAllowsEmptyToken|OpenClawHostNotifySinkNotifyUsesRouteRegistry)' -count=1` | Adds the OpenClaw delivery sink constructor/notify/close path on top of the existing formatting helpers: effective settings resolution, loopback hook URL validation, reusable Rustls/std webhook request sending, fixed `AWiki` hook name, route registry loading, no-route error, per-route hook payloads, optional bearer token, HTTP response validation, success if any route is accepted, and Go-shaped aggregate failure text (`channel=<channel> to=<to>: <err>` joined by `; `). Focused local-server contract tests cover payload/token delivery, partial success, all-route failure aggregation, missing routes, and constructor URL validation. The awiki-system-test OpenClaw probe starts `runtime listener run`, sends a real direct message, waits for SQLite cache persistence, and verifies a loopback webhook receives the OpenClaw payload with bearer token, `deliver=true`, channel/target, and AWiki prompt while the fake legacy OpenClaw binary remains unused. This is a scoped system claim for the direct-message OpenClaw listener path; mail/group/group-state OpenClaw prompts and generic noop/log/file/Hermes sink dispatch remain separate rows. No dependency was added. |
| `awiki-cli/internal/runtime/listener/host_notify.go` sink/dispatcher subset | `HostNotifySink`, noop/log/file sinks, `newFileHostNotifySink`, and `newHostNotifySink` construction for noop/log/file/openclaw/hermes | `crates/awiki-cli/src/runtime/host_notify_sink.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_host_notify_sink_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_host_notify_sink_contract --locked`; Go source parity plus later listener dispatch tests pending | Translates the sink construction layer from Go `host_notify.go` without foreground listener wiring: noop/log close/notify behavior, file sink blank path error, parent directory creation with Go-style `0700` mode for new directories, append/create `0600` file open for new files, newline-delimited JSON writes with sync, idempotent close, status construction, disabled-host-notify returning noop with status, blank sink normalization to `log`, `webhook` alias normalization to `hermes`, unsupported sink error text, Hermes constructor dispatch, and OpenClaw constructor dispatch with effective hook URL status refresh. This does not implement Go `handleNotification`, foreground WebSocket session loop, SQLite storage side effects, local bridge I/O, or runtime listener service execution. No dependency was added. |
| `awiki-cli/internal/runtime/listener/hermes_host_notify.go` | Hermes host-notify URL validation, HMAC request signature, sink construction, HTTP POST delivery, and response/error mapping | `crates/awiki-cli/src/runtime/hermes_host_notify.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/tests/runtime_hermes_host_notify_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_hermes_host_notify_contract --locked`; Go `go test ./internal/runtime/listener -run 'Test(NewHermesHostNotifySinkRejectsInvalidNotifyURL|HermesHostNotifySinkNotifySignsRequest)' -count=1` | Translates Hermes host-notify sink behavior: required notify URL and secret errors, HMAC-SHA256 over `timestamp + "." + raw_json_body`, lowercase hex output, `sha256=` signature header value, Go header names, `http`/`https` notify URL validation with host requirement and Go-like parse rejection for malformed host/port inputs, config-file secret precedence (`runtime.host_notify.hermes.secret`, then legacy `runtime.host_notify.webhook.secret`), env fallback precedence (`AWIKI_HOST_NOTIFY_HERMES_SECRET`, then `AWIKI_HOST_NOTIFY_WEBHOOK_SECRET`), whitespace trimming, config read-error fallback, 15-second POST timeout cap, JSON content type, 2xx success, non-2xx body trimming, empty-body status errors, and `close` no-op. It also corrects the Hermes config view legacy env metadata to Go's `AWIKI_HOST_NOTIFY_WEBHOOK_SECRET`. This does not wire Hermes delivery into Go `newHostNotifySink`/`handleNotification`, foreground WebSocket session loop, SQLite storage side effects, host-notify dispatch, or local bridge I/O. No dependency was added; it reuses existing `sha2`, the hand-written config parser, and the existing Rustls/std `transportcfg` HTTP client. |
| `awiki-cli/internal/runtime/openclawnotify/config.go` | OpenClaw hook URL/token resolution, OpenClaw JSON config probing, hook path normalization, and redaction | `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/src/config/mod.rs`, `crates/awiki-cli/tests/runtime_contract.rs`, `crates/awiki-cli/tests/runtime_openclaw_config_contract.rs`; `tests_v2/runtime/test_runtime_cli.py` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test runtime_openclaw_config_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test runtime_contract host_notify_openclaw --locked`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/runtime/test_runtime_cli.py::test_runtime_host_notify_openclaw_config_probe_uses_local_openclaw_json -q`; Go `go test ./internal/runtime/openclawnotify -run 'TestProbeGatewayPortReadsEnvBeforeConfig|TestProbeGatewayPortReadsOpenClawConfig|TestResolveSettingsUsesAutoDetectedHookURLWhenConfigHookURLUnset|TestResolveSettingsPrefersConfigTokenOverEnvironmentAndOpenClawConfig|TestResolveSettingsPrefersEnvironmentTokenOverOpenClawConfig' -count=1` | Supports Go-style loopback hook URL validation, explicit AWiki config-file hook URL output, auto-detected hook URL output with `OPENCLAW_GATEWAY_PORT`, OpenClaw JSON config probing through `OPENCLAW_CONFIG_PATH` or `~/.openclaw/openclaw.json`, env-port precedence over JSON port, Go positive-`int` port metadata/URL behavior, typed JSON parse failure fallback, hooks path normalization and `/agent` endpoint construction, `OPENCLAW_HOOK_TOKEN` source detection, config-file token precedence, OpenClaw config token fallback, and token redaction in success/error output. The awiki-system-test selector rewrites an isolated runtime config to use OpenClaw with no explicit hook URL, supplies a temporary `OPENCLAW_CONFIG_PATH`, verifies auto-detected hook URL/path/token metadata and redaction through the real Rust subprocess, then verifies `OPENCLAW_GATEWAY_PORT` takes precedence over the JSON port while keeping the JSON path/token. No OpenClaw process, listener, message-service, user-service, mail-service, or service-manager dependency is required. |
| `awiki-cli/internal/runtime/openclawnotify/routes.go`, `awiki-cli/internal/runtime/openclawnotify/webhook.go` | OpenClaw host-notify route registry load/add/remove/write, session-key parsing, and route-add confirmation webhook | `crates/awiki-cli/src/runtime/openclaw_routes.rs`, `crates/awiki-cli/src/runtime/openclaw_webhook.rs`, `crates/awiki-cli/src/runtime/mod.rs`, `crates/awiki-cli/src/app/runtime_handlers.rs`; `tests_v2/runtime/test_runtime_cli.py` | `system_verified` | `cargo +1.79.0 test -p awiki-cli openclaw_routes --locked`; `cargo +1.79.0 test -p awiki-cli openclaw_webhook --locked`; `cargo +1.79.0 test -p awiki-cli --test runtime_contract host_notify_openclaw --locked`; `cargo +1.79.0 test -p awiki-cli --test runtime_contract --locked`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/runtime/test_runtime_cli.py::test_runtime_host_notify_openclaw_route_add_list_remove_uses_local_webhook -q`; Go `go test ./internal/runtime/openclawnotify -run 'TestResolveRouteInput|TestAddAndRemoveRoutePersistRegistry|TestLoadRoutesMissingFileReturnsEmpty' -count=1` | Translates local `openclaw.host-notify.routes.json` path resolution, route normalization, `agent:...:<channel>:<type>:<to>` session-key parsing, duplicate suppression, missing-file empty load, JSON pretty registry shape, atomic temp-file write, Unix file mode, and directory sync via `durablefs`. `host_notify_config_view.routes` now reads persisted routes. Newly added non-dry-run routes send the Go-compatible confirmation JSON (`AWiki`, `wakeMode=now`, `deliver=true`, route channel/to, optional bearer token) through the existing Rustls/std HTTP client using explicit AWiki config hook URL, `OPENCLAW_GATEWAY_PORT`, or OpenClaw JSON config probe settings; duplicate routes do not resend; delivery failures are warnings after persistence. The awiki-system-test selector runs the real Rust subprocess against an isolated runtime plus loopback webhook capture server, verifies dry-run route normalization, session-key parsing, route persistence/list/config-view exposure, bearer token forwarding without stdout/stderr leakage, Go-shaped confirmation payload and run ID handling, duplicate add without a second POST, and remove/list-empty behavior. No OpenClaw process, listener, message-service, user-service, mail-service, or service-manager dependency is required. No dependency was added. |
| `awiki-cli/internal/store/helpers.go` | shared store helper primitives for time, thread IDs, owner/credential trimming, nullable coercions, metadata, bools, defaults, and generated local IDs | `crates/awiki-cli/src/store/helpers.rs` | `unit_verified` | `crates/awiki-cli/tests/store_helpers_contract.rs`; `crates/awiki-cli/tests/store_import_contract.rs`; `crates/awiki-cli/tests/store_rebind_contract.rs`; `go test ./internal/store` | Extracts the Go-equivalent helpers used by legacy import and rebind into a split Rust module before the larger recover-merge translation. This keeps `store/import.rs` at 1117 lines and avoids creating another oversized store file. No dependency was added. |
| `awiki-cli/internal/store/open.go` | SQLite open/configuration | `crates/awiki-cli/src/store/open.rs` | `unit_verified` | `/tmp/awiki_rusqlite_probe`; `cargo +1.79.0 test -p awiki-cli --locked`; `cargo +1.79.0 tree --workspace --locked` | User approved `rusqlite + bundled` for SQLite on 2026-05-14. This avoids host SQLite at runtime but compiles bundled SQLite C code through `libsqlite3-sys`; no OpenSSL/native-tls path was present in the probe. |
| `awiki-cli/internal/store/schema.go` | SQLite schema, indexes, views, schema version | `crates/awiki-cli/src/store/schema.rs` | `unit_verified` | `/tmp/awiki_rusqlite_probe`; `cargo +1.79.0 test -p awiki-cli --locked`; `cargo +1.79.0 run --bin xtask --locked -- check-structure` | Schema version 12 and SQL features are translated in a dedicated file. Probe verified partial unique indexes, `ON CONFLICT`, `sqlite_master`, and `ROW_NUMBER() OVER (...)`; Rust integration tests verify `init` creates queryable schema. |
| `awiki-cli/internal/store/dao.go` | Safe debug SQL execution | `crates/awiki-cli/src/store/query.rs`, `crates/awiki-cli/src/app.rs` | `system_verified` | `crates/awiki-cli/tests/core_contract.rs`; `tests_v2/debug/test_debug_cli.py::test_debug_db_query_rejects_unsafe_sql_and_supports_table_output` | Safe SQL behavior matches the verified Go contract: empty/multiple statements rejected, DROP/TRUNCATE rejected, DELETE requires WHERE, SELECT returns rows, non-SELECT returns `rows_affected`. |
| `awiki-cli/internal/cli/debug.go`, `awiki-cli/internal/cmdmeta/catalog.go` | `debug db handle-history <HANDLE>` local DID history inspection | `crates/awiki-cli/src/app/debug_handlers.rs`, `crates/awiki-cli/src/cli/mod.rs`, `crates/awiki-cli/src/cmdmeta/mod.rs`, `crates/awiki-cli/src/store/contacts.rs`, `crates/awiki-cli/tests/debug_contract.rs` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test debug_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test core_contract --locked`; Go `go test ./internal/cli -run 'TestNormalizeDebugHandleTrimsPrefixesAndDomains|TestBuildHandleHistoryOwnersAggregatesByOwner' -count=1`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/debug/test_debug_cli.py::test_debug_db_handle_history_reads_contact_bindings -q`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/debug/test_debug_cli.py -ra -q` | Translates the Go local SQLite debug handler and catalog entry: exactly-one-handle validation, handle trim/lowercase/`wba://`/domain normalization, empty-normalized-handle error, `contact_handle_bindings` parameterized query ordered by owner/currentness/last-seen time, `sql: no rows in result set` not-found mapping, Go-shaped `database_file`, `handle`, `rows`, and owner aggregate output with current/historical DIDs. The `awiki-system-test` selector seeds isolated local SQLite rows through the subprocess runtime, verifies normalized full-handle lookup, row ordering/currentness, owner aggregation, and the no-row `not_found` envelope. This slice reuses the approved bundled SQLite lane and adds no dependency. |
| `awiki-cli/internal/store/dao.go` | E2EE outbox DAO subset: queue, mark sent/failed, status/failure update by ID, get, and list | `crates/awiki-cli/src/store/e2ee_outbox.rs`, `crates/awiki-cli/src/store/mod.rs`, `crates/awiki-cli/tests/store_e2ee_outbox_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test store_e2ee_outbox_contract --locked`; Go `go test ./internal/store -count=1` | Translates the local SQLite E2EE outbox DAO functions needed by queued secure direct execution: `QueueE2EEOutbox`, `MarkE2EEOutboxSent`, `MarkE2EEOutboxFailed`, `UpdateE2EEOutboxStatus`, `SetE2EEOutboxFailureByID`, `GetE2EEOutbox`, and `ListE2EEOutbox`. Preserves Go SQL shapes, owner-vs-credential fallback branches, owner/credential trimming, raw peer/status/error-code values, default outbox IDs, default `original_type=text`, default `local_status=queued`, optional string/int/null coercions, metadata blank-to-null behavior, sent attempt increment and failure-field clearing, failed/status COALESCE behavior, no-row `NotFound` shape, JSON row mapping, and `updated_at DESC` list ordering. This does not wire message secure execution, ANP SDK file session stores, E2EE clients, WebSocket RPC, or CLI secure commands. No dependency was added beyond the existing approved `rusqlite + bundled` store lane. |
| `awiki-cli/internal/store/rebind.go`, `awiki-cli/internal/store/dao.go` | local identity owner-DID rebind and E2EE cleanup helpers | `crates/awiki-cli/src/store/rebind.rs` | `system_verified` | `crates/awiki-cli/tests/store_rebind_contract.rs`; `crates/awiki-cli/tests/identity_replace_did_live_contract.rs`; `go test ./internal/store` | Translates the post-identity replacement helpers: missing DB soft no-op, Go's five-key missing-DB store count shape, six-table `UPDATE OR IGNORE` owner rebind transaction, pre-update row counts, owner trim/no-op rules, and `e2ee_outbox`/`e2ee_sessions` old-owner cleanup. The live `id replace-did` CLI now calls this helper after successful identity replacement, exposes `store_rebind` and `e2ee_cleanup`, and preserves partial count output plus warning behavior when local SQLite rebinding fails. No dependency was added. |
| `awiki-cli/internal/store/recover_merge.go` | recovered-handle local state merge and E2EE cleanup | `crates/awiki-cli/src/store/recover_merge.rs`, `crates/awiki-cli/src/store/recover_merge/{records,sql}.rs` | `system_verified` | `crates/awiki-cli/tests/store_recover_merge_contract.rs`; `crates/awiki-cli/tests/identity_recover_live_contract.rs`; focused `tests_v2/id/test_identity_cli.py::test_id_register_resolve_profile_bind_and_recover_flow`; `go test ./internal/store` | Translates Go `MergeRecoveredHandleLocalState` as a store-only module split under the 1200-line default: orchestration, record normalization/merge algebra, and SQL helpers are separate files. Covers missing DB no-op, existing empty DB schema creation, normalized old-owner filtering/deduplication, single-transaction table merge order, pre-merge source counts, old-owner row deletion, message thread/self-DID remapping, contact/current-handle normalization by latest timestamp then DID DESC, global `relationship_events.event_id` conflict behavior, group/member self-DID remapping, and old-owner-only E2EE cleanup. The live `id recover --otp` CLI now calls this helper before final promotion and exposes `store_merge_counts` plus `e2ee_cleanup_counts`. Non-dry-run `id replace-did` remains a later identity-service slice. No dependency was added. |
| `awiki-cli/internal/store/import.go` | Legacy SQLite scan/import | `crates/awiki-cli/src/store/import.rs`, `crates/awiki-cli/src/app.rs` | `system_verified` | `crates/awiki-cli/tests/store_import_contract.rs`; `crates/awiki-cli/tests/core_contract.rs::debug_db_import_v1_supports_dry_run_and_missing_path_errors`; Go `go test ./internal/store`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/debug/test_debug_cli.py::test_debug_db_import_v1_imports_seeded_legacy_sqlite_without_message_service -q`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/debug/test_debug_cli.py -ra -q` | Translates Go's legacy DB path scan, missing-file soft scan, schema/table discovery, identity-owner lookup boundary, pre-v6 owner guard, table import order, missing-table skip behavior, row-level owner/thread/default normalization, required-row skip rules, message/outbox/contact/group/member/event/session write semantics, and contact handle-binding side effect. The deterministic `awiki-system-test` selector creates a local v6 legacy `database/awiki.db` with `messages`, `contacts`, and empty `e2ee_outbox`, imports a local flat legacy identity first so `credential_name=legacy` can infer `owner_did`, then verifies the `debug db import-v1` report, imported message owner/thread/content-type/credential fields through `debug db query`, and imported handle binding through `debug db handle-history` without message-service or user-service dependencies. `store/import.rs` is now 1117 lines after shared helper extraction, under the 1200-line default and still intentionally monitored because it is a direct file-level Go translation. |
| `awiki-cli/internal/identity/anp_service.go` | default ANP service endpoint/DID helpers, service endpoint/DID validation, and agent ANPMessageService construction | `crates/awiki-cli/src/identity/did.rs`, `crates/awiki-cli/tests/identity_contract.rs`; `tests_v2/id/test_identity_cli.py` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test identity_contract --locked`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/id/test_identity_cli.py::test_id_create_generates_default_anp_message_service -q`; Go `go test ./internal/identity -run 'TestGenerateIdentity|TestGenerateIdentityRejectsLoopbackANPServiceEndpoint|TestGenerateIdentityRejectsNonBareANPServiceDID' -count=1` | Ports the pure Go helper boundary into the existing DID module and makes `generate_identity` reuse it. Preserves `/anp-im/rpc`, `did:wba:<host>`, endpoint scheme/hostname/localhost/loopback validation, bare-domain `did:wba` validation, `#message` service id at the helper boundary, `ANPMessageService` type, trimmed endpoint/serviceDid, profiles `anp.core.binding.v1`, `anp.direct.base.v1`, `anp.attachment.v1`, and `securityProfiles=["transport-protected"]`. The awiki-system-test selector runs real Rust `id create` in an isolated workspace with `did_domain=tenant.example`, verifies the persisted DID document contains the SDK-expanded `<identity-did>#message` service id, `ANPMessageService`, default endpoint `https://tenant.example/anp-im/rpc`, default service DID `did:wba:tenant.example`, expected profiles, and `securityProfiles=["transport-protected"]`, without user-service, message-service, mail-service, listener, or service-manager dependencies. This does not implement PEM compatibility conversion, live service calls, or new ANP SDK features. No dependency was added. |
| `awiki-cli/internal/identity/key_compat.go` | legacy/private identity PEM compatibility migration on identity load | `crates/awiki-cli/src/identity/key_compat.rs`, `crates/awiki-cli/src/identity/store.rs`, `crates/awiki-cli/tests/identity_key_compat_contract.rs`, local `../anp/rust/src/keys.rs` compatibility API; `tests_v2/id/test_identity_cli.py` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test identity_key_compat_contract --locked`; `AWIKI_CLI_UNDER_TEST=rust AWIKI_CLI_RUST_REPO=/home/ecs-user/awiki-space/awiki-cli-rs2 AWIKI_CLI_UPDATE_CACHE_ONLY=1 uv run pytest tests_v2/id/test_identity_cli.py::test_id_current_migrates_legacy_anp_private_key_pems -q`; Go `go test ./internal/identity -run 'TestManagerLoadMigratesLegacyANPPrivateKeysToPKCS8|TestReplaceDIDConvertsLegacyANPK1KeyWhenJWTMissing' -count=1` | Translates Go's pre-load compatibility migration for key-1, E2EE signing, and E2EE agreement private PEM files. Missing files remain a no-op; empty/invalid/unsupported PEM errors use the `authentication required` boundary; supported legacy labels (`ANP ED25519 PRIVATE KEY`, `ANP X25519 PRIVATE KEY`, `ANP SECP256R1 PRIVATE KEY`, `ANP SECP256K1 PRIVATE KEY`) and SEC1 EC private keys are normalized to standard `PRIVATE KEY` PKCS#8 before callers read stored identities. The awiki-system-test selector seeds an isolated identity, rewrites its three private key files to legacy ANP PEM labels, runs real Rust `id current`, and verifies the summary plus filesystem rewrite to standard `PRIVATE KEY` PEM without user-service, message-service, mail-service, listener, or service-manager dependencies. The standard ANP runtime parser still rejects legacy ANP labels; compatibility stays in the migration path. No dependency was added. |
| `awiki-cli/internal/identity/types.go`, `layout.go`, `store.go`, `did.go`, `handle_input.go`, `readiness.go`, `public.go`, `legacy.go`, `service.go` local paths | local identity store, summaries, handle input normalization/completion, user readiness, e1 DID generation, legacy credential import, register/bind/profile/resolve/recover/replace-did live service helpers | `crates/awiki-cli/src/identity/{types,layout,store,did,handle_input,legacy,recover,replace_did,service}.rs` | `system_verified` | `crates/awiki-cli/tests/identity_contract.rs::identity_handle_input_helpers_match_go_contract`; `crates/awiki-cli/tests/identity_contract.rs` `full_handle` filter; `cargo +1.79.0 test -p awiki-cli --test identity_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test identity_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test identity_register_email_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test identity_recover_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test identity_replace_did_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test identity_wire_contract --locked`; `cargo +1.79.0 test -p awiki-cli --locked`; focused `tests_v2/id` and `tests_v2/page` selectors listed above | Translated as split files under the 1200-line default. `handle_input.rs` centralizes Go `NormalizeHandleInput`, `CompleteBareHandle`, stored handle field derivation, DID-domain fallback for handle DIDs, default string helper behavior, and `Manager.Load` persisted handle/full-handle backfill; `msg` and non-E2EE `group` dry-run handlers now share `identity::complete_bare_handle` instead of carrying divergent local bare-handle completion logic. Live register now covers phone OTP send, phone+OTP `did-auth.register`, email activation send without local writes, email status wait/polling, already-verified skip-send behavior, and email-backed final registration. Live refresh-token uses the shared Rustls/authsdk `EnsureJWT` path without seeding stored bearer and persists the returned JWT through `Manager::update_jwt`. Live bind/profile/resolve now reuse the shared auth session and identity wire builders for authenticated phone/email bind REST calls, authenticated `update_me`/`get_me`, public `get_public_profile`, handle `lookup`, and DID profile `resolve`, with fake-server coverage for payload mapping, local display-name update, public lookup sequencing, warning behavior, phone OTP/verify, email send, and email already-verified completion. Live recover covers OTP send, recovered handle DID generation, backup/manifest snapshots, temp identity staging, promotion, active/default identity updates, and Go-shaped final public output. Live replace-did covers handle-path e1 DID generation, replacement backup/manifest, authenticated `replace_did`, no-JWT bootstrap, local identity replacement, old-directory removal, danger warning, and CLI-owned store rebind/cleanup output. Service profile timeouts and shared RPC/JWT trace phases now flow through the shared Rustls HTTP request path; deeper handle lookup and local DB trace wiring remain separate trace-depth work. No dependency was added. |
| `awiki-cli/internal/identity/recover.go`, `awiki-cli/internal/identity/service.go`, `awiki-cli/internal/cli/id.go`, `awiki-cli/internal/cmdmeta/catalog.go` recover flow | handle-recovery plan builder, live OTP send, full `recover_handle` staging/finalization, and public `id recover` command surface | `crates/awiki-cli/src/identity/recover.rs`, `crates/awiki-cli/src/identity/{mod,types,service}.rs`, `crates/awiki-cli/src/app/id_recover_handlers.rs`, `crates/awiki-cli/src/{app,cli,cmdmeta}/`, `crates/awiki-cli/tests/{identity_contract,identity_recover_live_contract}.rs` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test identity_recover_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test identity_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test identity_wire_contract --locked`; full `cargo +1.79.0 test -p awiki-cli --locked`; focused `awiki-system-test` id recover selector; Go `go test ./internal/cli ./internal/identity ./internal/store ./internal/authsdk ./internal/cmdmeta -run 'Test.*Recover|TestRecoverStagesAndFinalizesSameHandleLiveIdentities|TestMergeRecoveredHandleLocalState|Test.*Bind|Test.*Profile|Test.*Resolve' -count=1` | Preserves Go recover planning and live execution: handle normalization, final identity naming, same-handle candidate selection, excluded identity conflict detection, stable same-handle sorting, temp identity name selection, backup path preview, `recover_handle` vs `send_recover_otp` action selection, command schema flags, parser/dispatch mapping, and the warning that global `--identity` is ignored. Non-dry-run without OTP posts `/user-service/handle/rpc` `send_otp` and does not write local identity state. Non-dry-run with OTP generates a handle-path e1 DID, snapshots `.legacy-backup/recover-handle`, posts `/user-service/did-auth/rpc` `recover_handle`, stages the recovered identity under a temporary name, merges old-owner SQLite state into the final owner DID/name, promotes the temp identity into the final identity, removes internal temp fields from public output, exposes merge/cleanup counts, and emits the archived same-handle warning. No dependency was added. |
| `awiki-cli/internal/identity/client.go`, `awiki-cli/internal/identity/service.go` remote wire portions | user-service identity endpoint constants, identity JSON-RPC/REST request builders, service error display/conversion, helper normalization, handle lookup nil normalization, trace phases, and deterministic command result shapes | `crates/awiki-cli/src/identity/{client,wire}.rs`, `crates/awiki-cli/tests/identity_wire_contract.rs` | `system_verified` | `cargo +1.79.0 test -p awiki-cli --test identity_wire_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test identity_register_email_live_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test identity_replace_did_live_contract --locked`; Go `go test ./internal/identity -count=1` | Translates the identity remote contract for `/user-service/did-auth/rpc`, `/user-service/handle/rpc`, `/user-service/did/profile/rpc`, email status/send, and phone bind REST endpoints. Covers `lookup`, `send_otp`, `register`, `recover_handle`, `replace_did`, profile `resolve`/`get_public_profile`/`get_me`/`update_me`, refresh `get_me` with `AuthRefresh`, phone/email/OTP normalization, profile payload mapping (`nick_name` vs `display_name` changed field), live `replace_did` null semantics for empty optional role/endpoint URL, full identity result JSON, summaries, and warning preservation. Current live consumers now use these builders/client helpers for phone/email register, refresh-token, bind, recover, replace-did, profile set/get, and resolve, including unauthenticated REST POST/GET for registration email activation, authenticated JSON REST POST, and bearer REST GET for bind. Shared profile timeout caps and shared RPC/JWT trace phases now apply to identity JSON-RPC, REST, bearer GET, and auth refresh calls; deeper handle lookup trace wiring remains separate. No dependency was added. |
| `awiki-cli/internal/anpsdk/registry.go` | transport-free authentication/proof/direct-E2EE facade aliases, key-material convenience helpers, and direct-E2EE file session/prekey/pending stores | `crates/awiki-cli/src/anpsdk.rs`, `crates/awiki-cli/tests/anpsdk_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test anpsdk_contract --locked`; `cargo +1.79.0 test -p awiki-cli --test authsdk_contract --locked`; Go `go test ./internal/anpsdk -count=1`; Go secure store/listener focused guards; dependency audit | Expands the Rust facade over local `../anp/rust` with existing public Rust SDK equivalents for Go registry authentication/proof/direct-E2EE symbols and Go-shaped PascalCase spellings: `ModulePath`, `ModuleVersion`, `DidProfileE1/K1`, `AuthModeHTTPSignatures/Auto`, `BuildANPMessageService`, `CreateDidWBADocument`, DID resolver aliases, `GenerateAuthHeader`, `GenerateHTTPSignatureHeaders`, `NewDIDWbaAuthHeader`, `NewDidWbaVerifier`, IM proof/RFC9421 type aliases, proof builder/verifier aliases, DID-WBA binding/group receipt aliases, and `TargetKindAgent/Group/Service`. Adds Go-shaped key-material facade helpers for `KeyType`, `KeyTypeSecp256r1`, `KeyTypeSecp256k1`, `KeyTypeEd25519`, `KeyTypeX25519`, `GeneratedKeyPairPEM`, `GenerateKeyPairPEM`, `PrivateKeyFromPEM`, and `PublicKeyFromPEM`: generation returns standard PKCS#8 `PRIVATE KEY` and SPKI `PUBLIC KEY` PEM for ed25519, secp256k1, secp256r1, and x25519; runtime parsers keep rejecting legacy `ANP ...` PEM labels. Adds CLI-side file store facades for Go `NewFileSessionStore`, `NewFileSignedPrekeyStore`, `NewFileOneTimePrekeyStore`, and `NewFilePendingOutboundStore`: Go-shaped root directory creation, `.pem` private key files, pretty `.json` metadata without trailing newlines, `latest.txt` signed-prekey pointer trimming/missing semantics, sorted one-time prekey listing, missing-key error strings, missing-ok deletion, pending outbound not-found mapping, and Rust SDK trait implementations where local traits exist. `MessageServiceE2EEClient` / `NewMessageServiceDirectE2eeClient` remain tracked in the message secure-client row instead of being re-exported from `anpsdk`, because that high-level adapter depends back on CLI message/auth/store wiring rather than being a mechanical SDK registry alias. No dependency was added; files stay under the default 1200-line review-size cap. |
| `awiki-cli/internal/anpsdk/registry.go`, `awiki-cli/internal/authsdk/session.go` local token subset | ANP facade constants and auth session bearer scope/token persistence | `crates/awiki-cli/src/anpsdk.rs`, `crates/awiki-cli/src/authsdk/mod.rs`, `crates/awiki-cli/tests/authsdk_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test authsdk_contract --locked`; Go `go test ./internal/authsdk -run 'TestCaptureTokenPersistsOnlyConfiguredScopes|TestCaptureTokenStillAcceptsLegacyAuthorizationResponseHeader' -count=1`; dependency audit | Adds a narrow Rust facade over local `../anp/rust` for Go ANP module path/version and DID-WBA auth types, then ports the local `Session` token semantics from Go: persistent server scopes use hostname fallback, `set_bearer` seeds helper tokens and marks the scope, `capture_token` accepts `Authentication-Info` access tokens and legacy `Authorization: Bearer` response headers, only remembered scopes update/persist the current JWT, `clear_token` clears current JWT only for remembered scopes, 401 retry policy delegates to the ANP helper, and HTTP/RPC error display strings match Go. Later authsdk rows add request header and HTTP execution behavior; the identity refresh-token command now consumes `EnsureJWT` live, while full `internal/anpsdk/registry.go` alias coverage and broader endpoint-specific identity/message clients remain deferred. No dependency was added. |
| `awiki-cli/internal/authsdk/session.go` request auth header helpers | `Headers`, `ChallengeHeaders`, bearer-cache bypass, JSON base header signing, and 401 challenge signing | `crates/awiki-cli/src/authsdk/mod.rs`, `crates/awiki-cli/tests/authsdk_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test authsdk_contract --locked`; `cargo +1.79.0 test -p awiki-cli authsdk --locked`; Go `go test ./internal/authsdk -count=1`; dependency audit | Adds the Go-shaped request header boundary on top of the local ANP Rust SDK: `Session::headers` starts with `Content-Type: application/json`, delegates to `DIDWbaAuthHeader::get_auth_header`, merges returned auth headers, reuses cached bearer tokens when `force_new=false`, and bypasses the cache for signed force-new requests. `Session::challenge_headers` passes the JSON base headers into `get_challenge_auth_header` so `Accept-Signature` can include `content-type`, while returned headers stay auth-only like Go. Tests generate real DID documents/private keys, assert `Signature-Input`, `Signature`, `Content-Digest`, key id, cached bearer reuse, force-new signing, server nonce reuse, and the later HTTP-execution row verifies the 401 retry loop uses this helper. No dependency was added. |
| `awiki-cli/internal/authsdk/session.go` JSON-RPC, DID-auth result, and HTTP execution helpers | JSON-RPC payload/response shape, HTTP status error trimming, header flattening, JSON content-type header, `DoJSONRPC`, `DoJSON`, `doRequest`, one-shot 401 retry, response token capture, `EnsureJWT` HTTP flow, and shared phase hooks | `crates/awiki-cli/src/authsdk/{mod,wire}.rs`, `crates/awiki-cli/tests/authsdk_contract.rs` | `unit_verified` | `cargo +1.79.0 test -p awiki-cli --test authsdk_contract --locked`; Go `go test ./internal/authsdk -count=1` | Translates Go `DoJSONRPC`, optional nil-output JSON-RPC, `DoJSON`, optional nil-output JSON, private `doRequest`, `flattenHeaders`, and `EnsureJWT` on top of the shared Rustls/std `transportcfg::HttpClient`: fixed JSON-RPC envelope (`jsonrpc=2.0`, `id=req-1`), RPC error mapping to `RpcError`, HTTP body trimming for `HttpError`, first-header-value response flattening, JSON content type, initial signed/bearer request, one 401 retry through challenge headers or cleared-token force-new signing, successful response token capture before body/result fallback, body access-token persistence, already-captured header token fallback, stored-token fallback, then Go missing-token error string. Profile-aware variants now cap request response-read timeouts for service RPC and auth refresh calls. Shared trace hooks record normal JSON-RPC/plain JSON operations and allow caller-labeled JWT refresh phases while suppressing nested `business_rpc:get_me` inside `EnsureJWT`. No dependency was added. |
