# File Size Exceptions

Rust source files should target 1200 non-generated lines by default. The normal relaxed limit is 3000 lines when the Go source is large or a traceable translation owner would otherwise become less reviewable. Files above 3000 lines are rare special exceptions only and require a documented reason instead of becoming the normal module shape. The older structure checker still reports files over 1200 lines so large translation-time owners stay visible during review.

| Rust path | Rust lines | Go path | Go lines | Reason |
| --- | ---: | --- | ---: | --- |
| `crates/awiki-cli/src/runtime/listener_supervisor_run.rs` | 2030 | `awiki-cli/internal/runtime/listener/server.go` | 1802 | Foreground listener execution remains a large traceable 1:1 runtime owner for run/service-run startup, bridge ownership, WebSocket session loops, secure notification normalization, local secure ACK recovery, queued local notification ownership/flush, queued secure outbox flushing, secure backlog replay, direct/group incoming contact lookup wiring, bridge-created session bootstrap waiting, startup-known-session bootstrap waiting, per-session startup error recording, listener-connect secure prekey retry through Go's non-session publisher path, and the live session-owner shutdown/reader-error status path. The session-RPC sender/registry/request/pending lifecycle was split into `listener_session_rpc.rs` so future runtime work has a smaller non-overlapping edit surface, but this file remains above the older 1200-line visibility threshold and below the current ordinary 3000-line target because it still owns the integrated foreground/session orchestration translated from oversized Go `server.go`. New helper behavior should stay in smaller modules where practical; bridge bootstrap waiting lives in `listener_bridge_runtime.rs`, known-session startup decision/error algebra lives in `listener_known_sessions.rs`, foreground OS signal shutdown handling lives in `listener_shutdown_signal.rs`, session-RPC lifecycle lives in `listener_session_rpc.rs`, WebSocket pending-dispatch helper state lives in `listener_wsclient.rs`, WebSocket poll-read timeout and shutdown-aware ping support lives in `listener_ws_transport.rs`, local notification queue/flush primitives remain in `listener_local_notifications.rs` and `listener_local_notification_flush.rs`, and prekey retry decision algebra remains in `listener_session_loop.rs`. |
