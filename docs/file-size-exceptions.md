# File Size Exceptions

Rust source files should normally stay below about 3000 non-generated lines. Smaller files remain preferred, and exceptions above that target require a documented reason instead of becoming the normal module shape. The older structure checker still reports files over 1200 lines so large translation-time owners stay visible during review.

| Rust path | Rust lines | Go path | Go lines | Reason |
| --- | ---: | --- | ---: | --- |
| `crates/awiki-cli/src/runtime/listener_supervisor_run.rs` | 2334 | `awiki-cli/internal/runtime/listener/server.go` | 1802 | Foreground listener execution currently mirrors the oversized Go supervisor file for traceable 1:1 translation of run/service-run startup, bridge ownership, WebSocket session loops, secure notification normalization, local secure ACK recovery, queued local notification ownership/flush, queued secure outbox flushing, secure backlog replay, direct/group incoming contact lookup wiring, bridge-created session bootstrap waiting, startup-known-session bootstrap waiting, per-session startup error recording, real notification ping-loop wiring, bridge RPC reuse of the active session WebSocket through a single-reader RPC channel, secure backlog polling through that shared session RPC channel with Go's 15-second sync timeout boundary, secure E2EE client factory RPC reuse for notification normalization/ACK/outbox paths, listener-connect secure prekey retry through Go's non-session publisher path, and the live session-owner shutdown/reader-error status path. This is an intentional translation-time large-owner record; the file remains below the current ordinary 3000-line target, but is still listed because the checker keeps the older 1200-line visibility threshold. Splitting this runtime owner into smaller Rust files is a later optimization/refactor task, not mixed into the parity slice. New helper behavior should stay in smaller modules where practical; secure replay wiring reuses existing helper modules instead of carrying duplicate inline parsing/scanning logic, bridge-created sessions reuse the Supervisor host-notify sink and local-notification queue through small helpers, bridge bootstrap waiting uses `listener_bridge_runtime.rs` for one-shot signal/wait semantics rather than growing this file with another state machine, known-session startup decision/error algebra lives in `listener_known_sessions.rs`, foreground OS signal shutdown handling lives in `listener_shutdown_signal.rs`, WebSocket pending-dispatch helper state lives in `listener_wsclient.rs`, WebSocket poll-read timeout and shutdown-aware ping support lives in `listener_ws_transport.rs`, local notification queue/flush primitives remain in `listener_local_notifications.rs` and `listener_local_notification_flush.rs`, and prekey retry decision algebra remains in `listener_session_loop.rs`. |
