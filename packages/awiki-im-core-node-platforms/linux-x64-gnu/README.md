# @awiki/im-core-node-linux-x64-gnu

`@awiki/im-core-node` 的 Linux x64 GNU Tier 1 原生包。它只由 root wrapper 作为 optional
dependency 加载，不得直接作为业务 API 使用。最低运行基线为 Linux kernel 4.18、glibc
2.28，并按 AGPL-3.0-only 分发。
